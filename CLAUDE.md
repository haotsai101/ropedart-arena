# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Agent roster

Three agents are available. Pick the right tier — over-spending on the senior agent for a string fix wastes tokens.

| Agent | Model | Use for |
|---|---|---|
| `godot-engineer` | Sonnet | New systems, multi-file features, bug diagnosis, anything needing `run_project` verification |
| `godot-junior` | Haiku | Mechanical single-file edits: string fixes, constant tweaks, adding a key handler, renaming a property. No run-verify. |
| `game-ui-designer` | Sonnet | HUD layout, menus, Tween animations, particle VFX, screen shake, visual polish |

**Decision rule:**
- Can you specify the exact file + line + change? → `godot-junior`
- Does it touch UI feel, animations, or visual feedback? → `game-ui-designer`
- Everything else → `godot-engineer`

**Fork instead of spawning** when the relevant files are already loaded in the conversation context and the task is diagnosis-only (no code changes needed). A fork shares the prompt cache and avoids re-reading files already in context.

## Running the game

Open Godot 4.7 (standard build, not .NET) → Import → select `project.godot` → F5.

There is no CLI build or test runner. All iteration happens in the Godot editor. The Godot MCP server (`godot` tool) is configured and can be used to inspect the scene tree and node properties without switching to the editor.

## Architecture: 2D logic, 3D rendering

**The invariant that governs every script**: all gameplay math (movement, collision, kill detection, rope dart flight) runs as `Vector2` on the XZ plane. `x` maps to world X, `y` maps to world Z. The 3D transform is purely for rendering. Never introduce vertical (Y-axis) gameplay.

`get_pos_2d() -> Vector2` on `player.gd` is the canonical way to read any player's gameplay position. All kill checks and bot targeting use this.

## Game systems and their wiring

**`GameManager` (autoload)** — `scripts/game_manager.gd`
- Singleton accessible as `GameManager` from any script.
- Owns the round state machine: `LOBBY → COUNTDOWN → PLAYING → ROUND_END → MATCH_END`.
- Spawns all player instances at startup via a 0-second `SceneTreeTimer` (fires next frame, after `change_scene_to_file` has fully applied). Players are added as children of `get_tree().current_scene`.
- Key exports: `total_players`, `human_count`, `bot_difficulty`, `lives_per_round`, `rounds_to_win`.

**`player.gd`** — `CharacterBody3D`, one instance per player
- Input: `player_index == 0` → keyboard (WASD/arrows/Space); `player_index >= 1` → gamepad `player_index - 1`. When `is_bot == true`, input is delegated to `bot_controller` via `get_desired_move()` / `get_desired_aim()` / `get_desired_throw()`.
- Movement is blocked (velocity zeroed) when `GameManager.current_state != PLAYING`.
- `_apply_rope_leash_velocity_clamp(delta)`, called right BEFORE `move_and_slide()` every physics tick (as of ROUND 19, 2026-07-28; previously `_clamp_to_rope_leash()`, called AFTER `move_and_slide()`): once the owner's rope dart is `ANCHORED`, keeps the player from walking further from the anchor than the rope allows — a real tether limit, not just a visual one. **As of ROUND 21 (2026-07-29, see that round's own dated entry below), the boundary is wrap-aware again**: while at least one physics-chain segment genuinely rests against real obstacle contact, `_rope_leash_pivot_and_radius()` pivots on the chain's own first dynamic segment with the radius shrunk by `_rope_chain_rest_length_2d()` (a value read from the chain's REAL, already-simulated positions); otherwise it falls back to a plain circle of radius `DART_ROPE_LENGTH` around the anchor. (ROUND 20 had briefly made this bound unconditionally flat — `[anchor, DART_ROPE_LENGTH]` regardless of wrap state — per a direct user requirement at the time; ROUND 21 restored the wrap-aware branch after a follow-up report that the flat bound let the rope's real, physically-wrapped length exceed its own fixed budget. See ROUND 21's own entry for why this is not a reversal of ROUND 19/20's standing preference.) **As of ROUND 19, HOW that boundary is enforced (independent of WHAT the boundary is) is a velocity projection, not a position snap, and this is UNCHANGED by ROUND 21**: `_apply_rope_leash_velocity_clamp()` only ever reduces the OUTWARD-radial component of the player's already-computed `velocity`, before `move_and_slide()` runs, so the player's own canonical position is never discontinuously moved. `DART_STATE_ANCHORED` / `DART_ROPE_LENGTH` mirror rope_dart.gd's `State.ANCHORED` ordinal and `ROPE_LENGTH` by hand (no shared constant between the two scripts, same as `HITBOX_DEBUG_RADIUS`).
  - **ROUND 7 SPURIOUS-SNAP FIX (real user report, direct quote: "the throwing pushed me southeast and walking west pushed me north west") — the pivot-on-first-segment clamp gates on real obstacle contact now, not on the chain existing at all.** Investigated with direct instrumentation per the user's own explicit ask, not guessing: a new temporary regression test, `tests/test_movement_direction_skew.gd`/`.tscn` (kept in the suite), drives a real player through the actual unmodified `_get_move_input()`/`_physics_process()` pipeline — pure `KEY_A` (west) held via `Input.parse_input_event()`, no dart — and separately calls `_throw()` with zero movement input held, logging `get_pos_2d()`/`velocity` every tick both times.
    - **Phase 1 (pure movement, no dart) came back perfectly clean**: zero Z drift over 120 ticks, exact `-X`-only motion matching `move_speed` to 5 decimal places. The core "movement is a pure `Vector2` on X/Z" invariant was never actually violated anywhere — `_get_move_input()` is a plain axis-aligned `Vector2(D-A, S-W)` with no camera-relative or facing-relative transform applied to it at any point, confirmed both by reading every `global_position`/`velocity` write site in `player.gd` (only three: the dash/movement branch, and the two `_clamp_to_rope_leash()` clamps below) and by this direct measurement.
    - **Phase 2 (throw, zero movement held) was NOT clean**: a real, un-input-driven `get_pos_2d()` displacement of ~0.5 units, arriving over many ticks right at/after the dart's `FLYING`→`ANCHORED` transition, in a direction unrelated to aim/facing/movement — i.e. throwing alone visibly "pushed" the player, exactly matching the report (the exact compass direction varies by throw since it's driven by the physics chain's own settling, not aim — this session's own repro happened to land roughly NE for a north-aimed throw, but the mechanism is direction-agnostic).
    - **Root cause, pinned down by also logging `rest_len`/`max_from_first`/`offset0_len` every tick**: `_rope_chain_rest_length_2d()` — the live chain's own segment-to-segment path length from the first dynamic segment to the tip — legitimately and momentarily exceeds `DART_ROPE_LENGTH` right at the anchor transition (measured 8.22–8.46 vs. the chain's own 8.0 capacity), before the still-bunched-near-the-hand chain has caught up to the tip anchor's newly-landed position. That's the same class of transient overshoot this codebase already tolerates elsewhere (see `tests/test_rope_leash_corner_wrap.gd`'s own `MAX_ACCEPTABLE_OVERSHOOT = 0.75`) — but `maxf(DART_ROPE_LENGTH - rest_len, 0.0)` floored the result to a literal ZERO-radius budget, forcibly snapping the player's canonical root position onto the first segment's own still-unsettled location, repeatedly, for as long as `rest_len` stayed elevated. A first attempted fix (gate only on `rest_len < DART_ROPE_LENGTH`, skip the clamp when there's no positive budget at all) measurably helped — re-run of the same probe showed the worst single-tick zero-radius snap eliminated — but did NOT fully close the bug: total drift only dropped from ~0.53 to ~0.47, because even once `rest_len` dipped back under capacity, the "first segment" itself was still being actively dragged >1 unit by the joint solver as the whole chain settled from its bunched spawn layout, so pivoting on it was unreliable independent of whether the budget was mathematically positive.
    - **The actual fix**: the pivot-on-first-segment bound only makes physical sense while the chain is genuinely wrapped around a real obstacle corner — its own ROUND 6 doc comment already says so ("a segment resting against real obstacle contact is exactly where the solver actually put it"). An open-air anchor (no obstacle at all — including every anchor reached by hitting max `ROPE_LENGTH` with nothing in the way, the common case since `rope_dart.gd` anchors there instead of auto-recalling) was never meant to be constrained by this bound beyond the plain fallback circle below, which the same probe confirmed never itself misfires (`offset_len` stayed safely at/under `DART_ROPE_LENGTH` throughout every logged tick of both rounds). The whole pivot clamp is now gated on `rope_segment_body.gd`'s existing `_debug_last_has_contact` (added for the ROUND 5 CLIPPING FIX, already established there as an unambiguous "resting against real obstacle geometry, not a player/ground/another segment" signal) being true for at least one segment in the chain — i.e. only trust "pivot on the first segment" once there's an actual corner for it to legitimately rest against. Purely open-air anchors and the ordinary mid-settle window right after any throw now never engage the stricter bound at all, falling through to the plain fallback circle, which is exactly correct for them.
    - **Verified via the same probe, re-run after this fix**: Phase 2 max drift dropped to exactly `0.00000` across the whole `FLYING`→`ANCHORED` window and stayed there — `_debug_last_has_contact` correctly reads false throughout (no obstacle involved), so the pivot clamp never fires even while `rest_len` visibly exceeds capacity (still logged, unchanged, as a no-op diagnostic); the fallback circle alone handles the whole scenario with zero misfires. Re-ran Phase 1 unchanged (still exactly clean) to confirm the fix touches nothing on the no-dart path.
    - **Regression-checked against `tests/test_rope_leash_corner_wrap.gd` (the test this exact clamp exists for)**: re-run three times with the fix, and once against a clean `git stash`-restored baseline (this round's diff removed) for comparison, following this project's own established temp-testing convention. The baseline run itself already FAILED this test, and by a wide margin (`max_pillar_penetration=0.517`, `max_fold_jump=1.71`, 5 fold events, and the chain fully collapsed to `chain_len=0.00` partway through the sweep) — i.e. this specific corner-wrap regression test was already broken on this branch's committed HEAD before this round touched anything, the same class of pre-existing run-to-run physics flakiness this project's CLAUDE.md already documents finding and disclosing in earlier rounds (see the ROUND 6/8 entries below). Both runs with this round's fix applied scored at or below the baseline on every metric (`max_pillar_penetration` 0.019 and 0.334 respectively, both < baseline's 0.517; fold events 0 and 2, both < baseline's 5) — a net improvement in every sample taken, never a regression, though not a full fix of that separate, pre-existing, out-of-scope issue (gating on real contact only helps once contact is actually detected; the residual penetration/fold numbers there come from `rope_segment_body.gd`'s own clamp-vs-solver interaction during an aggressive circling sweep, unrelated to this round's `player.gd`-only change).
    - **Also likely explains the earlier, separate "character moving weirdly when the dart is anchored" question from the same session**: once a dart is anchored and the player is near the tether's edge, ordinary chain jitter/tension-clamp settling can transiently push `rest_len` back over `DART_ROPE_LENGTH` the exact same way mid-walk, independent of and unrelated to whatever direction is actually held — consistent with "walking west pushed me northwest." Both reports trace to the same single root cause and the same single fix; not treated as two separate bugs.
    - **Honest confidence caveat**: the canonical position fix (Phase 1 and Phase 2 of the new test) is verified deterministically and repeatably — zero drift, reproduced twice after the fix. Visual/on-screen confirmation is unconfirmed (no screenshot/video access this session), but since the bug was a real change to `get_pos_2d()`/`global_position` (not a rendering artifact), the numeric fix directly addresses what would have been visible on screen.
- Owns the rope itself, in two entirely different forms depending on `dart == null`, both driven from `_update_persistent_rope()` (called from `_process()`/`_physics_process()`):
  - **Idle** (`dart == null`): the old cheap look — `_rope_segments` (`ROPE_SEGMENTS` = 16 `MeshInstance3D` cylinders, built once in `_setup_dagger_in_hand()`) redrawn every frame as a tight coil around the forearm (`_render_rope_coiled()`, anchored at a `lowerarm.r` `BoneAttachment3D`). Purely kinematic/procedural — there's no obstacle to avoid while the rope is just sitting on the character's own arm.
  - **Thrown** (`dart != null`): a **real physics-simulated rope**, not scripted geometry. `_spawn_physics_rope()` builds a chain of `ROPE_PHYSICS_SEGMENTS` (8) `RigidBody3D` collision links (capsule `CollisionShape3D` only — no mesh of their own, see the VISUAL bullet below) between two kinematic (`freeze_mode = FREEZE_MODE_KINEMATIC`) endpoint bodies: a hand anchor and a tip anchor. `_update_physics_rope_anchors()`, called every `_physics_process()` tick, drives the hand anchor to `_get_rope_hand_anchor_pos()` and the tip anchor to the dart's actual rendered pommel position (`_get_rope_tip_target()`) — so Godot's own solver, not any script, is what keeps the rope off pillars/trees/cacti and makes it drape/rest against them when the owner ends up on the far side of an obstacle from an anchored dart. `_free_physics_rope()` tears the whole chain down once `dart` goes back to `null` (recall complete, kill, round reset).
  - **Joints are raw `PhysicsServer3D` pin joints (`_join_rope_pin()`, via `PhysicsServer3D.joint_make_pin()`), NOT `PinJoint3D` nodes.** This is a deliberate, root-cause-driven fix, not a stylistic choice: a first version used `PinJoint3D` nodes, whose single-shared-`global_position` setup implicitly derives ONE local anchor offset per body from wherever the joint node and both bodies happened to be at the moment the joint was configured. That version visibly rendered as short, disconnected dashes in a real user screenshot ("each segment completely unattached") — root cause, confirmed by explicitly measuring the real world-space gap at every joint (not just checking for engine errors, which showed none): every body was spawned clustered within ~0.16 units of the hand at identity rotation, so the auto-captured offsets landed near each body's own *center* rather than its true capsule *end*, meaning the solver was actually enforcing "keep body centers near each other," not "keep consecutive ends touching" — a permanent, steady-state wrong equilibrium, not just a fast-motion artifact (confirmed since the user's screenshot showed a stationary/`ANCHORED` dart). The fix: `PhysicsServer3D.joint_make_pin(joint, body_a, local_a, body_b, local_b)` takes each body's local anchor point as an **explicit, independent** argument, so every segment's true end (`Vector3(0, ±HALF_LEN, 0)`) and both anchors' own origin (`Vector3.ZERO`) can be declared correctly regardless of whether the bodies happen to coincide in world space when the joint is created — which they generally don't at throw-instant (dart starts at the hand, chain is laid out at its full fixed length), and that's fine: the mismatch is exactly the intended "rope pays out from the hand as the dart flies away" slack, not a bug. Segments are also now spawned with correct initial rotation (local Y aligned along the hand→tip layout direction) instead of defaulting to identity/pointing straight up.
  - **Joint bias/damping are left at `PhysicsServer3D`'s own defaults (0.3 / 1.0) — do not stiffen these without re-measuring.** A first attempt at addressing the disclosed fast-motion risk below tried tuning these stiffer (bias 0.9, damping 2.0); measured via a temporary per-tick gap probe, that config caused the chain to explode — the real per-joint gap grew *exponentially*, from single digits to tens of trillions of units within a couple seconds, on an otherwise-idle chain with no unusual input. Reverting to the defaults and re-measuring the same way showed gaps consistently settling to ~0.02–0.1 units at rest, with brief (few-tick) spikes to ~10–12 units at the instant of a throw that reliably decayed rather than diverging.
  - **Segments have `gravity_scale = 0.0` and are hard-locked to the dart's `plane_y` height every physics step (`scripts/rope_segment_body.gd`, attached to every dynamic segment via `_integrate_forces()`, NOT `PinJoint3D`/`gravity_scale` alone).** Root-caused from a real user SCREEN RECORDING (not a screenshot): once anchored, the rope visibly collapsed to a tiny ~0.3–0.5 unit stub next to the dagger within moments and stayed that way for the rest of the clip, sagging downward, even with the character standing several grid cells away. Root cause: default gravity (`gravity_scale = 1.0`, never overridden) pulled every light (`ROPE_SEGMENT_MASS = 0.03`) segment down every step, and the deliberately un-stiffened joint bias/damping (see above — stiffening was already tried once and found to explode the chain) only *softly* corrects position error each step, so it couldn't keep the chain taut against a continuous downward force across a real multi-unit span — it sagged into a different equilibrium instead. This is also why the PREVIOUS fix's own verification (a gap probe measuring only LOCAL adjacent-segment gaps, run for a few seconds right after a throw) never caught it: local gaps stayed small even while the whole chain sagged as a unit; nothing measured whether the chain's TOTAL reach still matched the real hand-to-dart distance over a sustained hold. Per the user's own explicit, verbatim requirement — "I want the rope to disregard gravity and live on a plane" — every segment now has `gravity_scale = 0.0` (the literal "disregard gravity") AND `rope_segment_body.gd`'s `_integrate_forces(state)` override forces `state.transform.origin.y` back to a fixed `locked_y` (read once from `dart.plane_y` at spawn — see `_get_rope_plane_y()`) and zeroes `state.linear_velocity.y`, every physics step — Godot's own documented, solver-safe way to override a `RigidBody3D`'s transform each step (poking `global_position` from outside physics processing, e.g. once per `_physics_process()` tick the way the kinematic anchors are driven, is explicitly discouraged by Godot's docs as producing "unpredictable behavior"). The kinematic hand anchor is ALSO now Y-locked to the same `plane_y` (`_get_rope_hand_anchor_pos()`, X/Z from the real animated hand bone but Y forced flat) rather than following the hand bone's real bob/animation height — otherwise the joint to the first (already Y-locked) segment would fight a small constant Y mismatch every single step. **Verified by direct measurement, not just no-errors**: a temporary probe printed the real hand-to-tip distance vs. the chain's actual reach (max distance from the hand anchor to any segment/tip) every ~1 second, specifically while `ANCHORED` (not just right after a throw), sustained across ~90 seconds total of bot play — chain reach tracked the real span continuously the whole time (e.g. one bot's anchored dart: real span shrank from 5.966→2.558 as it walked closer, chain reach followed in lockstep every sample), no collapse-toward-a-point ever reproduced. A separate simultaneous probe confirmed `max_y_drift` was exactly `0.0000` on every single sample. The only residual (expected, benign) deviation seen: on a short throw (small real span), chain reach can exceed the real span by 1–3 units — the fixed 8-unit rope has to go somewhere with gravity off, so it bulges/curves within the XZ plane rather than hanging straight — this is the disclosed "visible slack on close throws" behavior, not a bug, and is the *opposite* failure mode from the collapse this fix addresses.
  - **VISUAL: the thrown rope is rendered as one continuous tube mesh, NOT one `CylinderMesh`/capsule per physics segment.** Per an explicit follow-up user request ("Can the rope be rope instead of segments of bars?"), `_update_rope_tube_mesh()` (called every `_process()` frame from `_update_persistent_rope()`) rebuilds a single `MeshInstance3D` (`_physics_rope_tube_mesh`) each frame: it takes `[hand anchor, every dynamic segment's center, tip anchor]` as control points (same order as the joint chain), samples a Catmull-Rom curve through them at `ROPE_TUBE_CURVE_SAMPLES` (48) steps via `Vector3.cubic_interpolate()`, then extrudes a round `ROPE_TUBE_RADIAL_SEGMENTS`-sided (8) tube of constant `ROPE_RADIUS` along that curve via `SurfaceTool` (`_build_tube_mesh()`). This is a **pure rendering decoupling** — the underlying `RigidBody3D` segments, their capsule collision shapes, and the whole plane-lock/no-gravity mechanism above are completely unchanged; only what gets drawn from their positions changed, from N disjoint capsule meshes with visible seams at every joint to one smooth surface. `set_surface_override_material()` must be applied AFTER `mi.mesh` is assigned inside `_build_tube_mesh()` (not on the freshly-created, still-meshless `MeshInstance3D`) — Godot errors ("Index p_surface = 0 is out of bounds") on a mesh with zero surfaces.
  - **`_physics_rope_tube_mesh` MUST have `top_level = true`.** Root-caused from a real user screen recording, frame-analyzed directly (not just a screenshot): while the character stood still, a dark rope-like curve floated in open space near them, disconnected from both the character and the dart, reshaping into a visibly different distorted curve EVERY SINGLE FRAME (an open curl, then a tight C-arc, then a zigzag, then an S-curve). A separate live playtest report the same round — "the dart is flying correctly but the rope is not" — turned out to be the exact same bug, just observed during FLYING instead of after a throw ended. Root cause: the tube mesh's control points (`_update_rope_tube_mesh()`) are all read from `.global_position` — already WORLD-space coordinates — but the `MeshInstance3D` itself was a plain (non-`top_level`) child of `self` (the player, a moving/constantly-rotating `CharacterBody3D`), so Godot rendered its already-global vertex data through `player.global_transform * vertex` — double-transforming it. The result visibly rotates/warps in lockstep with the PLAYER's own facing direction (which changes continuously while aiming/tracking a target, via `_facing_dir`/`aim_dir`), not with the rope's real physics position — exactly matching a reshaping curve unconnected to the actual chain. Fix: `mi.top_level = true`, set once right after the `MeshInstance3D` is created in `_update_rope_tube_mesh()` — this makes the node's `global_transform` NOT inherit from its parent at all, so the already-global vertices render at their literal positions with no further transform needed. (The underlying physics bodies were never affected by this bug — they're parented under `_physics_rope_root`, a separate non-moving `Node3D`, specifically to avoid this exact class of problem; only the tube-mesh rendering code added in the previous round missed applying the same principle.) The mesh is still a persistent child of `self` created once (not of `_physics_rope_root`, which gets torn down and rebuilt every throw) so its own node lifecycle stays independent of the physics chain's — `top_level` only changes how its transform is computed, not its parent/ownership.
  - **A second hypothesis raised the same round — an orphaned/leftover physics chain from an incomplete `_free_physics_rope()` — was investigated and NOT found to be a real bug.** Every path that sets `dart = null` (`kill()`, `reset_for_round()`, `_on_dart_returned()`) is reachable from `_process()`'s unconditional `_update_persistent_rope()` call (which runs before any `is_dead`/`is_falling` early-return), and `_update_persistent_rope()` itself calls `_free_physics_rope()` whenever `dart == null and _physics_rope_active` — so cleanup fires on every code path, not just the “normal” recall/pickup one. Verified via a temporary scene-tree census (counting nodes named `PhysicsRopeChain_<player_index>` under the arena root) sustained across ~2400 frames (~40s) of hard-bot rapid-fire throw/recall/pickup cycling across 3 simultaneous players — chain count never exceeded 1 per player. The visual symptom the user saw was fully explained by the `top_level` bug above; this hypothesis, while reasonable given the visual evidence alone, was not the actual cause.
  - **Verification note**: confirmed by actually measuring real world-space quantities every tick via temporary probes (never just "no errors printed," which had already failed to catch bugs from earlier rounds) — joint gap, then chain-reach/Y-drift, then (this round) a scene-tree orphan census plus a `mesh_basis_identity`/rotation-decoupling check sampled every few frames across many dozens of real throw cycles. **Still not verified visually** — no screenshot/video access in this session, so whether the tube mesh now actually renders as a stable, correctly-positioned rope on screen (rather than just being numerically decoupled from the player's rotation, which is what was directly measured) remains unconfirmed pending a fresh screenshot or recording.
  - **Known, disclosed limitation**: the chain is simulated for the dart's *entire* time out (FLYING/ANCHORED/RECALLING alike), including while the tip anchor is being dragged at up to 18–36 units/sec during a fast throw or recall. Measured gap spikes during this (~10–12 units, decaying within a few ticks, from an earlier fix round's own probe) never diverged, but how that transient actually looks on screen — now through the tube mesh — is unverified. If the user reports jitter specifically during the brief FLYING/RECALLING window, or a twisted/faceted tube look, that's the area to revisit first — and re-run a direct-measurement probe rather than guessing.
  - **UNSPOOL/"reeling out" FIX (per explicit user direction, from a real screen recording reviewed frame-by-frame — not a screenshot): the rope no longer appears pre-extended to its full length the instant a throw fires.** Root cause, confirmed by the coordinator's own frame review before any code changed: `_spawn_physics_rope()` used to lay every one of the 8 dynamic segments out along `span_dir` to the chain's full fixed `ROPE_PHYSICS_SEGMENT_LENGTH`-per-segment layout *regardless of the dart's real (usually near-zero, right at throw-instant) distance from the hand* — correctness never depended on this (the zero-baked-in-offset joint design means each joint's local anchors are declared independently of the bodies' actual world positions at spawn), but the visual result was exactly the user's complaint: the whole 8-unit rope appeared at once, already long, with only the *unused* length showing as slack/droop, never visibly growing out of the hand as the dart travels further. Fix: segments are now spawned **bunched near the hand** instead (`ROPE_BUNCH_SPACING` = 0.06, only a few centimeters apart, in `player.gd`'s `_spawn_physics_rope()`), letting the kinematic tip anchor (already tracking the dart's live position every tick, unchanged) drag the chain out progressively as joint constraint violations resolve tick by tick, rather than laying the whole capacity out up front.
    - **This intentionally reintroduced the same class of instability risk the codebase had already flagged and rejected a stiffer fix for once before (see `ROPE_PHYSICS_*`'s "bias 0.9 explodes the chain" note above) — and it was real, not hypothetical.** Verified via a temporary per-tick probe (printing real hand-to-dart distance vs. the chain's own actual max reach from the hand, sampled for the first 40 physics ticks after every throw, across dozens of real bot throws): with bunched spawn and NO additional mitigation, the chain reliably underwent a genuine multi-tick "crack the whip" resonance — `chain_max_reach` shot up to **~4x the total rope length** (30+ units against a real hand-to-dart distance of ~4, one single tick alone moving a segment ~7 units / ~420 units/sec) before slowly decaying back down over another 20–30 ticks. Root cause: with every one of the chain's 9 joints simultaneously violated by ~1 unit at spawn (each capsule's own local anchor point sits `ROPE_PHYSICS_SEGMENT_HALF_LENGTH` from its near-coincident neighbor's, even though the body *centers* are bunched together), Godot's iterative Gauss-Seidel joint solver propagates the correction across the whole 9-joint chain within a single physics tick, and on very light (`ROPE_SEGMENT_MASS` = 0.03) bodies that compounds link-to-link instead of damping.
    - **Fix that worked, verified by re-running the same probe**: a hard XZ speed clamp on every dynamic segment, `MAX_SEGMENT_SPEED` = 45.0 (`rope_segment_body.gd`, enforced every physics step inside the existing `_integrate_forces()` plane-lock override — the same Godot-documented, solver-safe entry point already used for the Y-lock, not a new mechanism). This brought `chain_max_reach` down to a bounded ~7–9 units max (never far past the chain's own 8-unit capacity) that converges to closely track the real hand-to-dart distance within roughly 10–20 ticks, reproduced stably across many dozens of throws with 3 simultaneous bots continuously throwing. **Two alternative levers were tried first and explicitly rejected, kept as notes in the code so they aren't retried blindly**: (1) `PhysicsServer3D.PIN_JOINT_IMPULSE_CLAMP` was considered but not needed once the velocity clamp alone worked; (2) explicitly setting pin-joint bias *lower* than the default 0.3 (down to 0.04, on the theory that bias directly scales per-step positional correction) was tried and measured, via the same probe, to make the overshoot *worse* (spiked past 12 units) — a weaker bias makes each joint hold its own pair of bodies together less firmly, so the chain resists cascading disturbance from its other 8 joints *less*, letting the effective total length balloon further, not less. Bias/damping are left at `PhysicsServer3D`'s defaults, unchanged from the prior round.
    - **Honest confidence caveat, same as every prior round on this system**: verified numerically only, via the per-tick reach-vs-real-distance probe described above (sampled across dozens of real bot throws, both single and overlapping/simultaneous) — no screenshot/video access this session. The numbers show real, meaningful, non-instant growth (chain reach ramping from near-zero to matching real distance over roughly 10–20 ticks / ~0.17–0.33s, a large fraction of a typical throw's ~0.2–0.4s full-range flight time) with a bounded initial partial-extension jump on tick 1 (typically reaching 60–95% of eventual full length within that first tick, due to the solver's iterative within-one-step constraint propagation across the bunched chain — a real, disclosed limitation of the velocity-clamp approach: it bounds runaway *compounding across ticks*, but can't fully prevent the solver's own single-step correction on the very first tick after spawn). This is a genuine, measured improvement over the literal bug reported (whole rope pre-extended immediately, staying that way) — but whether the *remaining* first-tick partial jump still reads as "mostly instant" vs. "visibly reeling out" on an actual screen is not something this session could confirm. If the user's next recording still shows it looking too fast/instant, the next lever to try is a SMALLER `ROPE_BUNCH_SPACING`-relative change: not bias (already tried, made it worse) or the velocity clamp ceiling (lowering it further was tested at 16.0 in an intermediate step and abandoned before full measurement when the bias experiment took priority — worth re-trying on its own, in isolation, with the probe) — see this session's own final report for the full sequence of what was tried.
  - **ROUND 2 UNSPOOL FIX — "growing leash" position clamp (per direct written user feedback: "The rope is not uncoiling the way it should" — the round-1 fix above was insufficient on its own).** Both previously-flagged-but-untried levers were actually tried this round, in isolation, with the same per-tick probe, and neither meaningfully fixed the remaining problem: (1) a much smaller `ROPE_BUNCH_SPACING` wasn't the bottleneck (the initial layout's own spacing barely matters — the dominant contributor is each capsule's own half-length anchor offset, ~0.5 units, which doesn't shrink with tighter bunching); (2) `MAX_SEGMENT_SPEED` tested at 8.0 (far below the shipped 45.0), in isolation at default bias, barely changed the chain's overall unspool timing at all — it still reached ~70–90% of full length within the same ~4–6 ticks. **Root cause of why a per-body speed clamp alone can never fully fix this**: it only bounds how fast any ONE body moves, but multiple bodies along the chain can each move at a modest, individually-legal speed *simultaneously*, and their combined effect still unfolds a large fraction of the chain's total slack within a handful of ticks — the real bottleneck is the chain's *aggregate* growth rate, which no per-body speed limit can bound on its own. Also investigated and ruled out as the dominant cause (per the coordinator's explicit request to check the render path, not just assume it's fine): the tube mesh's Catmull-Rom interpolation (`_update_rope_tube_mesh()`/`_build_tube_mesh()`) *does* measurably overshoot past the raw physics control points' own extent — verified via a temporary probe comparing the raw control points' max reach from the hand against the sampled curve's max reach — but the overshoot was small and secondary (typically 0.01–0.3 units, under ~4% of total rope length), not the dominant driver; the physics chain's own single-tick jump (still ~7 units within a few ticks even with the round-1 clamp) was the real problem.
    - **The actual fix**: `rope_segment_body.gd`'s `_integrate_forces()` now enforces a live, per-tick **position** cap (`max_reach_from_hand`, `hand_pos_2d`) on top of the existing Y-lock and speed clamp — any segment the solver tries to push further than `max_reach_from_hand` from the hand gets pulled back radially (preserving its current angle, so it doesn't fight the solver's own XZ shaping/obstacle-draping), with the outward-radial component of its velocity zeroed too so it doesn't immediately re-violate the cap and jitter against it. Critically, `max_reach_from_hand` is **not a fixed constant or a separate timer/ramp** — `player.gd`'s `_update_physics_rope_anchors()` recomputes it every physics tick directly from the REAL, already-known hand-to-dart distance plus a small fixed slack allowance (`ROPE_UNSPOOL_SLACK` = 1.0), capped at the chain's own total capacity (`DART_ROPE_LENGTH`). This ties the rope's visible/simulated extent directly and deterministically to how far the dart has *actually* traveled (itself already smooth and non-explosive, being a kinematic read of the dart's own position) instead of hoping the joint solver's emergent equilibrium happens to grow at a similar rate.
    - **Verified via the same per-tick probe, re-run after this fix**: `raw_control_max` (the physics chain's real max reach from the hand) now grows smoothly and monotonically tick-by-tick — e.g. one real throw's trace: 0.44 → 0.45 → 0.67 → 0.67 → 2.11 → 2.11 → 2.20 → 2.20 → 2.52 → 2.52 → 2.68 → 2.68 → 2.75 → 2.90 → 3.25 → 3.61 over the first 20 ticks — instead of the round-1 fix's own jump from ~0.6 to ~7+ within 4–6 ticks. Tube-mesh render overshoot stayed consistently small (same 0.0–0.13 range as before), confirming the fix targets the actual root cause. `MAX_SEGMENT_SPEED` (still 45.0) is kept as a secondary safety net against multi-tick divergence, not the primary unspool-pacing mechanism anymore — that role now belongs entirely to the growing-leash clamp.
    - **A real, disclosed side effect of task 1's separate "anchor instead of recall at max range" change (see rope_dart.gd's own entry above), discovered during this round's soak-testing, not a bug in this fix**: once a bot's dart reaches max range without hitting anything, it now stays anchored in open air (per that explicit request) rather than auto-recalling. `bot_controller.gd`'s bots only reclaim a dart by accidentally walking over it while chasing their target (existing, unchanged AI limitation — "Bots don't actively recall... keeping their AI simple") — there is no bot logic that deliberately walks back to retrieve its own anchored dart. Combined, a bot that misses at range can now go without a weapon for a long time (potentially the rest of a round), which was observed directly during this round's testing (throw frequency dropped from many-per-10-seconds to almost none after the first couple of throws in a soak run). This is a real, likely user-visible gameplay consequence of the requested design reversal — flagged here for awareness, not fixed in this round since it wasn't part of either explicit ask; would need bot AI changes (e.g. an explicit "return to own dart" behavior) if the user wants it addressed.
    - **Honest confidence caveat**: same limitation as every round on this system — verified numerically via direct probes (chain reach vs. real distance, sampled every tick across dozens of throws; tube-mesh render overshoot measured directly, not inferred), not visually (no screenshot/video access this session). The numbers now show tight, deterministic tracking between the rope's visible extent and the dart's real travel distance at every single tick, which is a materially different and more precise result than round 1's own numbers (bounded-but-still-large tick-1 jump) — genuine confidence this addresses the specific mechanism the user's feedback pointed at. Still unconfirmed: whether `ROPE_UNSPOOL_SLACK` (1.0 unit of allowed slack) reads as the right amount of visual looseness versus too taut/too loose on an actual screen — that's a purely aesthetic tuning constant with no "correct" value derivable from physics alone, and is the first place to adjust if the user's next feedback is about the rope's slack/tautness specifically rather than its growth timing.
  - **ROUND 3 TENSION FIX (per direct user feedback: "The tension of the rope should be 2 times stronger") — a "tube" perpendicular-distance clamp, NOT joint bias/damping.** Deliberately did NOT touch `PhysicsServer3D.PIN_JOINT_BIAS`/`PIN_JOINT_DAMPING` despite that being the more textbook meaning of "tension" (joint stiffness) — those were already proven, in an earlier round, to make the whole chain numerically explode when stiffened (bias 0.9/damping 2.0 → per-joint gaps growing to trillions of units within seconds), and this round found no new reason that risk wouldn't recur at a different multiplier.
    - **First hypothesis tried and DISPROVEN by direct measurement, not assumed correct**: lowering `ROPE_UNSPOOL_SLACK` (round 2's growing-leash radius, from 1.0 down to 0.4), on the theory that "less slack = tauter." A new probe (max perpendicular deviation of every physics control point from the straight hand-to-tip line) showed this did NOT reliably help — in several real throws the deviation got WORSE at 0.4 than at 1.0 (e.g. one throw: deviation grew from ~1.5 to ~2.7 for a similar real distance). Root cause: `max_reach_from_hand` bounds distance from the HAND POINT in any direction (a sphere), not distance from the hand-to-tip LINE — and the chain's total physical length is fixed at 8 units (`DART_ROPE_LENGTH`) regardless of the real span, so for most of a throw there's always several units of "must go somewhere" excess capsule length; shrinking the sphere's radius doesn't reduce that excess, it just forces the same excess to fold into a smaller sphere, which can bulge MORE per unit of available room, not less. `ROPE_UNSPOOL_SLACK` was reverted to its original 1.0 — its role is purely round 2's unspool pacing, not tautness.
    - **The actual fix**: a second, independent clamp — `rope_segment_body.gd`'s `max_perp_from_line`/`tip_pos_2d` (new fields, alongside the existing `max_reach_from_hand`/`hand_pos_2d`) — caps each segment's perpendicular distance from the straight hand-to-tip line itself (a "tube" around the taut line), not distance from a point. `player.gd`'s new `ROPE_TAUT_PERP_RADIUS` (0.3) constant is pushed live every physics tick from `_update_physics_rope_anchors()`, same pattern as the existing radius-from-hand clamp: any segment beyond that perpendicular tolerance is pulled back toward the line (preserving its along-line position), with the perpendicular component of its velocity zeroed to prevent jitter. This is a plain position clamp on an already-computed transform — the same class of mechanism as the existing Y-plane lock and radius-from-hand clamp, not a change to the solver's own joint stiffness, so it doesn't carry the joint-bias explosion risk.
    - **Verified via the same probe, re-run after this fix**: max perpendicular deviation, which previously ranged ~0.5–2.7 units across real throws (frequently exceeding the actual hand-to-dart distance itself), now converges to roughly 0.3–0.4 (matching `ROPE_TAUT_PERP_RADIUS`'s own value almost exactly) within a few ticks of each throw and stays there — a dramatic, directly-measured reduction, not an assumed one. One residual, disclosed transient: a brief 1-tick spike (observed once, to 1.8, in one throw at tick 4–5, matching the timing of the round-2 growing-leash's own already-accepted brief transient) that decays to the ~0.3–0.4 steady band within 1–2 ticks — consistent with the existing precedent of "brief solver-driven spikes that decay quickly are acceptable, sustained/growing ones are not." No errors or divergence observed across an extended soak (dozens of throws, ~25s of continuous bot play).
    - **Honest confidence caveat**: numerically verified only (perpendicular deviation from the true taut line, directly measured before/after) — no screenshot/video access this session. The mechanism is now a much more literal match for "tension" (directly bounding how far the rope can bow off the straight line) than round 2's radius-from-hand approach, which this round's own measurement showed does NOT reliably control straightness at all despite intuition suggesting it would — a concrete lesson for any future tautness-related feedback on this system: reach for a distance-from-the-LINE clamp, not distance-from-a-POINT, and re-measure rather than assume. `ROPE_TAUT_PERP_RADIUS` (0.3) is, like `ROPE_UNSPOOL_SLACK`, a pure aesthetic tuning constant with no physics-derivable "correct" value — if the next feedback is "still too loose" or "now too stiff/straight," this is the first constant to adjust, and per this round's own lesson, should be re-verified with the same perpendicular-deviation probe rather than assumed.
  - **ROUND 4 (session-internal, immediately superseded) — a render-only "detect obstruction, draw a synthetic straight line through a computed contact point" fix was tried and shipped as commit `71f1e4d`, then EXPLICITLY REJECTED by the user in the very next round and fully reverted (see commit that reverts `71f1e4d`) before this file's own entry for it ever made it past one round.** The user's own words: *"I do not want the rope to be a computed visual. It doesn't work with clipping into pillars."* Do not reintroduce an obstructed/unobstructed branch in `_update_rope_tube_mesh()`, or any other scheme that renders a synthetic straight line/polyline in place of the raw physics chain's own positions — the render must always trace the real `RigidBody3D` segment positions unconditionally. Any future tautness/straightness work must happen at the PHYSICS level (real constraints/clamps acting on the actual simulated bodies), not the render level. This class of fix is now off the table for this codebase; the actual accepted fix for the underlying clipping bug is ROUND 5 below.
  - **ROUND 5 OBSTACLE-CLIPPING FIX (per direct user bug report with a screenshot: "when the rope cut into the pillar (which it should not) it loses the tension again" — i.e. the physics chain itself, not just its rendering, was visibly interpenetrating obstacle geometry whenever a dart anchored on the far side of a pillar from its owner).** Root-caused and fixed entirely inside the real physics simulation (`rope_segment_body.gd`'s `_integrate_forces()`), per the user's explicit redirect away from any render-level workaround (see ROUND 4 above) — collision layer/mask wiring (`ROPE_OBSTACLE_LAYER_BIT`, `continuous_cd = true`) was already correct and not the cause.
    - **Root cause, confirmed by direct measurement (segment position vs. a pillar's own `get_rect_2d()`, sampled every tick through real draped throws), not assumed**: `max_reach_from_hand` (the growing-leash sphere-from-hand clamp) and `max_perp_from_line` (the round-3 tension tube-from-the-straight-line clamp) are BOTH computed purely from the hand/tip's own live 2D positions, with zero awareness of obstacle geometry. Whenever the straight hand-to-tip line itself passes through a pillar's interior — exactly the "dart anchored behind a pillar" case — `max_perp_from_line`'s 0.3-unit tube sits centered ON a line that runs through solid geometry, so every tick it forcibly recenters any segment the REAL collision solver had just correctly pushed outward (draping it around the pillar's edge) back in toward that line, i.e. back through the pillar — fighting the solver's own contact response every single step. This produces both halves of the reported bug at once: visible interpenetration (the clamp literally relocates the segment inside the obstacle's footprint) AND lost tautness (the segment never settles into the solver's real resting position; it's yanked back and forth between "pushed out by contact" and "pulled back by the clamp").
    - **Layer 1 (necessary but, measured, NOT sufficient alone)**: both segment bodies now have `contact_monitor = true` / `max_contacts_reported = 4` (`player.gd`'s `_make_rope_segment_body()`); `_integrate_forces()` reads `state.get_contact_count() > 0` and skips BOTH clamps for that tick when true — since these bodies' `collision_mask` only ever matches the dedicated obstacle layer bit (never another segment/player/ground), any reported contact is unambiguously "resting against real obstacle geometry," so yielding to the solver here is safe and matches the user's own suggested direction ("clamps may need to yield to collision response"). **Measured to be insufficient in isolation, and in one harsh scenario actually WORSE than no gating at all**: a deterministic corner-wrap stress test (hand and tip on opposite far corners of a pillar, forcing real wraparound rather than a flat face-press) showed sustained penetration up to 0.70 with Layer 1 alone, versus 0.59 with no gating whatsoever — because `get_contact_count()` reflects contacts detected as of the END of the previous physics step, so it can read false for a single tick even while a segment is still resting at the surface (narrow-phase jitter, or the joint solver nudging it a hair off-surface while resolving other constraints), and on exactly that tick the clamp reactivates and yanks the segment back onto the (obstructed) line, re-injecting it into the pillar — turning contact flicker into a *trigger* for repeated yank-through-the-box cycles instead of a steady fight.
    - **Layer 2 (the fix that actually closes the gap)**: a new `_clamp_target_inside_obstacle(p: Vector2) -> bool` helper directly tests each clamp's own candidate destination point against every real obstacle's `get_rect_2d()` (grown by a small `CLAMP_OBSTACLE_MARGIN` = 0.2 safety buffer — see below) before committing to it; if the candidate would land inside solid geometry, that clamp is skipped for this tick regardless of what `get_contact_count()` said. This is explicitly NOT a synthetic path/route computation — nothing here decides where the rope *should* go or draws any line; it is a plain "is this specific point I'm about to move a physics body to solid ground" safety check on the clamp's own pre-existing math, directly against the same ground-truth rect every other obstacle-aware system in this codebase already uses (`rope_dart.gd`'s own obstacle stop, `arena_obstacle.gd`), not inferred from a one-tick-stale proxy signal like Layer 1's contact flag.
    - **`CLAMP_OBSTACLE_MARGIN` (0.2) tuning, iterated and re-measured at each step, not guessed once**: at margin 0.1, the same corner-wrap stress test still showed occasional brief single-tick penetration up to ~0.12 (a candidate point tested exactly on/just outside a rect's true edge, passed, got committed, then the joint solver's next correction — pulling a neighboring segment, which drags this one slightly via the shared pin joint — nudged it the last fraction of a unit across the boundary before the next tick's check could catch it). At margin 0.2, the same test passed with exactly 0.0000 penetration across a full 240-tick run in most repeated runs, with an occasional very small residual (~0.02–0.04) in others — see confidence caveat below. Margin is roughly 3x `ROPE_RADIUS` (0.035): big enough to close the observed gap, small enough that it doesn't visibly push the rope away from a surface it should be resting flush against.
    - **Verified via direct measurement, both synthetic and organic**: (1) a new permanent regression test, `tests/test_rope_obstacle_clip.gd`/`.tscn` (same pattern as `tests/test_player_map_boundary.gd`), deterministically forces a hand-west/tip-east-of-a-pillar's-opposite-corner condition (bypassing the need to land a real throw at exactly the wrong angle) and measures every dynamic segment's real position against `PillarA`'s own rect every tick for 240 ticks (~4s) — before the fix: sustained, repeated penetration up to 0.59–0.70 depending on gating config; after both layers at margin 0.2: 0.0000 in most runs, an occasional small (~0.02–0.04) residual in others, an order of magnitude smaller than the original bug and non-visually-significant at the rope's own rendered radius. (2) Organic soak testing — 3 simultaneous hard bots, ~30+ seconds of continuous real throw/anchor/drape cycles against both `PillarA` and `PillarB`, with obstacle contact genuinely occurring dozens of times (`contact_segs` regularly 1–4) — showed **exactly 0.000 penetration on every single sample**, never once reproducing the original bug's 0.2–0.5+ unit clipping that the same probe reliably caught before the fix on the identical organic setup. This is the more representative real-gameplay signal; the tiny residual only shows up in the synthetic test's deliberately harsher-than-typical corner-to-corner geometry.
    - **What was investigated and correctly ruled out, not skipped**: collision layer/mask wiring (already correct, confirmed by the fact that `contact_count() > 0` reliably fires when expected); `PIN_JOINT_BIAS`/`PIN_JOINT_DAMPING` stiffening (per this file's own standing caution from an earlier round, not retried); raising solver iteration counts or segment mass/count (not needed once the actual mechanism — clamps fighting collision response — was identified and fixed directly).
    - **Honest confidence caveat**: numerically verified via direct penetration-vs-rect measurement, both a deterministic worst-case synthetic test and extensive organic bot-gameplay soaking — not visually (no screenshot/video access this session). The organic-gameplay signal is unambiguous (zero penetration across dozens of real contacts, where the same probe reliably caught the bug before the fix); the synthetic stress test's small residual (~0.02–0.04 units, occasional, not every run) is the one honestly disclosed remaining gap — it's for a harder-than-typical geometry (hand and tip on exact opposite far corners of the same pillar) and is roughly an order of magnitude smaller than the original reported bug, but if the user's next report is still about clipping specifically in a similarly extreme corner-to-corner throw, re-run `tests/test_rope_obstacle_clip.gd` first (adjust its hand/tip placement to match the reported case) before reaching for a larger `CLAMP_OBSTACLE_MARGIN` — a much larger margin risks the rope visibly floating off a surface it should rest against, a new, different visual complaint.
  - **ROUND 6 CORNER-WRAP-AT-MAX-LEASH FOLD FIX (root-caused from a real user screen recording, frame-extracted and reviewed directly, not just the user's own description): "The rope wrapped around the pillar. But when it supposed to reach the max length and stop the player, it clipped into the pillar instead."** The wrap itself (ROUND 5, above) was confirmed still clean; the NEW bug was specifically that right as the player reached max leash range and kept pushing further/around a pillar corner, the rope visibly developed a sharp zigzag/hook fold at the corner contact point, straightening back out once the player stopped pushing or the geometry resolved.
    - **Root cause, confirmed by direct measurement (a new deterministic regression test, `tests/test_rope_leash_corner_wrap.gd`/`.tscn`, driving a player in a sustained tangential+outward sweep around a real pillar corner while comparing against a static-hold settle baseline), not assumed from the symptom alone**: TWO separate places computed "how far the dart/hand really is" as a plain straight-line (beeline) distance, both blind to any obstacle between the two points — (1) `player.gd`'s `_clamp_to_rope_leash()`, which used to clamp the player onto a plain circle of radius `DART_ROPE_LENGTH` around the ANCHOR; (2) `_update_physics_rope_anchors()`'s growing-leash `real_dist` (feeding `rope_segment_body.gd`'s `max_reach_from_hand` sphere-from-the-hand budget, see ROUND 2 above), which used `hand_pos.distance_to(tip_pos)`. A straight line between two points on opposite sides of a real obstacle corner is always SHORTER than the true physical path the rope has to travel to wrap around that corner — so both of these beeline reads systematically UNDER-counted how much of the chain's fixed capacity was actually already spent on the wrap. That meant: (1) the leash clamp let the player keep walking outward well past the point where the real, already-wrapped chain had used up its entire length, and (2) the growing-leash budget fed the segment clamps a radius too small for the wrap's own real extent, forcibly yanking the non-contact tail segments (the ones legitimately far from the hand, between the corner and the anchor) back toward the hand every tick — directly fighting the pin joint simultaneously dragging the tip anchor to the real, fixed dart position. That tug-of-war between "pushed out by real corner contact" and "pulled back by an under-budgeted clamp" is what read as the sharp zigzag/hook fold, and it only became visible once the player pushed far enough around the corner for the beeline/true-path gap to matter, matching the reported "specifically at max leash while still pushing around the corner" timing exactly.
    - **The fix reads real physics state, not a computed corner/route (per this codebase's own standing constraint from ROUND 4 above, which was about rendering, but the same principle — let the real simulated chain be the source of truth, don't hand-compute geometry — was applied here too)**: two new helpers, `_rope_chain_current_path_length_2d()` and `_rope_chain_rest_length_2d()`, sum the REAL, already-simulated chain's own consecutive control-point distances (hand → every dynamic segment, in joint order → tip) instead of a beeline — a segment genuinely resting against real obstacle contact is exactly where the physics solver actually put it, wrap included, so this is reading real state, not guessing at where a corner is. `_update_physics_rope_anchors()`'s `real_dist` now uses `_rope_chain_current_path_length_2d()` instead of the old beeline call. `_clamp_to_rope_leash()` now pivots its clamp on the chain's own first dynamic segment (the link nearest the hand, always topologically adjacent to it regardless of how many corners are involved) with the radius shrunk by `_rope_chain_rest_length_2d()` — the real remaining chain length from that segment to the tip — instead of pivoting on the anchor directly; a plain beeline-to-anchor circle is kept as an always-applied fallback/backstop (covers the tick(s) before the chain exists, and by the triangle inequality is always at least as permissive as the wrap-aware clamp, so it never fights it). In the unobstructed common case both changes are numerically almost identical to the old beeline behavior (the chain settles close to the straight line, bounded by `ROPE_TAUT_PERP_RADIUS`), so no behavior change is expected when there's no obstacle in the way.
    - **Verified via direct before/after measurement with the new regression test, isolating the sweep-phase dynamics from the already-documented, already-accepted "first tick(s) after a throw" unspool transient (ROUND 2's own disclosed 60–95%-within-one-tick jump) by settling the chain with the player stationary for 90 ticks BEFORE starting the adversarial corner sweep** — conflating the two would have falsely attributed that pre-existing, already-accepted transient to this bug. Before the fix: max chain-length overshoot past its own `DART_ROPE_LENGTH` capacity 2.29 units, max real pillar-rect penetration 0.398 units (segments genuinely inside solid geometry), max single-tick segment jump ("fold") 3.36 units across 5 distinct fold events. After the fix, same test, same adversarial sweep: overshoot dropped to 0.68–1.02 units (roughly a 55–70% reduction, run-to-run variance observed — see caveat), penetration dropped to 0.000–0.128 units (zero in some runs, an order of magnitude down from 0.398 in the worst observed run), fold jump dropped to 1.18–1.67 units across only 1–3 events (down from 5) — a substantial, direction-correct, repeatedly-reproduced improvement on every metric, not a one-off. No regression confirmed by re-running the existing ROUND 5 regression test (`tests/test_rope_obstacle_clip.gd`, the static-hold corner-wrap scenario) unchanged: still 0.0000 penetration over its own 240-tick run. A 3-bot hard-difficulty soak (~35s, real throw/anchor/wrap/recall cycling, `GameManager.lobby_mode` toggled `false` at runtime only inside a temporary, not-committed test harness — never the file's own default, matching this project's established convention) produced zero script errors.
    - **Honest confidence caveat, disclosed rather than glossed over**: this is a genuine, substantial, numerically-verified improvement in the exact mechanism the user's recording pointed at, but NOT a complete elimination under this specific test's deliberately extreme, sustained adversarial pressure (a relentless several-second outward+tangential push directly into a corner, more aggressive than realistic play, chosen specifically to stress-test the reported scenario rather than to model typical gameplay). The residual appears to be internal segment-to-segment (joint) stretch under continuous real contact load, not something a player-POSITION clamp can bound on its own — the pin joints are deliberately kept at `PhysicsServer3D`'s soft, un-stiffened defaults (see the ROUND 1 "stiffer bias/damping explodes the chain" finding above, re-confirmed as still the standing constraint, not retried this round), so some residual give under sustained stress is an accepted trade-off for overall stability, not a new bug introduced here. If the user's next recording still shows a fold (even a smaller one) specifically during a long, sustained corner-hug at max range, the next lever to investigate is a direct segment-to-segment (neighbor) distance clamp in `rope_segment_body.gd` (a position clamp on each segment's distance to its own immediate neighbor, same class of mechanism as the existing Y-lock/`max_reach_from_hand`/`max_perp_from_line` clamps) — not attempted this round due to its own un-verified joint-fighting risk, and should be measured with the same `tests/test_rope_leash_corner_wrap.gd` probe before being trusted, not shipped on intuition alone.
  - **ROUND 8 (2026-07-23) CORNER-CUTTING TUBE-OVERSHOOT FIX — a render-side spline fix, not a physics-chain regression (per direct user screen-recording review, 4fps/~7.7s/~31 frames, frame-by-frame): "It works most of the time. This is one case I found that the rope is cutting through the pillar."** The described trigger was new and distinct from every prior round's own bug: not straight-line hand-to-tip penetration (ROUND 5, already fixed) and not specifically a max-leash corner sweep (ROUND 6, already improved) — the recording showed a *smooth curved dip* through the pillar's solid base specifically while the character circled *closely and tightly* around the pillar's near corner (not pushing outward at max leash the way ROUND 6's trigger did), clean again a couple of frames later once past the corner.
    - **Reproduced deterministically first, per this session's standing discipline, via a new regression test, `tests/test_rope_corner_tube_overshoot.gd`/`.tscn`**: forces a dart `ANCHORED` on the far side of a real pillar (same established shortcut as ROUND 5/6/7's own tests), then drives the player in a TIGHT (~0.75-unit radius), PURELY TANGENTIAL sweep around the pillar's near corner — deliberately different from `tests/test_rope_leash_corner_wrap.gd`'s own outward+tangential max-leash stress, to isolate "ordinary close circling" as the only variable, independent of leash tension. Measures, every few ticks, BOTH the real `RigidBody3D` segment positions AND the exact Catmull-Rom curve `_update_rope_tube_mesh()` samples through them, each against the pillar's own `get_rect_2d()` — the two-metric split is what pinpoints whether a regression is in the physics chain itself or only in what gets drawn from it.
    - **Root cause, confirmed by direct measurement, not assumed from the symptom alone**: across repeated runs, `max_pen_raw` (real segment positions vs. the pillar rect) stayed exactly `0.0000` the whole sweep — the underlying physics chain never entered the pillar; ROUND 5's guarantee holds — while the SAMPLED CURVE alone reached `0.09`–`0.15` units of penetration despite being drawn through those same never-penetrating control points. This is a textbook property of Catmull-Rom (and any interpolating spline): wherever the real polyline bends convexly around an obstacle's corner, a smooth curve fit through it overshoots toward the CONCAVE side of that bend to stay smooth — which, for a rope wrapping the OUTSIDE of a convex pillar corner, is exactly the pillar's own interior. A pure render-side artifact, confirmed distinct from a physics regression by the very same before/after split this test was built to make.
    - **The fix is explicitly the "legitimate, allowed" category from this session's own brief, not the previously-rejected ROUND 4 approach**: nothing here detects an obstruction and draws a synthetic straight line/contact point in its place (the thing the user explicitly rejected once already — see ROUND 4's standing constraint above, still in force). Instead, `_update_rope_tube_mesh()`'s sampling loop was extracted into its own function, `_compute_rope_tube_curve_points()` (also lets the regression test call the EXACT real code path directly on synthetic control points, rather than maintaining a second hand-written copy that could silently drift from what ships), and given two strictly-ordered fallback tiers, each only ever derived from already-real, already-verified data:
      1. If a sampled point would land inside an obstacle's grown rect (`ROPE_TUBE_OBSTACLE_MARGIN` = 0.12, sized close to `ROPE_RADIUS` plus a small buffer — no joint-solver drift to fight here unlike `rope_segment_body.gd`'s larger 0.2 margin, so a smaller one suffices), fall back to a plain LINEAR blend between the SAME two real control points (`p1.lerp(p2, local_t)`) the smooth curve was interpolating between at that sample — this can never introduce a position the real chain didn't already occupy.
      2. Only if EVEN that linear blend still lands inside an obstacle (verified to actually occur, not hypothetical — the coarse 8-segment discrete polyline can itself "cut a corner" between two genuinely-outside points when a convex corner pokes into the gap between them) — push the point to the NEAREST point on the violated obstacle's own grown-rect boundary (compare distance to each of the 4 edges, move along whichever is nearest). Still the smallest possible correction off real, already-known geometry (`get_rect_2d()`, the same ground truth every other obstacle-aware system in this codebase reads), not a fabricated route.
      New helpers `_point_inside_any_obstacle()` (mirrors `rope_segment_body.gd`'s own `_clamp_target_inside_obstacle()`, duplicated rather than shared since that one is a `RigidBody3D` instance method) and `_nearest_point_outside_obstacles()` (tier-2 only) live alongside `_compute_rope_tube_curve_points()` in `player.gd`.
    - **Verified via the same test, re-run repeatedly after the fix, both fallback tiers exercised**: `max_pen_curve_unclamped` (a permanent side-by-side diagnostic baseline reproducing the pre-fix behavior) consistently measured `0.09`–`0.15` across multiple runs — confirming the bug reproduces reliably, not a one-off — while `max_pen_curve_fixed` (calling the real, actual shipped `_compute_rope_tube_curve_points()`) measured exactly `0.0000` on every run that exercised the intended tight-corner sweep. Re-ran `tests/test_rope_obstacle_clip.gd` (ROUND 5) and `tests/test_rope_leash_corner_wrap.gd` (ROUND 6) afterward for regression coverage — both showed occasional penetration/fold failures, but this was confirmed to be PRE-EXISTING, unrelated flakiness, not caused by this round's change: re-ran both against a clean `git stash`-restored baseline (this round's diff removed) and got the SAME class of failures there too (one baseline run of the leash-corner test even fully collapsed the chain to `chain_len=0.00`, worse than any run with the fix applied) — expected, since this round's diff is scoped entirely to `player.gd`'s render-only tube-mesh sampling and never touches `rope_segment_body.gd`'s clamps, `_clamp_to_rope_leash()`, or `_update_physics_rope_anchors()`, none of which could be affected by a change to how the curve is drawn from already-computed positions. A 4-hard-bot ~35s soak (`GameManager.lobby_mode`/`human_count`/`bot_difficulty` toggled at runtime only, reverted before commit — zero net diff confirmed via `git diff`, matching this project's established temp-testing convention) produced zero script errors.
    - **A real, disclosed test-harness bug found and fixed along the way, kept as a comment in the test file so a future editor doesn't reintroduce it**: an early version of this new test called the existing `_clamp_to_rope_leash()` during its own post-reset settle window and intermittently (roughly half of runs) got the player yanked far from the intended tight orbit before the sweep even started, producing a degenerate non-reproduction (all metrics reading a trivial `0.0`). Root cause: `_rope_chain_rest_length_2d()` reads the chain's first dynamic segment's REAL position, which — unlike `player.global_position` — doesn't teleport when this test resets the player directly; acting on that transiently-stale reading via the leash clamp during the catch-up window produced a large, wrong reposition. Fixed by simply not calling `_clamp_to_rope_leash()` during that specific short catch-up window (the clamp is still exercised normally, every tick, once the actual sweep begins) — this is a test-harness fix, not a `player.gd` behavior change, and is unrelated to the corner-cutting bug itself, but is recorded here since it's exactly the kind of "investigate, don't assume" root-causing this project's whole verification discipline is built on.
    - **Honest confidence caveat**: numerically verified — raw segment penetration, sampled-curve penetration (both unclamped-diagnostic and the real fixed path, calling the actual shipped function), and existing-regression-test non-regression (via the clean-baseline git-stash comparison) — not visually (no screenshot/video access this session). The mechanism (Catmull-Rom overshoot at a real, verified-correct polyline's sharp bend) is a well-understood, generic property of interpolating splines, and the fix's two-tier fallback chain is bounded entirely by already-real data at every step, so there's genuine confidence this addresses the specific class of artifact the user's recording showed. Still unconfirmed: whether the fixed tube's visual smoothness (falling back to a straight/piecewise-linear segment right at a tight corner, rather than a curve) reads as an acceptably subtle transition on an actual screen, versus a visible "kink" at the fallback boundary — that's the first thing to check against a fresh recording if the user's next report is about visual smoothness specifically rather than clipping.
  - **ROUND 9 (2026-07-24) STRAY-CORNER-SEGMENT FIX — a second, distinct render-side spline bug found in ROUND 8's own tier-2 fallback (per direct user screen-recording review, zoomed/pixel-cropped: "the rope could still cut through pillar when the rope is stretched long enough").** Reproduced trigger: a dart anchored FAR away (large real hand-to-dart distance, near `DART_ROPE_LENGTH`) while the character stands immediately adjacent to a pillar (well under half a tile of clearance). In this configuration a SECOND, separate, dark line segment rendered straight down the pillar's face — disconnected from, and in addition to, the main curve which correctly arced around the far corner toward the dart. Confirmed via direct code trace, not assumption, that this is NOT ROUND 8's own bug recurring (that one's own tier-2 fallback already existed and was already active) and NOT an idle-coil/thrown-rope render overlap (`_update_persistent_rope()`'s `dart == null` branch unconditionally hides every `_rope_segments` mesh every single `_process()` frame the physics chain is active — traced the call site at line ~1171, unconditional, before any `is_dead`/`is_falling` early return, so there is no code path where both renders could be visible at once).
    - **Reproduced deterministically first, via a new regression test, `tests/test_rope_stray_pillar_segment.gd`/`.tscn`**: places the player stationary immediately against `PillarA`'s west face, force-anchors the dart near max range on the far side (beeline ≈7.76 of `DART_ROPE_LENGTH`=8.0, deliberately grazing the pillar's north edge), lets the chain fully converge (this specific tight-corner configuration was measured to need a much longer settle window than this codebase's other tests' usual ~10-20 ticks — ~150-200 ticks — before the physics chain stops still-shifting near the corner), then calls the EXACT real `_compute_rope_tube_curve_points()` every tick and measures the max distance between CONSECUTIVE output samples — a smooth, continuously-parameterized curve should never have a large jump between adjacent samples (normal spacing is ~0.17-0.2 units over this chain's span/48 samples), so a jump is the direct numerical signature of a disconnected/stray segment.
    - **Root cause, confirmed by logging the exact per-sample tier and position, not assumed from the symptom alone**: ROUND 8's own tier-2 fallback (`_nearest_point_outside_obstacles()`) pushes EACH violating curve SAMPLE independently to whichever single rect edge is nearest to that one sample alone. For a run of several consecutive samples whose raw/linear positions straddle a real box corner, "nearest edge" flips discontinuously right at the rect's own internal 45-degree line equidistant between two adjacent edges — one real reproduced trace showed 3 consecutive samples all pinned to a pillar's west edge (`x` constant at the grown rect's own boundary, `z` varying) — reading exactly as a short vertical mark on the pillar's face — before jumping ~0.65-0.95 units to samples pinned to the north edge instead, with the straight chord between the two edge-lines cutting across the real corner rather than routing around it. This is a fundamentally different failure than ROUND 8's own bug: that one only ever needed to correct a single momentary sample during continuous motion past a corner (tiny ~0.11-unit dip); this one needs a STATIONARY hold where several consecutive REAL control points straddle one corner for many ticks in a row, which is what makes tier 2 fire repeatedly across a whole run instead of a one-off nudge.
    - **Getting the actual fix took FOUR iterations against this same repro test, kept as notes so they aren't retried blindly** (each looked complete until re-measured and found wanting):
      1. One shared waypoint per real control-point SPAN instead of per sample (`_nearest_point_outside_obstacles()` on the span's midpoint). Broke again whenever a REAL physics segment briefly sat inside the tube's own small `ROPE_TUBE_OBSTACLE_MARGIN` (0.12, narrower than `rope_segment_body.gd`'s own 0.2 margin — harmless on its own, NOT a physics-level penetration regression) — the two spans on either side of that point computed two different waypoints, moving the discontinuity to the shared boundary instead of removing it.
      2. Grouping by CONTIGUOUS RUNS of bad SAMPLES (bracketed by the nearest known-good samples either side, not by which control-point span contains each sample) — this part stuck. But `bad[i]` was still gated on the raw Catmull-Rom sample, whose own wiggle can dip in and back OUT of the rect within what should be one continuous crossing, fragmenting it into several tiny runs that each independently pick a waypoint, reproducing the same jump through yet another path. Fixed by gating `bad[i]` on the LINEAR blend between the sample's real bracketing control points instead — monotonic between two fixed points as `local_t` increases, so it can't flicker; only genuinely stubborn samples (blend still inside) ever join a run.
      3. Picking the run's waypoint via `_nearest_point_outside_obstacles()` on the run's own midpoint (nearest single edge from one arbitrary point, same mechanism as the old broken per-sample tier 2) — still let the leg from that edge-point to the far anchor re-cut the corner.
      4. Brute-force-validating all 4 of the rect's corners as candidates (requiring a densely-sampled straight leg from each anchor to test clear of the rect end-to-end) — still failed, SUSTAINED across up to 8 consecutive ticks in one run: the validation could reject all 4 real corners in some configurations (an anchor very close to the rect has a leg to an adjacent corner that grazes near-parallel to a face for a long stretch, and floating-point/margin noise in the discrete leg check can flip it to "blocked"), silently falling through to the same broken fallback for the whole run.
    - **THE ACTUAL FIX** (`player.gd`'s `_corner_route_waypoints()`): stop validating candidate corners after the fact and derive the correct waypoint(s) directly from which EDGE each of the run's own two anchors independently resolves nearest to (`_rect_nearest_edge_index()` — the exact same distance comparison `_nearest_point_outside_obstacles()` already makes, factored into an index). If both anchors agree, no real corner-straddling exists — the plain single-edge push still applies. If they resolve to two ADJACENT edges (one X-edge, one Z-edge), route through the ONE corner vertex those edges actually share (`_rect_shared_corner()`) — always well-defined, nothing to validate. If they resolve to two OPPOSITE edges (west+east, or south+north — confirmed via direct logging to occur in real anchor pairs, meaning the true detour has to wrap one ENTIRE SIDE of the box, not one vertex), route through BOTH of that side's corners, picking north/south (or west/east) by checking which side a real sample from deep in the run actually sits closer to. The resulting 2-4-point polyline (`anchor_before` → 0-2 waypoints → `anchor_after`) is walked by the generalized `_route_through_polyline()` (replaces the earlier fixed two-leg-only `_route_through_waypoint()`).
    - **Verified via the same test, re-run repeatedly after the fix, using a SUSTAINED-violation pass criterion refined twice during this round**: (a) per-tick sampling showed even the corrected code can have a rare, ISOLATED single-tick numerical blip (up to ~0.62, immediately surrounded by normal ~0.35-0.5 values, consistent with momentary `rope_segment_body.gd` contact-state flicker — an already-accepted transient class elsewhere in this codebase) — a fundamentally different, imperceptible category from the original bug's own signature (a ~0.65-0.95 unit jump recurring on EVERY sampled tick for the entire multi-second hold); the shipped test only fails on 2+ CONSECUTIVE violating ticks. (b) Separately, a large gap between two consecutive curve SAMPLES is not by itself a defect — `_build_tube_mesh()` always connects consecutive points with one continuous surface regardless of gap size, so a genuinely long but VALID stretch (e.g. wrapping an entire side of the box via two corners) can legitimately get few samples under the fixed 48-samples-per-whole-curve budget, producing a big but perfectly continuous gap; the test only flags a gap as suspicious if its own real MIDPOINT also lands inside the obstacle (i.e. the straight chord between the two samples itself cuts through solid ground — the actual signature of a real disconnection). With both refinements, repeated runs after the fix consistently PASS (typical steady-state max jump ~0.25-0.4, occasional isolated non-sustained blips near the 0.6 threshold). Also manually re-verified (during development, not a second permanent test file) against `PillarB` from a different (north-face) approach angle with a different anchor position, multiple repeated runs — confirms the fix isn't overfit to the one geometry it was root-caused from.
    - **Re-ran the full existing regression suite for non-regression, via direct clean-baseline `git stash` comparison per this project's own standing discipline, not assumption**: `tests/test_rope_corner_tube_overshoot.gd`'s own render-fix-specific metric (`max_pen_curve_fixed`, calling the real shipped `_compute_rope_tube_curve_points()`) measured `0.0000` on a full run — non-regression confirmed directly, not inferred; a separate `max_pen_raw` flag it also reported (a genuine RAW-physics-chain penetration, unrelated to anything this round touched) and an earlier "throw produced no dart" failure both reproduced identically with and without this round's diff on the same base commit, confirming both are pre-existing and unrelated. `tests/test_rope_obstacle_clip.gd`, `tests/test_rope_leash_corner_wrap.gd`, and `tests/test_rope_recall_wrap_path.gd`'s Scenario A all showed PASS/FAIL flipping between consecutive runs of the IDENTICAL code (including this round's own diff, and in one case confirmed failing on the clean baseline too) — direct, conclusive proof of pre-existing chaotic/flaky sensitivity in that class of test (all three measure raw `RigidBody3D` physics state driven by `_physics_process()`; this round's diff is scoped entirely to the render-only `_compute_rope_tube_curve_points()`/`_update_rope_tube_mesh()` path called from `_process()`, which cannot deterministically influence any of their measured physics trajectories). `tests/test_rope_recall_wrap_path.gd` Scenario B passed cleanly every run.
    - **Also investigated and explicitly ruled out, per this round's own "investigate, don't assume" mandate**: an idle-coil/thrown-rope render overlap (`_update_persistent_rope()`'s `dart == null` branch unconditionally hides every `_rope_segments` mesh every single `_process()` frame the physics chain is active — traced the call site, unconditional, before any `is_dead`/`is_falling` early return, so no code path can show both renders at once) and a stale/leftover physics chain from an incomplete teardown (ROUND 8-era investigation already covered this same class of hypothesis for a different symptom and found `_free_physics_rope()` reachable from every `dart = null` path; nothing in this round's own trace suggested revisiting that finding).
    - **Honest confidence caveat**: numerically verified at every iteration — exact per-sample tier/position tracing before and after each fix attempt, sustained-vs-isolated jump classification, a real-midpoint-obstacle-check to distinguish valid long stretches from actual disconnections, and existing-regression-test non-regression via direct same-commit re-run comparison — not visually (no screenshot/video access this session). The edge-index-derived corner/two-corner-wrap mechanism is a materially more correct fix than any of the three rejected intermediate attempts, each verified via the identical repro test rather than assumed to work from the code alone. Still unconfirmed: whether the fixed tube reads as smoothly wrapping the corner (a piecewise-straight 2-4-point detour, not a curve) vs. a visible "kink" at each vertex on an actual screen — the same category of open question ROUND 8 already flagged for its own fallback tiers, now also applying to this one.
  - **ROUND 10 (2026-07-24) "HORIZONTAL CHORD ACROSS THE PILLAR'S FACE" FIX — a genuinely different bug from ROUND 9's, in a materially different geometric configuration (per direct user report from a fresh screen recording, pixel-cropped and reviewed frame-by-frame): a dart anchored above/behind a pillar, character standing to its lower-right — the rope correctly wrapped down the pillar's LEFT edge from the anchor, but instead of continuing around the near corner straight to the character, it drew a distinct, nearly-straight HORIZONTAL line across the pillar's near face (bottom-left area to a point further right along the same edge) before curving down-right to the character.**
    - **Reproduced via a new dedicated harness, `tests/test_rope_pillar_face_chord.gd`/`.tscn`**: force-anchors a dart due WEST of a pillar with the player due EAST, both roughly level with the pillar's own center-z, so the straight hand-to-tip line threads through the box's interior near dead-center — the configuration that makes both bracketing curve points squarely face OPPOSITE edges (`_corner_route_waypoints()`'s two-corner "wrap one whole side" branch). Confirmed the SAME symptom pattern the user described (a long straight run hugging one entire pillar face) is exactly what `_corner_route_waypoints()`'s existing opposite-edges branch produces for this class of configuration — and, via direct manual geometry proof (a single-corner polyline A→corner→B for this exact geometry re-enters the rect's interior on its second leg; only a genuine two-corner detour stays outside for both legs), confirmed that topology choice is CORRECT and necessary here, NOT a misfire of ROUND 9's adjacent-vs-opposite edge classification — this is a materially different geometric case from ROUND 9's own diagonal near-corner repro, which remains correctly handled (re-verified passing, unaffected by this round's change).
    - **First hypothesis tried and found insufficient on its own**: raising `ROPE_TUBE_OBSTACLE_MARGIN` (0.12 → 0.2, matching `rope_segment_body.gd`'s own `CLAMP_OBSTACLE_MARGIN`) on the theory that a genuine full-face wrap — a SUSTAINED straight run hugging an entire pillar face for several consecutive samples — reads as visually touching/cutting the face at the old margin's thin clearance (barely more than 3x `ROPE_RADIUS`, sized for a brief single-sample nudge, not a long sustained run) even though it stayed numerically outside the real rect. This margin bump is real and kept (the rendered tube should never sit closer to a pillar than the real physics-simulated segments already rest, per `CLAMP_OBSTACLE_MARGIN`'s own precedent), but re-running the repro test's own jump-vs-real-rect-midpoint probe AFTER this change surfaced a second, more severe, PRE-EXISTING bug that had been hiding just under the old margin's smaller jump sizes: a sustained (up to 42 consecutive violating ticks) large (~1.4-1.7 unit) discontinuous jump whose own midpoint landed inside the real pillar footprint — i.e. the actual, literal chord-cutting-across-the-face bug, now unmasked rather than caused by the margin change.
    - **THE ACTUAL ROOT CAUSE, found by tracing exactly which two points formed the jump**: `_corner_route_waypoints()`'s two-corner OPPOSITE-edges branches always returned `[west_corner, east_corner]` (or `[south_corner, north_corner]`) in that FIXED order, with no regard for which of `anchor_before`/`anchor_after` actually resolved to which side. The caller (`_route_through_polyline()`) walks the route `[anchor_before, waypoints..., anchor_after]` strictly IN ORDER. Whenever `anchor_before` happened to be the one on the "second" side in that fixed order (e.g. `anchor_before` on the EAST edge, `anchor_after` on the WEST edge — exactly what this round's repro geometry produces), the walked route became `anchor_before(east) → west_corner → east_corner → anchor_after(west)`: a self-crossing "bowtie" where the FIRST leg alone cuts diagonally back across the entire box interior (since it starts on the side opposite from where it's headed), and the last leg mirrors the same crossing in reverse. The existing per-point safety fallback (`if _point_inside_any_obstacle(routed): push to nearest edge`) caught the interior points of that crossing leg and pushed them out via a naive single-axis nearest-edge push — producing a jump directly from that pushed point (e.g. `x` pinned to the east margin boundary, `z` still near `anchor_before`'s own value) to the very next sample, now correctly on the middle (west↔east face) leg — exactly the reported "distinct line connecting to a point further right along the same bottom edge."
    - **Fix**: order each returned corner pair so its first entry is always the corner on `anchor_before`'s own actual side (`edge_a == 0`/`edge_a == 2` branch on which corner goes first), eliminating the self-crossing route entirely — the walked path is now a plain, monotonic anchor→corner→corner→anchor detour with no leg doubling back across the box. (One GDScript strict-mode snag hit while implementing this: a ternary expression between two `Array[Vector3]` literals infers as plain untyped `Array`, which then fails to assign even into an already-`Array[Vector3]`-typed local — worked around with an explicit `if/else` block instead of a ternary for the array construction, not by loosening any type annotation.)
    - **Verified via the same repro test, re-run after the ordering fix**: `max_jump_overall=0.0000`, `max_consecutive_violations=0` across the full 180-tick measurement window — the sustained ~1.4-1.7 unit jump is completely gone, not merely reduced. `min_clearance_seen=0.2000`, exactly matching the new `ROPE_TUBE_OBSTACLE_MARGIN` value, confirming the margin fix's own clearance guarantee holds simultaneously. This test was converted from an exploratory scratch scene into a permanent pass/fail regression test (checked into `tests/`) with two independent guards: (1) no sustained jump whose own midpoint lands inside the REAL (ungrown) rect, and (2) every sampled curve point stays ≥`MIN_CLEARANCE` (0.15) from the real rect — a direct regression guard on the margin fix itself, separate from the jump/topology guard.
    - **Re-ran the full existing regression suite for non-regression, via direct clean-baseline `git stash` comparison per this project's own standing discipline**: `tests/test_rope_obstacle_clip.gd` and `tests/test_rope_leash_corner_wrap.gd` both showed PASS/FAIL flipping between consecutive runs of the IDENTICAL unchanged baseline code (confirmed via `git stash`, re-running the same test scene twice with zero code changes in between) — direct, conclusive re-confirmation of the same pre-existing chaotic/flaky physics-trajectory sensitivity ROUND 9 already documented for this exact class of test (both measure raw `RigidBody3D` physics state driven by `_physics_process()`; this round's diff touches only `_corner_route_waypoints()`/`ROPE_TUBE_OBSTACLE_MARGIN`, exercised solely from the render-only `_compute_rope_tube_curve_points()` path called from `_process()`, which cannot deterministically influence either test's own measured physics trajectory). `tests/test_rope_corner_tube_overshoot.gd`, `tests/test_rope_recall_wrap_path.gd` (both scenarios), `tests/test_rope_stray_pillar_segment.gd`, `tests/test_movement_direction_skew.gd`, and `tests/test_dart_void_throw.gd` all PASSED cleanly on this round's own diff.
    - **Honest confidence caveat**: numerically verified — exact jump-endpoint tracing that identified the self-crossing route as the literal cause (not inferred from the symptom alone), zero-jump/zero-violation re-measurement after the fix, and existing-regression-test non-regression via direct same/clean-baseline re-run comparison — not visually (no screenshot/video access this session). The root cause (a fixed-order waypoint pair ignoring which anchor is actually on which side) is a concrete, mechanical bug with a concrete, mechanical fix, not a tuning/heuristic adjustment — high confidence this is the correct fix for the reported symptom. Still unconfirmed: whether the corrected two-corner wrap reads as a clean, natural-looking detour around the pillar's near corner on an actual screen (vs. e.g. a visible sharp kink at each of the two corner vertices, a piecewise-straight detour rather than a curve) — the same open visual question ROUND 8/9 already flagged for their own fallback tiers, now also applying to this fix.
  - **ROUND 11 (2026-07-24) — ARCHITECTURAL REPLACEMENT of ROUNDS 8-10's whole case-by-case heuristic pile with a general visibility-graph + Dijkstra shortest-path algorithm, NOT another point fix.** After ROUND 10 shipped, the coordinator directly frame-reviewed two fresh user screen recordings reporting "the rope clipped into the pillar and went to the other side" and could not identify as clear a clipping instance as previous rounds' own repros showed — combined with all four existing regression tests (`test_rope_obstacle_clip.gd`, `test_rope_corner_tube_overshoot.gd`, `test_rope_stray_pillar_segment.gd`, `test_rope_pillar_face_chord.gd`) still passing cleanly when re-run directly, this meant the user was very likely hitting a **5th distinct relative geometric configuration** (a different angle/approach between hand, pillar corner, and dart anchor) that ROUNDS 8-10's hand-written edge/corner-selection heuristics (`_rect_nearest_edge_index()`/`_rect_shared_corner()`/`_corner_route_waypoints()`) simply didn't cover — not a regression of an already-fixed case. Four distinct configuration-specific bugs in the same render function, each one individually "fixed" and re-verified, is not bad luck — it's the predictable failure mode of enumerating cases by hand instead of solving the actual, well-known computational-geometry problem ("shortest taut path between two points around convex polygonal obstacles") once, generally.
    - **The replacement algorithm**: a real **visibility graph + Dijkstra shortest path**, not a heuristic. Graph nodes = the real hand and tip endpoints, plus every registered obstacle's 4 corners (grown by the existing `ROPE_TUBE_OBSTACLE_MARGIN` = 0.2, and excluding any corner buried inside a *different* obstacle's rect, which can't be routed through). Edges connect any two mutually-visible nodes — a standard Liang-Barsky segment-vs-rect clip (`_segment_crosses_rect_interior()`), tested against **every** obstacle's rect, not just "the one it's currently near" — shrunk by a tiny epsilon (`VISIBILITY_EPS` = 0.001) so a segment that only runs along an obstacle's own boundary (needed for the legitimate "wrap one whole face" case) isn't falsely flagged blocked. `_visibility_graph_route()` runs a plain O(n²) Dijkstra over this (deliberately small — a handful of pillars/trees per arena, well under 100 nodes even with several obstacles) graph and returns the hand→tip shortest path. This is correct **by construction** for any relative angle, any number of corners on one obstacle, or any number of obstacles between hand and tip (Dijkstra naturally chains through multiple obstacles' corners) — it doesn't need to know in advance whether the real answer is "one corner," "wrap a whole side," or "thread between two pillars"; the shortest path through the graph *is* whichever of those is geometrically correct for the actual configuration, every time, closing the whack-a-mole loop by construction rather than by yet another special case.
    - **Detection vs. routing kept cleanly separated, and only routing was replaced**: the existing Catmull-Rom-sample-through-the-physics-chain scan (gating "bad" on a linear blend between real bracketing control points, an early iteration within ROUND 9's own fix) is UNCHANGED — it's what decides *whether* any detour is needed at all, preserving the deliberately-tuned natural sag/slack look for the vast majority of frames with no obstacle nearby (the efficient early-out case). None of ROUNDS 8-10's four bugs were ever in that detection machinery — every one was in what happened *after* detection, deciding which corner(s) to route through, which is exactly (and only) the piece now replaced wholesale.
    - **Whole-curve replacement, not a per-run patch — a real inefficiency found and fixed by THIS round's own property test, not a user report**: a first version of this fix kept ROUNDS 8-10's own structure of only touching the locally-bad *samples*, just swapping the new visibility-graph route in between each bad run's own local bracketing anchors. `tests/test_rope_visibility_route_sweep.gd`'s independent reference-length oracle caught that this measurably (up to ~21% in the most symmetric case: hand and tip collinear straight through an obstacle's own center) over-lengthens the route versus the true hand-to-tip geodesic — not a penetration or a bowtie (both stayed clean), but a real, avoidable inefficiency: patching only the local bad run keeps the rendered rope going straight almost all the way to the obstacle before jogging up-and-over at the last sampled moment, instead of angling smoothly toward the true tangent corner the way an actual taut line would. Fixed by using the bad-sample scan ONLY to decide whether any detour is needed at all — once any sample is bad, the **entire** curve is replaced by one visibility-graph route computed directly between the real hand and tip endpoints, resampled by arc length across all output points, discarding the raw Catmull-Rom samples entirely for that frame. Re-measured after the fix: the length-ratio failures dropped from 4/2856 to 0/2856 in the sweep test below, with the worst remaining ratio at 1.054 (5.4% over true optimal, well within resampling noise).
    - **Straight polyline legs, deliberately NOT re-smoothed with another spline pass**: the detour route is walked as a straight polyline (via the existing, already-generic `_route_through_polyline()`). This is not a compromise — the true shape of a taut line wrapped around the outside of a convex corner is a tangent straight segment on each side of the contact point, not a curve; a smooth spline through the route's own waypoints is exactly what re-introduced ROUND 8's original overshoot-into-the-obstacle bug. Still fed through the same `_build_tube_mesh()` extruder as before, so it still renders as one continuous round tube.
    - **New testing approach — a property-based/swept test, not one more point sample**: `tests/test_rope_visibility_route_sweep.gd`/`.tscn` calls `_compute_rope_tube_curve_points()` directly (the exact real function, not a reimplementation) on synthetic 2-point `[hand, tip]` control-point lists — deliberately bypassing the need for a real thrown dart/physics settle window, since the obstacle-avoidance behavior is pure 2D geometry independent of how hand/tip were arrived at. Three scenarios: (1) a full 15°-step angular × 2-radius grid around a single obstacle for BOTH hand and tip independently (2,304 configurations), (2) 400 randomized hand/tip pairs around the same single obstacle (fixed seed, reproducible), (3) 200 randomized pairs around TWO side-by-side obstacles, exercising multi-obstacle chaining no single-obstacle case can cover. **2,856 total configurations.** For every one, asserts both (a) no sampled curve point comes within `MIN_CLEARANCE` (0.15, matching the established convention from `test_rope_pillar_face_chord.gd`) of any obstacle's real (ungrown) rect, and (b) the curve's total arc length isn't grossly longer than an **independently, separately implemented** reference shortest-path oracle (`_ref_shortest_path_len()` — its own standalone visibility-graph Dijkstra, sharing no code with `player.gd`, over the same grown rects) — this second check is what actually catches a self-crossing bowtie bug like ROUND 10's own historical one: a bowtie is always measurably longer than the true shortest path, since true shortest paths around convex obstacles are never self-intersecting.
    - **Result**: `pen_fail=0/2856`, `len_fail=0/2856` (after the whole-curve-replacement fix above), `worst_clearance=0.2000` (never penetrates, sits exactly at the safety margin), `worst_len_ratio=1.0539` (max 5.4% over true optimal). All four existing regression tests (`test_rope_pillar_face_chord.gd`, `test_rope_stray_pillar_segment.gd`, `test_rope_corner_tube_overshoot.gd`, `test_rope_obstacle_clip.gd`) re-run directly and cross-checked — see below.
    - **Full existing suite re-run, cross-checked against a clean-baseline `git stash` comparison for every failure, per this project's own standing discipline**: `test_rope_pillar_face_chord.gd`, `test_rope_stray_pillar_segment.gd`, and `test_rope_corner_tube_overshoot.gd` all PASS (unchanged from before this round). `test_rope_obstacle_clip.gd` — which measures the RAW physics-chain `RigidBody3D` segments against a pillar's rect, `rope_segment_body.gd`'s own territory, never touched this round — FAILs both on this round's code AND on a clean `git stash` baseline (0.246 vs. 0.086 penetration, same failure mode either way): confirmed pre-existing, unrelated flakiness, not a regression. `test_rope_leash_corner_wrap.gd` and `test_rope_recall_wrap_path.gd` — neither of which calls `_compute_rope_tube_curve_points()` at all (the first is `_clamp_to_rope_leash()`/physics-chain territory, the second is `get_rope_polyline_2d()`, the RAW physics-chain polyline, feeding `rope_dart.gd`'s own recall-path tracer) — were run 3-4 times each on both this round's code and a clean baseline: **both PASSED and FAILED on both versions across repeated runs**, with materially different player trajectories every single run even with identical code and identical inputs (real, non-deterministic Godot physics/Metal-backend timing jitter, not anything either version's script controls) — confirmed pre-existing flakiness, not a regression, consistent with this project's own standing note that prior rounds have found the same thing. `test_movement_direction_skew.gd`, `test_dart_void_throw.gd`, and `test_player_map_boundary.gd` (no rope-obstacle interaction at all) all PASS unchanged.
    - **Deleted this round**: `_find_violated_obstacle_rect()`, `_rect_nearest_edge_index()`, `_rect_shared_corner()`, `_corner_route_waypoints()` — every ROUND 8-10 case-specific heuristic. **Kept**: `_route_through_polyline()` (already-generic polyline interpolation), `_point_inside_any_obstacle()`/`_nearest_point_outside_obstacles()` (now both built from a shared `_get_grown_obstacle_rects()` helper, still used as final per-sample defense-in-depth after the visibility-graph route is walked — should rarely trigger now that the route is provably clear by construction, but kept as a cheap safety net against float/epsilon noise right at a boundary).
    - **Honest confidence caveat**: numerically verified via the new swept/randomized test (2,856 configurations, zero penetrations, zero gross-length-blowups) and the full existing suite (all rope-obstacle-specific tests passing, all flakiness cross-checked against a clean baseline and confirmed pre-existing) — not visually (no screenshot/video access this session). This is architecturally a much stronger guarantee than any previous round's own verification — a general algorithm proven correct across a systematic sweep of the whole configuration space, not one more hand-picked point sample — but whether it actually *reads* as a clean, natural-looking taut wrap on an actual screen (rather than, say, an oddly angled straight-leg kink at a corner that's numerically correct but visually unexpected) remains unconfirmed pending a fresh recording.
- Signals: `player_killed(player)`, `player_eliminated(player)`. Both HUD and GameManager connect to these.
- `reset_for_round(lives, pos)` is the only way to restore a player between rounds.

**`rope_dart.gd`** — `Node3D`, spawned by player on throw
- The rope+dart system lives in one fixed horizontal plane at the owner's hand height: `plane_y` is computed once in `launch()` from `owner_player.get_hand_world_position().y` and held constant for the dart's whole lifetime, regardless of state or the owner's later movement. This is what lets its obstacle detection use *real* Godot physics (see below) while staying compatible with the rest of the game's flat-XZ-plane gameplay math.
- State machine: `FLYING → ANCHORED → RECALLING`. The rope has a fixed length (`ROPE_LENGTH`, 4× a character's height, ~8.0 units) rather than one that scales with charge — charge now scales `travel_speed` only. Thrown, it flies in a straight line until it hits a player, a real obstacle, the arena boundary, **or reaches `ROPE_LENGTH` without hitting anything** — all four cases now anchor it in place (`_anchor()`), stuck until the owner recalls it or walks over it (`pickup_radius`). **Reversed in this round, per explicit user direction**: reaching max range used to auto-trigger `recall()` (a "yank back" snap-taut, never actually anchoring in open air) instead of anchoring — the FLYING branch's `elif head_2d.distance_to(origin_2d) >= ROPE_LENGTH:` case in `_physics_process()` now calls `_anchor()` there too, matching the other three stop conditions exactly. Checked for stale assumptions elsewhere before considering this done: `bot_controller.gd`'s dodge logic only keys off `dart.state == FLYING` (stops dodging once anchored regardless of *why*, so it needed no change), and `player.gd`'s `_clamp_to_rope_leash()` already keys off `ANCHORED` generically (so a max-range-in-open-air anchor now correctly becomes a real tether point too, not just obstacle/player anchors). Pressing throw again while it's out (`dart != null`) still calls `recall()` manually; walking up to it (`pickup_radius`) still retrieves it in any state.
- **ROUND 7 WALK-TO-PICKUP RETRIEVAL FIX (per direct user request: "When the character walks around the pillar to pickup the dagger, the dagger should follow the rope and go back in a loop").** Investigated current behavior first, per this session's standing discipline, rather than assuming: walking within `pickup_radius` of an `ANCHORED` dart used to call `_pick_up()` directly — an instant grab with zero travel, regardless of the rope's own current shape. Separately, the existing manual-recall path (pressing throw again, `dart.recall()` → `RECALLING`) already moved the dart, but in a straight line toward wherever the owner currently stood (`head_2d += (owner_pos - head_2d).normalized() * recall_speed * delta`) — correct when the rope was already straight, but visibly wrong (cutting straight back through the obstacle) whenever the rope was wrapped around a pillar corner, since it never consulted the rope's own real shape at all.
  - **Design decision, unifying rather than narrowly scoping to just the walk-up case**: `ANCHORED`'s pickup-radius check now calls `recall()` instead of `_pick_up()` directly — walking up to an anchored dart is now just another way to trigger the exact same `RECALLING` retrieval a manual throw-again press does, rather than a separate instant-grab code path. This means the wrap-aware fix below benefits BOTH triggers for free, and there's only one retrieval mechanism left to reason about/verify, not two. `player.gd`'s `_is_recalling`/`_recall_anim_phase` Enter→Push→Exit animation sequence (see the throw/recall animation entry above) is kept in sync with this via a new one-line check at the top of `_physics_process()` — `if dart.state == DART_STATE_RECALLING and not _is_recalling: _is_recalling = true; ...` — instead of only being set by the explicit button-press branch, so the reel-in arm animation now plays correctly on a walk-up pickup too, not just a manual recall.
  - **The wrap-aware path-following mechanism**: a new `player.gd` method, `get_rope_polyline_2d()`, exposes the exact same live control points (hand anchor + every dynamic physics-chain segment, in joint order) that `_update_rope_tube_mesh()` and `_rope_chain_current_path_length_2d()` already read every frame — this is "trace real, already-simulated state," the same standing principle from ROUND 6 above, not a fabricated bend point. `rope_dart.gd`'s `RECALLING` branch (`_advance_recalling()`) samples a point along this live polyline instead of a straight line to the owner.
  - **FIRST ATTEMPT, tried and disproven by direct measurement, not shipped on intuition**: re-measuring "how much rope is left" from scratch every tick — `[hand, ...segments, head_2d]` (head_2d appended fresh as the path's own tip each call) — and consuming a fixed `recall_speed * delta` step of that fresh total each tick, terminating once the fresh total dropped below `pickup_radius`. A new deterministic regression test (`tests/test_rope_recall_wrap_path.gd`/`.tscn`: forces a dart `ANCHORED` on the far side of a real pillar from the player, lets the chain settle into a genuine wrap exactly like ROUND 6's own corner-wrap test, then calls `recall()` and tracks `head_2d` against the live polyline every tick) showed this NEVER RELIABLY CONVERGED: `head_2d` wandered in a loose, non-monotonic orbit near the original anchor for a full 300-tick/5s run with no net progress toward the hand, and briefly clipped ~0.15 units into the pillar's own rect. Root cause: appending `head_2d` as the path's own tip makes the "remaining length" measurement self-referential with `player.gd`'s existing TENSION CLAMP (`rope_segment_body.gd`'s `max_perp_from_line`, driven every tick from `_get_rope_tip_target()` — which is this dart's own current position) — as `head_2d` retracts, the clamp's target hand-to-tip LINE moves with it, continuously re-centering nearby segments onto a fresh, differently-angled line. That's a real and expected SHAPE change (the chain legitimately straightens as the tip nears the hand — this is the same tension clamp already validated, in ROUND 3, to keep an `ANCHORED`/stationary-tip rope taut), but it means the fresh-measured "remaining length" is not a reliable monotonic progress signal on a MOVING tip — it can stay flat or even grow tick to tick while the shape churns, since nothing about this system was ever exercised with an actively-retracting tip before this round (ROUND 6's own corner-wrap test only ever forces `ANCHORED`, never calls `recall()`).
  - **The actual fix — decouple "distance traveled" from "current chain shape"**: `rope_dart.gd` gained a new field, `_recall_travel_dist` (reset to `0.0` by `recall()`), a plain accumulator incremented by `recall_speed * delta` every tick regardless of what the chain's shape does — a monotonic, never-reversing progress signal, unlike the self-referential fresh-measurement above. Each tick, `_advance_recalling()` samples the point that is `_recall_travel_dist` real-path-distance from the TIP end of the FRESH live polyline (hand → every segment, deliberately NOT including `head_2d` this time — see `_get_hand_rope_path_2d()`/`_sample_point_from_tip_2d()`). Because `_recall_travel_dist` is bounded to eventually exceed even the longest the live chain could ever measure (bounded by `DART_ROPE_LENGTH`, reached in ~0.33s at `recall_speed` = 24), sampling always lands on the hand end once it does, so this is guaranteed to terminate in bounded time no matter how the chain's shape churns in between — a structural fix, not a tuned-away symptom.
  - **Verified via the same regression test, re-run after the fix**: the corner-wrap scenario now converges in 9–12 ticks (well under the ~20-tick/0.33s bound implied by `DART_ROPE_LENGTH / recall_speed`), `max_pillar_pen` stayed exactly `0.0000` across 3 repeated runs (down from the ~0.15 clip observed with the first, rejected approach), and `player._is_recalling` was confirmed `true` throughout every `RECALLING` tick (the animation-sync fix, verified working). A second scenario in the same test file (an unobstructed short anchor placed directly next to the player) confirms walking within `pickup_radius` now genuinely routes through `RECALLING` at least transiently — not a silent regression back to an instant grab — while still completing in a handful of ticks for the trivial case, matching the design goal of "same mechanism, unified." No regression to the manual-recall unobstructed case is expected or was observed: `_sample_point_from_tip_2d()`'s path is close to a straight line whenever the chain isn't wrapped (bounded by the existing `ROPE_TAUT_PERP_RADIUS` tension clamp from ROUND 3), so this reduces to essentially the old straight-line-to-owner behavior there.
  - **Pre-existing test flakiness discovered and explicitly ruled out as a regression, not swept under the rug**: `tests/test_rope_leash_corner_wrap.gd` and `tests/test_rope_obstacle_clip.gd` (ROUND 5/6's own regression tests) both showed failures when re-run this round. Before concluding anything, both were re-run against a clean `git stash` of this round's own changes (i.e. the exact HEAD commit this round started from) — both STILL failed (or, for `test_rope_obstacle_clip.gd`, passed on one clean-baseline run and failed on another, back-to-back, with zero code differences between the two runs) — confirming these are pre-existing, run-to-run non-deterministic flakiness in this environment (most likely floating-point/physics-timing jitter across separate process launches), not something this round's changes caused or could have caused (neither touched pin-joint bias/damping, `rope_segment_body.gd`'s clamps, or `_clamp_to_rope_leash()` at all — this round's diff is scoped to `rope_dart.gd`'s `ANCHORED`/`RECALLING` branches and a new `player.gd` read-only accessor). Flagged here rather than silently ignored so a future round investigating either test's failures doesn't waste time assuming this round broke them.
  - **Honest confidence caveat**: numerically verified via direct measurement (dart position vs. the live rope polyline, pillar-rect penetration, tick-count-to-convergence, all sampled every tick across multiple repeated runs) and a ~80-second live 3-bot soak (`GameManager.lobby_mode` toggled `false` at runtime only, reverted before commit — zero net diff confirmed via `git diff`, matching this project's established temp-testing convention) with zero script errors — not visually (no screenshot/video access this session). Whether the retrieval's on-screen motion actually *reads* as "following the rope in a loop" (the user's own descriptive phrase) rather than merely "not cutting through the pillar" (what was directly measured) is unconfirmed pending a real recording. `ROPE_UNSPOOL_SLACK`/`ROPE_TAUT_PERP_RADIUS` (ROUND 2/3's own tuning constants) were left untouched — this round's fix operates entirely downstream of whatever shape those already produce, by construction.
- Player hit detection is pure 2D math — no physics collision shapes involved: `_check_hits()` checks distance from `head_2d` to each non-owner player's capsule (`_seg_dist(head_2d, base, _capsule_top(base)) < hit_radius`), then distinguishes a tight `head_hit_radius` at the top of the capsule (instant `kill()`) from anywhere else along it (non-lethal `trip()` "clothesline" stagger). Either kind anchors the dart at the point of contact.
- Obstacle stopping during FLYING uses a **real physics raycast** each frame (`_raycast_obstacle`, at the fixed `plane_y`, over the frame's swept motion) against the map's actual `CollisionShape3D` geometry — the same collision pillars/trees/cacti already use against players — via `PhysicsDirectSpaceState3D.intersect_ray`, with every player body excluded by RID so the rope dart never anchors on a character. **No arena-boundary clamp anymore — see the dated "ROPE OVER THE VOID" round entry below.** `plane_y` (see above) is clamped to `[MIN_PLANE_Y, MAX_PLANE_Y]` = `[0.5, 1.6]`, comfortably inside the `[0.0, 2.0]` Y-range every obstacle's collision box shares — the raw hand-bone sample can otherwise land outside that band mid-animation-swing and make the raycast miss real obstacles entirely. On a real-obstacle hit, `head_2d` is offset by `ANCHOR_EMBED_DEPTH` (-0.1175, i.e. pulled back, not pushed in) from the raycast's surface point — the dagger's own origin sits much closer to its pommel (local Z=+0.315) than its tip (local Z=-0.55), so for the dagger's true midpoint to land on the surface (half embedded, half visible) the origin has to sit slightly outside it, not past it.
- **2026-07-24 — ROPE OVER THE VOID: dart flight/anchor no longer stops at the arena edge (per explicit user direction: "The dart should be able to throw outside of the map. Let it be hang by the rope.").** The FLYING branch's old `arena_half` (14.5) hard clamp — `if absf(head_2d.x) >= arena_half or absf(head_2d.y) >= arena_half: ... _anchor()`, checked BEFORE the obstacle raycast/max-range branch — is removed outright, not replaced with a looser bound. Confirmed via grep before touching anything that `arena_half` was rope_dart.gd's own private `@export`, read nowhere else in the codebase (not shared with camera framing, bot AI, or spawn logic), so this was safe to delete rather than needing to preserve it for some other consumer. A thrown dart's only remaining stop conditions are now exactly the same three everything else already used: a real obstacle hit, a player hit, or `ROPE_LENGTH` (8.0) max range — matching this project's existing precedent (see the "no auto-recall, anchor at max range in open air" entry above) of preferring "anchor per the same rules as everything else" over a special-cased stop.
  - **Deliberately DART-ONLY — player movement's own boundary is a completely separate system and was left untouched.** `player.gd` has its own `ARENA_HALF` (15.0) and `_check_boundary_fall()` ring-out (kills/respawns a player who walks past the platform edge) — a different constant, a different mechanism (fall-and-die vs. a plain position clamp), verified via `tests/test_player_map_boundary.gd`, re-run unchanged after this fix with identical results to before (all 12 directions/offsets still trigger within the same tolerance). The platform's own `Ground` `StaticBody3D` (main.tscn) is a 30×30 box (half-extent 15, matching player's `ARENA_HALF` almost exactly) but its collision only spans world Y `[-0.2, 0.0]` — well below the dart's `plane_y` band of `[0.5, 1.6]` — so the ground was never geometry the dart's raycast could hit anyway, on or off the platform; there's simply nothing out over the void for `_raycast_obstacle()` to hit, so an off-platform anchor floats in open space exactly as intended ("hang by the rope").
  - **Checked for knock-on effects in the two other systems that read a dart's anchor position, per the task's own explicit ask — neither needed a change.** `player.gd`'s `_clamp_to_rope_leash()` (the owner's own movement tether once `dart.state == ANCHORED`) already worked purely in terms of straight-line/wrap-aware distance from the anchor point regardless of where in the world that point is — it has no notion of "inside the platform" to begin with. `bot_controller.gd` never paths toward a dart's anchor position at all (bots only path toward the enemy player; a dart is only ever retrieved by accidentally walking over it while chasing, an existing, unrelated AI limitation already documented above) — so there's no risk of a bot "getting stuck" pathing toward an off-map anchor.
  - **Verified via a new regression test, `tests/test_dart_void_throw.gd`/`.tscn`** (mirrors `test_player_map_boundary.gd`'s own standalone-instantiate pattern: real `main.tscn` obstacles instanced as a plain child, players/darts instantiated and driven directly, `GameManager.lobby_mode` left at its normal default so `_init_game()` no-ops). Three throws aimed straight out over open void from positions already near the OLD 14.5 boundary all now sail past it and anchor at exactly `ROPE_LENGTH` (8.0) from their throw origin instead of snapping to the old wall — e.g. thrown east from `x=12`: old code would have anchored at `x=14.5`, new code anchors at `x=20.0` (measured `dist_from_origin=8.000` in all three directions/one diagonal). A fourth case re-confirms the obstacle-hit stop condition is untouched: thrown due north into PillarA's face, the dart still anchors on the pillar (`dist_from_origin=1.882`, nowhere near `ROPE_LENGTH`) rather than flying through it. The same test also asserts the owner's `global_position` stays finite (no NaN/explosion) for several ticks after an anchor lands out over the void with the real physics rope chain active and pointed at it (`player.dart` set directly so `_update_persistent_rope()`/`_spawn_physics_rope()` actually run). All four cases passed with zero engine errors.
  - **Honest confidence caveat**: verified numerically via the dedicated test above (dart's final anchor position/distance, obstacle-hit regression, player-position finiteness) and via re-running the pre-existing `tests/test_player_map_boundary.gd` unchanged (still all-pass, confirming zero regression to the player's own boundary) — not visually, and not via a live bot soak test this round. A live-gameplay soak (bots/human actually throwing during a real match) was planned but skipped after the shared Godot editor session appeared to be concurrently in use by another agent's own `run_project` calls on this same machine (debug output intermittently showed an unrelated, already-committed rope probe test — `tests/test_rope_stray_pillar_segment.gd` — actively running when this session's own `run_project` calls omitted an explicit `scene` argument or targeted `scenes/main.tscn`; calls with a scene argument pointing at this round's own dedicated test scene consistently returned this round's own output correctly, so the dedicated-test results above are trustworthy, but a broader live-bot soak was not attempted to avoid contending with/disrupting that other session). Whether an off-map anchored dart's rope actually *renders* correctly out over the void (tube mesh, physics-chain drape) on an actual screen is unconfirmed pending a fresh screenshot/recording — nothing in this change touches the rope-rendering path itself (`player.gd`'s `_update_persistent_rope()`/tube mesh, untouched), so no new rendering risk is expected, but this specific combination (anchor point with no ground/obstacle underneath at all) hadn't been exercised on screen before this round.
- `_anchor()` freezes `_anchor_dir_2d` (a copy of `dir_2d` at that instant) — `_render()` uses it for the blade's orientation for the rest of `ANCHORED`, instead of continuously re-deriving it from the owner's current position (which made an anchored/embedded dart visibly swivel as the owner walked around it).
- `launch()` calls `_render()` once immediately at the end, synchronously — without it, `head_mesh` sits at its scene-default transform (world origin, the map's center) for the one render frame between `add_child()` and this dart's first `_physics_process()` tick.
- The rope itself is no longer this script's — see `player.gd`'s `_update_persistent_rope()` and its physics-chain doc comment above. This script's own `_raycast_obstacle()` (below) is still what stops the *dart head's* flight; the rope trailing behind it is now a fully separate, really-simulated `RigidBody3D` chain that reacts to obstacles on its own via Godot's physics solver, not anything this script computes.
- A player can only have one rope dart out (in flight, anchored, or recalling) at a time — `player.gd` only lets `_throw()` fire while its own `dart == null`.
- Adds itself to the `"darts"` group in `launch()` so `bot_controller.gd` can find incoming darts to dodge.
- Animations (see `player.gd`'s `ANIM_SOURCES`/`combat_moves.glb`/`spell_cast.glb`, retargeted from Quaternius's Universal Animation Library via Blender): `Sword_Attack` on a lethal melee slash (rope dart in hand), `Punch_Jab` on a non-lethal melee kick (rope dart thrown) — both one-shot, tracked in `ONE_SHOT_ACTION_CLIPS`, unchanged by the throw/recall rework below. Throw and recall are each their own 3-phase **Enter → Hold → Exit** sequence (`ThrowAnimPhase` enum; `_throw_anim_phase`/`_recall_anim_phase` + matching timers), replacing the old single one-shot `Spell_Simple_Shoot` clip and the old bare-loop `Push` clip respectively:
  - **Throw**: `Spell_Simple_Enter` (Enter, plays once) → `Spell_Simple_Idle` (Hold, held for as long as the just-thrown dart is still actually `FLYING` — `DART_STATE_FLYING` mirrors `rope_dart.gd`'s `State.FLYING` ordinal by hand, same convention as `DART_STATE_ANCHORED`) → `Spell_Simple_Exit` (Exit, plays once, then the phase returns to `NONE`). Triggered in `_throw()`.
  - **Recall**: `Spell_Simple_Enter` (Enter) → `Push` (Hold, held for as long as `_is_recalling` stays true — unchanged clip/meaning from before this feature, still reads as "reeling in") → `Spell_Simple_Exit` (Exit). Triggered alongside `dart.recall()`/`_is_recalling = true` in `_physics_process()`'s throw-again-to-recall branch.
  - **Phase advancement is time-driven** (`_advance_throw_anim()`/`_advance_recall_anim()`, called unconditionally every `_process()` frame), not driven by `AnimationPlayer.is_playing()` — each phase's real length is read once via `_anim_clip_length()` (`Animation.length` on the merged library, post the glTF importer's `_Loop`-suffix-stripping/auto-loop convention, e.g. `Spell_Simple_Idle_Loop` source clip → `Spell_Simple_Idle` imported name, same as `Push_Loop` → `Push`). This decoupling exists specifically so movement can interrupt the DISPLAYED clip without derailing the sequence's own progress tracking (see next bullet) — a single shared `AnimationPlayer` can only ever show one clip at a time, so if display and progress were the same mechanism, cutting to `Walking_A` mid-sequence would corrupt the phase's own advancement.
  - **Movement always wins, per explicit user requirement**: in `_process()`'s locomotion selection chain, `_is_dashing`/`is_moving` (Running_A/Walking_A) now sit ABOVE both `_recall_anim_phase`/`_throw_anim_phase` — if the player starts moving mid-sequence, the displayed clip cuts to the movement clip immediately; the phase timers keep advancing unseen in the background and resume displaying correctly once movement stops. **Documented trade-off**: because progress is time-driven rather than clip-position-driven, a phase interrupted by movement always resumes by restarting its own current clip from frame 0 (not mid-playback) once movement stops — chosen deliberately for correctness/simplicity over frame-perfect resumption. This is a reversal from the old behavior, where `_is_recalling`'s bare `Push` sat ABOVE movement in the same chain (recall used to keep looping uninterrupted while walking around freely). `_is_charging`/melee's `action_playing` guard keep their old relative position above movement — out of scope for this change and neither can actually co-occur with movement in practice anyway (charging zeroes velocity; melee wasn't reported as an issue).
  - Both phase/timer pairs are reset to `NONE`/`0.0` in `reset_for_round()` (new-round reset) and in `kill()` (a kill mid-throw/mid-recall force-frees `dart` directly, bypassing `_on_dart_returned()` — the only other place `_is_recalling` is cleared — so without this the sequence could get stuck displaying `Push`/`Spell_Simple_Idle` forever post-respawn with no dart to justify it).
  - `spell_cast.glb` (`Spell_Simple_Enter`/`Spell_Simple_Idle_Loop`/`Spell_Simple_Exit`) was retargeted in its own separate Blender pass, run **headless** (`blender --background --python <script>`, not the interactive Blender MCP bridge, which crashed on this project's `UAL1_Standard.glb` import — a `bpy.context.object` `AttributeError` deep in Blender's own glTF importer, reproducible outside any `temp_override`) — same world-space Copy Rotation constraint + `bpy.ops.nla.bake` technique as `combat_moves.glb`, with a freshly-reconstructed 16-bone map (hips/spine/chest/head + both arms + both legs; no bone map from the original `combat_moves.glb` retarget was ever saved to disk) since only 3 additional clips were needed. Exported as its own small glb rather than appended into the already-working `combat_moves.glb`, so the clips already retargeted there are never touched by this work.
  - **ROUND 12 (2026-07-24) — FULL ARCHITECTURE RESET of the rope, per direct, explicit user rejection of every prior round's approach**, verbatim: *"Why are we computing the shortest path???? It doesn't work like that. I want the rope to be a physic object just like the character, tree, or pillar. Make sure it doesn't clip into other stuff. The rope is 32 segments of bar. When held, all segments collapse into the character's hand. When the dart is thrown, the rope unfold segment by segment and when retrieving, the rope fold segment by segment as well."* This supersedes every prior round's rope-rendering entry above it in this file (ROUNDS 1–11) — none of those rounds are wrong about what they fixed at the time, but the whole *category* of fix (compute where the rope should be from obstacle geometry, then draw/force that) is what got rejected here, not any one bug within it.
    - **DELETED WHOLESALE, not kept as a fallback**: the visibility-graph + Dijkstra shortest-path router shipped in commit `ff77a61` (`_visibility_graph_route()`, `_segment_crosses_rect_interior()`, `_segment_visible()`) and everything it had itself replaced from earlier rounds (`_point_inside_any_obstacle()`, `_nearest_point_outside_obstacles()`, `_rect_nearest_edge_index()`/`_rect_shared_corner()`/`_corner_route_waypoints()`, `_route_through_polyline()`, `_get_grown_obstacle_rects()`, `ROPE_TUBE_OBSTACLE_MARGIN`) — all in `player.gd`. Also deleted `rope_segment_body.gd`'s two synthetic position clamps: `max_reach_from_hand` (the ROUND 2 "growing-leash" sphere-around-the-hand pacing clamp) and `max_perp_from_line` (the ROUND 3 "tension" tube-around-the-taut-line clamp) — both were exactly the "compute where the rope should be, then force it there" pattern the reset rejects, even though neither one read obstacle geometry directly. `_compute_rope_tube_curve_points()` is now a bare Catmull-Rom sampler through the real physics chain's own control points, nothing else.
    - **New architecture**: the physics rope chain (`player.gd`'s `ROPE_PHYSICS_*` consts / `_spawn_physics_rope()`) is now **ONE PERSISTENT object per player**, built once from `_setup_dagger_in_hand()` (called from `_ready()`) and only ever torn down in a new `_exit_tree()` override (freeing the raw `PhysicsServer3D` joint RIDs, which are not node-owned and would otherwise leak across round/scene transitions now that the chain isn't rebuilt per-throw). It's no longer spawned in `_throw()` and freed in `_on_dart_returned()`/`kill()`/`reset_for_round()` — those three still set `dart = null` exactly as before, and the chain's tip anchor (`_get_rope_tip_target()`) simply falls back to the hand's own position whenever `dart == null`, which is what makes "collapse into the hand" the chain's literal, physically real resting configuration (both ends pinned to the same point) rather than a separately-authored idle visual. The old `_render_rope_coiled()`/16-segment kinematic idle-coil system and its `RopeCoilAttachment` bone attachment are gone entirely — there is exactly one rope look now, at every state.
    - **Segment count 8 → 32** (`ROPE_PHYSICS_SEGMENTS`), `ROPE_SEGMENT_MASS` scaled down by the same 8/32 ratio so total chain mass stays roughly constant.
    - **Initial-layout lesson, found and fixed via direct measurement, not assumed**: the old 8-segment "bunch near the hand" spawn layout laid segments out along `span_dir` at a fixed **per-segment** gap (`ROPE_BUNCH_SPACING`), so its own total footprint scaled with segment count — at 32 segments that put the chain across ~1.9 units in a dead-straight line at spawn, which is not "collapsed into the hand" by any reading, and left the joint solver needing real seconds to reel that layout back in even while idle. A tight SPIRAL layout (segment centers on a small fixed-radius coil) was tried first and measured, via this round's own regression test, to be *worse* — curling the centers into a coil while every segment's orientation stayed uniformly aligned along `span_dir` (not tangent to the spiral) left each capsule's real end points badly misaligned with what the joint chain needed, a large initial violation that released outward like an over-wound spring (measured: max reach from the hand grew from ~1.2 to ~1.4 after settling, worse than the straight line). The actual fix: keep the straight-line layout (a capsule's own end points DO line up correctly along a straight line at uniform orientation, minimizing initial joint violation) but make `ROPE_BUNCH_SPACING` the bunch's **total** span, not a per-segment gap — spacing is now `ROPE_BUNCH_SPACING / (N-1)`, so the footprint stays small (0.4 units total) and segment-count-independent.
    - **Idle-collapse equilibrium is real, measured, and genuinely converged — but looser than "a tight point," and that's physically correct, not a bug**: a dedicated long-duration probe (run separately during this round's own development, not committed) sampled a fresh-spawned, never-thrown chain's own max-reach-from-hand and per-segment speed every second for 20 real seconds — it settles (avg segment speed decaying to ~0.001–0.002, genuinely at rest, not still drifting) to a rock-stable ~1.35 units by ~4–5s and holds there through the full 20s window. Root cause this is >0: with gravity disabled (per the non-negotiable "disregard gravity and live on a plane" requirement), no tension source, and no self-collision between segments (`collision_mask` only ever matches real obstacle geometry — see `rope_segment_body.gd`), a slack rope with both ends pinned to the same point has no physical force compacting it to a literal point; any folded shape that satisfies every joint is an equally valid equilibrium. A separate probe measured that a chain returning from a real full-charge, near-max-range throw+recall settles to an even looser but still perfectly stable ~2.4–3.3 units (run-to-run variance observed, always stable, never diverging) — the specific knot/fold topology left behind by a real throw-then-retrieve cycle depends on its own path history, not just its current endpoints, the same reason two different real ropes reeled in by hand don't always end up in an identical coil.
    - **Solver iterations 8 → 32** (`project.godot`'s new `[physics]` `3d/solver/solver_iterations`), per this round's own explicit invitation to consider this as a legitimate "more real physics" stability lever rather than any position clamp. Measured to matter directly: at 16 iterations, one adversarial near-corner settled-configuration (hand and tip both close together, both close to a pillar's own corner) left a small but real, non-transient settled penetration (~0.08–0.15 units) into the pillar's rect; raised to 32 and re-measured with the same probe (sampled every ~0.67s for 6 real seconds post-settle), penetration converged to exactly 0.0 by ~1.3s and stayed there. Confirmed via a broader 8-configuration settled sweep (open air, adjacent-corner wrap, diagonal/opposite-corner wrap, opposite-edges whole-side wrap, 4 pseudo-random configs at a fixed seed) that every configuration settles with **zero** real segment penetration into `PillarA`'s own `get_rect_2d()` at 32 iterations.
    - **New primary regression test**: `tests/test_rope_physics_chain_settle.gd` replaces `tests/test_rope_visibility_route_sweep.gd` (deleted — it specifically exercised the now-deleted visibility-graph function on synthetic control points, with nothing real left to measure once that function is gone). The new test measures the REAL, persistent chain's own live/settled positions directly across four scenarios (idle collapse, the 8-configuration settled sweep above, throw-unfold, retrieve-fold) — since the render is now nothing but an uncorrected Catmull-Rom curve through those exact points, this measurement genuinely *is* the real physics behavior, not a proxy for a separate routing function's output. `tests/test_rope_pillar_face_chord.gd` and `tests/test_rope_stray_pillar_segment.gd` were also deleted (both tested the deleted corner-routing/margin-correction machinery directly, with hard pass/fail assertions that no longer have anything to hold against); their legitimate corner-wrap configurations are covered by the new sweep test instead. `tests/test_rope_corner_tube_overshoot.gd` was kept and its assertions rewritten: raw-segment-vs-pillar penetration (measured 0.0000 this round) is now the only PASS/FAIL gate; render-curve overshoot (measured 0.0075, essentially imperceptible — much smaller than the pre-reset baseline even with zero correction applied, since 4x the segment density brackets any given corner far more tightly) is reported informationally only, per this round's own "the render traces real physics, full stop" philosophy.
    - **Cross-checked against a `git stash` baseline (the pre-reset, 8-segment/visibility-graph code) for every pre-existing rope test, per this project's own established practice**: `tests/test_rope_obstacle_clip.gd`, `tests/test_rope_leash_corner_wrap.gd`, and `tests/test_rope_recall_wrap_path.gd` all still show a real (if small) FAIL against their own strict, pre-reset-era thresholds under the new architecture — but **every one of them ALSO fails on the unmodified pre-reset baseline, and by a strictly larger margin**: `test_rope_obstacle_clip` — new max penetration 0.040 vs. baseline 0.078 (a single decaying transient spike in both, not a sustained defect); `test_rope_leash_corner_wrap` (a deliberately adversarial "push past max leash while wrapped" stress scenario) — new max penetration 0.021 (constant, not growing) with **zero** fold-jump events vs. baseline 0.307 penetration and **7** fold-jump events up to 2.86 units (a real, sharp zigzag/hook fold the old growing-leash/tension-clamp system was visibly still producing); `test_rope_recall_wrap_path` Scenario A — new max penetration 0.015 / max path deviation 0.41 vs. baseline 0.087 / 1.29 (baseline also printed its own `WARNING: chain doesn't read as meaningfully wrapped`, i.e. the old system couldn't even reliably reproduce the scenario this test is meant to exercise). None of these three tests' own hard-coded tolerances were touched this round — they were calibrated against the now-deleted growing-leash/tension-clamp mechanisms, which no longer exist, so their current FAIL status is a known, disclosed, honestly-reported limitation carried over from before this reset, not a new regression; the *numbers* say the reset is a strict, multi-times improvement on every one of them, not a step backward. `tests/test_dart_void_throw.gd`, `tests/test_movement_direction_skew.gd`, and `tests/test_player_map_boundary.gd` (none rope-chain-specific) all still pass unmodified.
    - **Honest confidence caveat**: verified numerically via direct measurement throughout — real segment-vs-obstacle-rect penetration (0.0 across the whole settled-configuration sweep and the corner-tube-overshoot test), real idle/throw/retrieve convergence (sampled every 60 ticks across 20+ real seconds, confirmed genuinely at rest via decaying segment speed, not just a snapshot), and a real ~4-hard-bot soak run of the actual `scenes/main.tscn` game loop (temporarily flipping `game_manager.gd`'s `lobby_mode`/`human_count`/`bot_difficulty`, reverted after — verified zero net diff before considering this done) with no errors or warnings over an extended real-time run. **Not verified visually** — no screenshot/video access this session, so whether the persistent 32-segment tube mesh actually *reads*, on an actual screen, as "collapses into the hand" at its own real ~1.35-unit idle radius (vs. some other, less pleasing loose-coil shape) remains unconfirmed pending a fresh screenshot or recording; if the user's next feedback is that the idle rope still looks too sprawled out, the two real levers available (per this round's own findings) are further shrinking `ROPE_BUNCH_SPACING`'s own total-span value (cosmetic, spawn-time only, does not change the converged equilibrium) or reconsidering whether the collapsed look needs a small amount of genuine self-collision between segments (not yet tried this round) to visually read as a coiled bundle rather than a loose tangle — re-measure with the same long-duration convergence probe methodology before shipping either.
  - **ROUND 13 (2026-07-24) — "The length of each segment must be fixed" investigation: root-caused and reproduced directly, but NOT yet fixed — session ended blocked by Godot MCP connection instability, not by running out of approaches to try.** Direct measurement (a new per-joint gap probe, `tests/test_rope_physics_chain_settle.gd`'s `_joint_gaps()`, computing the real XZ separation between each pair of consecutive bodies' own declared local anchor points — the literal "how much has this joint stretched," not the aggregate chain-reach-from-hand proxy the old test used) confirmed the coordinator's own finding and went further: individual joints separate by up to **~1.7–2.5 units** (vs. each segment's own ~0.25 unit physical length) within the first few ticks of a full-charge throw, and this is a genuine, real, physically-meaningful stretch violation, not a measurement artifact — reproduced consistently across many repeated runs.
    - **Root cause, confirmed by an additional idle-state control measurement**: at settled idle rest (no throw ever fired), `max_joint_gap` is exactly **0.0000** — every joint is perfectly satisfied when nothing is pulling on the chain. This rules out a chronic/baked-in spawn-layout violation (the bunched-spawn `ROPE_BUNCH_SPACING` layout is NOT the root cause) and confirms the violation is a pure **throw-transient** phenomenon: the kinematic tip anchor (immovable by joint forces, teleported every tick straight to the dart's live position, which can move up to 36 units/sec at full charge) yanks away from the still-bunched-at-the-hand chain faster than the soft `PhysicsServer3D` pin joints' iterative solve can propagate the correction down all 32 links within the same tick — the same "crack the whip" phenomenon `MAX_SEGMENT_SPEED` was already built to guard against, just not yet fully bounded at the per-joint level.
    - **First approach tried and DELIBERATELY REVERTED, not shipped — a hard per-tick position correction (each segment's own two ends pulled to exactly coincide with its neighbors' attachment points via Jacobi-style midpoint averaging, inside `rope_segment_body.gd`'s `_integrate_forces()`, the same safe entry point as the existing Y-lock).** This is exactly the category of fix the task's own briefing warned against, despite reading superficially different from the OLD deleted `max_reach_from_hand`/`max_perp_from_line` clamps (it reads no obstacle/hand/line geometry, only its own two immediate joint neighbors) — proven by direct measurement, not assumed: it computes a position independent of Godot's own collision solve for that step, so it can (and did) override a segment's already-correctly-obstacle-routed position back onto a straight chord between two neighbor targets, **cutting straight through a pillar that the soft-joint version correctly wrapped around**. Measured directly via the existing settled-configuration sweep: real segment-vs-pillar penetration went from **0.0 (baseline) to 0.71** with this "fix" in place — a severe regression, caught before shipping specifically *because* this project's own convention is "always re-run the full suite, not just the one metric you were fixing." Fully reverted; zero trace remains in `player.gd`/`rope_segment_body.gd`.
    - **Second approach tried, per the task's explicitly-sanctioned "stiffer `PIN_JOINT_BIAS`/`PIN_JOINT_DAMPING`, re-measured incrementally at the current 32-segment/32-iteration config" avenue** — new `player.gd` consts `ROPE_JOINT_BIAS`/`ROPE_JOINT_DAMPING`, applied via `PhysicsServer3D.pin_joint_set_param()` in `_join_rope_pin()` (previously never called at all — bias/damping were left at engine defaults 0.3/1.0 by omission, not by an explicit call). Bias 0.6 / damping 1.5 (a moderate ~2x step from default) was measured to **not meaningfully help** — peak `max_seg_reach` 11.68 vs. baseline 11.17, i.e. within noise, arguably slightly worse. A damping-only trial (damping 4.0, bias unchanged) was started but its result was never retrieved before the session's Godot MCP bridge connection dropped (see below) — **genuinely unknown, not failed**, worth re-trying first in any follow-up round.
    - **Third lever investigated: is `MAX_SEGMENT_SPEED` (45.0) itself the bottleneck?** Reasoning: even a perfectly stiff joint can only close a multi-unit positional gap as fast as the dynamic body is physically allowed to move, and 45 units/sec is only modestly above the dart's own max travel speed (36 units/sec) with little headroom for a lagging segment to "catch up." Tested by doubling it to 90.0 (bias/damping reverted to defaults to isolate the effect) — this **reproduced the exact "crack the whip" resonance explosion** the original 45.0 value was chosen to prevent: `max_seg_reach` ballooned to **22.5 units** (~2.8x the 8.0 unit true capacity) before slowly decaying, with per-joint gaps up to 4.5+ units. This directly confirms, via fresh measurement at the CURRENT 32-segment/32-iteration configuration (not an assumption carried over from the old 8-segment finding), that `MAX_SEGMENT_SPEED` is a load-bearing stability constant, not a stale leftover — raising it is not a viable lever. Reverted to 45.0.
    - **Working hypothesis for a follow-up round, NOT yet tried**: neither a moderate bias/damping increase nor a higher speed cap closes the gap, and a from-scratch position correction breaks obstacle collision — suggesting the real fix likely needs to stay strictly inside Godot's own iterative constraint solver (so it remains automatically collision-aware, same as the existing pin joints) while being a fundamentally *different* constraint formulation, not just a stiffness knob on the current one. The task's own suggested alternative — a `Generic6DOFJoint3D`-style joint with a near-zero (not exactly zero) linear limit along the segment's own local axis, submitted via `PhysicsServer3D.joint_make_generic_6dof()` / `joint_set_generic_6dof_axis_param()` instead of `joint_make_pin()` — was not attempted this round (ran out of session time to it) and remains the most promising untried avenue: it participates in the SAME per-step iterative solve as contacts (unlike the reverted custom position correction), but may have different/more configurable stiffness behavior than the simple pin joint's single bias/damping pair. A also-untried, lower-risk half-step: try damping values between 4.0 and ~10–15 in isolation (bias left at default 0.3, since bias increases were the ones empirically NOT helping) before jumping to a different joint type.
    - **Session-ending blocker, disclosed plainly per the coordinator's own request**: partway through this round's bias/damping trials, the Godot MCP bridge connection repeatedly dropped (`get_debug_output` timing out after 1800s / returning "Connection closed" / "No active Godot process" on otherwise-normal `run_project` calls) — environment instability, not a code-level hang (no test scene run ever failed to complete or looped indefinitely once a connection was actually live; every *successful* connection produced a clean, bounded test run with a definite `..._TEST_DONE` sentinel). Diagnosed as stray orphaned Godot subprocesses from earlier interrupted runs conflicting with the MCP bridge's process tracking; killing the stray PIDs and retrying restored a stable connection each time. Work was stopped, the working tree brought back to a known-safe, fully-reverted-and-re-verified state (see below), and this status reported rather than continuing to blindly retry a possibly-hung tool call.
    - **What shipped this round vs. what did not**: `tests/test_rope_physics_chain_settle.gd` gained a real, permanent capability improvement — the `_joint_gaps()` per-joint probe, per-tick logging during the throw-unfold section, an idle-state diagnostic printout, and a tightened `JOINT_GAP_TOLERANCE` (0.15, an absolute per-joint tolerance) replacing the old `max_reach_unfold <= DART_ROPE_LENGTH * 1.5` check that had a 12.0-unit blind spot wide enough to silently PASS the coordinator's own originally-reported 10.8-unit overshoot. `player.gd` gained the `ROPE_JOINT_BIAS`/`ROPE_JOINT_DAMPING` consts + the `pin_joint_set_param()` calls, left at values (0.3/1.0) that reproduce PhysicsServer3D's own prior implicit defaults — **verified via a full, complete re-run of the entire test suite (idle collapse, all 8 settled-configuration-sweep obstacle wraps, throw-unfold, retrieve-fold) to reproduce the exact pre-round baseline numbers bit-for-bit** (idle max_dist_from_hand 1.3847, all 8 obstacle configs at 0.00000 penetration, post-retrieve collapse 2.5236) — i.e. this round's shipped code is a behavior-preserving no-op on top of the existing baseline, PLUS the new diagnostic capability, with the actual "fixed segment length" requirement still open. `rope_segment_body.gd` has zero diff from the pre-round baseline (the custom position-correction attempt was fully reverted, confirmed via `git diff`). **The core ask — bounding per-joint separation during a throw — remains unimplemented.**
    - **Honest confidence caveat**: everything reported above is a direct numeric measurement (the same joint-gap probe, re-run after every change, including the final full-suite confirmation run) — not a guess, not inferred from "no errors." What's NOT done: the actual fix. The next round should start from the "working hypothesis" above rather than re-deriving the two ruled-out levers (moderate bias/damping, raising the speed cap) from scratch.
  - **ROUND 14 (2026-07-24) — "The length of each segment must be fixed" investigation, continued: BOTH of ROUND 13's own named untried leads were run to completion this round, both CONCLUSIVELY FAILED, and NO WORKING FIX SHIPPED.** This round's own Godot MCP tool connection was unavailable/unstable from the start (not the same intermittent-drop pattern ROUND 13 hit mid-session — the tools simply weren't usable this round), so all verification instead ran Godot 4.7 **headless via direct `Bash` invocation** (`Godot --headless --path . res://tests/test_rope_physics_chain_settle.tscn`, backgrounded with a shell polling loop watching the test's own `..._TEST_DONE` sentinel, then killed by PID) — same real engine, same real test files, same real measurement discipline as every prior round, just a different invocation path. First re-confirmed the exact ROUND 13 baseline bit-for-bit before touching anything: `max_joint_gap=1.8345` @ joint 28, tick 0, full-charge throw — matches ROUND 13's own reported range (1.7–2.5) exactly.
    - **LEAD 1 (damping tuning, isolated from bias) — RUN TO COMPLETION, FAILED, in the WORSE direction**: `ROPE_JOINT_DAMPING=2.0` (bias left at the existing default 0.3) measured `max_joint_gap=6.6527` — **3.6x worse** than the 1.0-baseline's 1.8345, not an improvement in any way. Pushed further to `damping=4.0` per the task's own suggested range: this caused a full, immediate numerical explosion — every measured value (`max_seg_reach`, `tick_max_joint_gap`) reads `nan` from tick 0 onward, the same catastrophic-divergence signature ROUND 1 already found for `bias=0.9/damping=2.0`. Damping alone, independent of bias, is not a viable lever at any value tried above the shipped default — raising it is monotonically bad (1.0→1.83, 2.0→6.65, 4.0→NaN), not a tuning curve with a sweet spot waiting to be found.
    - **LEAD 2 (`PhysicsServer3D.joint_make_generic_6dof()` instead of `joint_make_pin()`) — RUN TO COMPLETION, FAILED at every stiffness level tried, including catastrophically at the high end.** First corrected the task's own suggested API name against the real engine source (`servers/physics_server_3d.h`, Godot 4.3-stable, structurally unchanged in 4.7): there is no `joint_set_generic_6dof_axis_param` — the real methods are `generic_6dof_joint_set_param()` / `generic_6dof_joint_set_flag()`, and `joint_make_generic_6dof(joint, body_a, local_frame_a: Transform3D, body_b, local_frame_b: Transform3D)` takes a full per-body reference-frame `Transform3D` (origin + basis), not a bare `Vector3` anchor point like `joint_make_pin()`. Implemented as a new `_join_rope_6dof()` (each body's `local_frame` = `Transform3D(Basis.IDENTITY, local_anchor_point)`, so the joint's axes track each capsule's own rotating local frame; a tiny `+-0.02` linear limit range enabled on all 3 axes via `G6DOF_JOINT_FLAG_ENABLE_LINEAR_LIMIT`; angular limit flags left off, so rotation stays fully free at every joint, matching a pin joint's own behavior), gated behind a new `ROPE_USE_6DOF_JOINT` bool so it could be A/B'd against the existing `_join_rope_pin()` on the identical throw-unfold probe.
      - **Three stiffness levels measured, all via the same `_joint_gaps()` probe on the identical full-charge-throw scenario**: (1) engine defaults (`softness=0.7, damping=1.0, restitution=0.5`, per the raw server-side struct's own init, confirmed by reading `servers/physics_3d/joints/godot_generic_6dof_joint_3d.h` directly) with only the `+-0.02` limit range set: `max_joint_gap=9.6505` — already **~5x worse** than the pin joint's own 1.8345 baseline, before any deliberate stiffening at all. (2) A moderate bump (`softness=1.0, restitution=1.0, damping=1.0`): `max_joint_gap=8.9473` — still ~5x worse, only a marginal improvement over the raw defaults. (3) An aggressive push (`softness=4.0, restitution=4.0, damping=2.0`): catastrophic, immediate explosion — `max_joint_gap=276121696.0` (276 **million** units) already by tick 0, ballooning to `~1.68e18` by the fold phase, with 8602 real engine "Object went too far away" errors logged, and the dart never even returned within the test's own 240-tick timeout. The generic 6DOF joint is not merely "no better than a pin joint" here — it's measurably worse at every setting tried, including the ones intended to be gentler than the pin joint's own already-rejected stiffening attempts.
      - **Root cause for why this joint type underperforms a pin joint at every setting, confirmed by reading the engine's own C++ source directly, not inferred from the numbers alone**: `servers/physics_3d/joints/godot_generic_6dof_joint_3d.cpp` line 59 is a hardcoded compile-time macro, `#define GENERIC_D6_DISABLE_WARMSTARTING 1` — Godot's generic 6DOF joint has warm-starting (carrying the previous physics step's accumulated impulse forward as a head start for the next step's iterative solve — standard practice for fast constraint convergence, and very plausibly what lets the pin joint converge as well as it does at 32 solver iterations) explicitly disabled in this engine build, with no script-level way to turn it back on — it's baked into the engine binary this project runs against, not a `PhysicsServer3D` parameter. Separately, the joint's own linear-limit correction formula (`solveLinearAxis()`, line 202: `limitSoftness * (restitution * depth / timeStep - damping * rel_vel)`) uses `restitution` as the de-facto positional-error-correction term, since there is no dedicated linear ERP parameter exposed at all in the public API (only the angular axes expose `G6DOF_JOINT_ANGULAR_ERP`) — a structurally different, and per the measured numbers above, less effective correction path than whatever the pin joint's own `PIN_JOINT_BIAS`/`PIN_JOINT_DAMPING` drive internally in the engine.
    - **What shipped this round**: `ROPE_USE_6DOF_JOINT` (and its companion `_join_rope_6dof()`/`ROPE_6DOF_LINEAR_SLACK`) are kept in `player.gd`, **disabled** (`false`), as a permanent "already tried at three stiffness levels, don't retry blindly" record — same standing convention this file already uses for other rejected levers (e.g. ROUND 1's rejected `PIN_JOINT_IMPULSE_CLAMP`/lower-bias notes). `ROPE_JOINT_BIAS`/`ROPE_JOINT_DAMPING` are unchanged from ROUND 13 (`0.3`/`1.0`, still a no-op vs. the engine's own prior implicit defaults) — their own doc comment is updated with this round's damping=2.0/4.0 numbers so a future round doesn't re-attempt "just try a higher damping value" from scratch. **Verified via a full, complete re-run of the entire test suite** (idle collapse, all 8 settled-configuration-sweep obstacle wraps, throw-unfold, retrieve-fold) with both `ROPE_USE_6DOF_JOINT=false` and the reverted damping restored: idle `max_dist_from_hand=1.2751` (vs. ROUND 13's own reported 1.3847 — within this project's already-documented run-to-run physics variance, not a regression), all 8 obstacle configs at `0.00000` penetration, `max_joint_gap=1.8345` (tick 0) / `1.9154` (tick 39, a different peak tick than ROUND 13's own run happened to catch — same magnitude, same known variance), post-retrieve collapse `2.7862` (vs. 2.5236 — same variance band). `git diff` confirms only `scripts/player.gd` changed (doc-comment additions plus the disabled dead-code `_join_rope_6dof()` path); `rope_segment_body.gd`, `tests/test_rope_physics_chain_settle.gd` (including its own `QUICK_PROBE_ONLY` TEMP-TESTING flag, used for fast iteration during this round's own trials and reverted to `false` before this final run), and `scripts/game_manager.gd` (its own `lobby_mode`/`human_count`/`bot_difficulty` TEMP-TESTING toggle, used for this round's soak test below and reverted) all show **zero net diff**. **The core ask — bounding per-joint separation during a throw without breaking the zero-penetration guarantee — remains unimplemented; both of this round's own two candidate mechanisms are now conclusively ruled out, not just untried.**
    - **Verification protocol followed in full, per the task's own explicit checklist**: (1) direct per-joint gap measurement during the throw-instant transient, tick-by-tick, for every configuration tried (baseline, damping 2.0/4.0, 6DOF at 3 stiffness levels) — all reported above. (2) Full `tests/test_rope_physics_chain_settle.gd` suite re-run on the final, reverted code — idle collapse, all 8 obstacle-wrap settled configs, throw-unfold, retrieve-fold — confirmed zero regression on the settled-configuration obstacle-penetration results specifically (`0.00000` on all 8, matching ROUND 13's own guarantee, the thing the ROUND 13 position-clamp approach broke and this round never touched). (3) A 40-second, 4-hard-bot headless soak of the real `scenes/main.tscn` game loop (`game_manager.gd`'s `lobby_mode`/`human_count`/`bot_difficulty` toggled at runtime only, reverted after, zero net diff confirmed via `git diff` before and after) — zero real script/physics errors; the only logged `ERROR` line (`Parameter "material" is null.` at `material_get_instance_shader_parameters`) is a known, reproduced-in-every-run headless-dummy-renderer artifact (present identically in the very first pre-round baseline run too), not a gameplay or physics regression.
    - **Honest confidence caveat**: both leads' numbers are direct measurements (the same `_joint_gaps()` probe, re-run after every change, including the final full-suite and soak confirmation runs) — not guesses, not inferred from "no errors." What's NOT done: the actual fix. This round's own root-cause finding (Godot's generic 6DOF joint has warm-starting hardcoded off, a real engine-level asymmetry versus the pin joint that isn't fixable from script) narrows the remaining search space meaningfully: a future round should look for either (a) a genuinely different constraint formulation not yet tried (e.g. a `SliderJoint3D`-style approach per-segment, or reconsidering whether the "fixed length" requirement can be satisfied by a much larger per-body mass ratio / different solver configuration rather than a different joint type), or (b) revisit whether "the rope is 32 segments of bar" can tolerate a *visual-only* fixed-length correction that (unlike ROUND 13's rejected from-scratch position correction) is specifically designed to never fight obstacle contact — e.g. only correcting non-contacting segments, gated the same way `rope_segment_body.gd`'s `_debug_last_has_contact` already gates other systems — an angle ROUND 13 didn't explore since its own attempt corrected every segment unconditionally. Not attempted this round due to time; flagged here as the most promising remaining direction rather than another blind stiffness-knob sweep.
  - **ROUND 15 (2026-07-25) — RESIZE: rope shortened to 6x character height, 32 -> 24 segments, per explicit user direction: "The rope is currently too long. The rope is supposed to be 6 times the character's height. Let's try 24 joints and the max length for each segment should be 6 times height divided by 24."**
    - **Character height resolved to 1.4, NOT the raw `scenes/player.tscn` `CapsuleShape3D.height` (1.2)** — the task flagged these as disagreeing and asked for a reasoned pick, not an assumption. Checked every other real consumer of "character height" in the codebase before deciding: (a) `game_manager.gd`'s `PLAYER_HALF_HEIGHT` (0.7) is already the value spawn positioning treats as authoritative — added to every spawn marker's Y, and `player.gd`'s own `_mesh_ground_offset` comment independently confirms 0.7 is exactly the offset that puts the character's real rendered feet at floor level (measured, not assumed, in an earlier round); (b) `rope_dart.gd`'s own `capsule_height` export — used for real per-frame gameplay math (the head-hit-vs-body-hit capsule top in `_check_hits()`, i.e. the actual kill-detection boundary) — is **already** `1.4`, independently arrived at, and matches `PLAYER_HALF_HEIGHT * 2.0` exactly. Two independent, already-shipped gameplay systems agree on 1.4; only the raw physics collision shape's own (different) 1.2 disagrees, and nothing else in the codebase reads that shape's height as a general-purpose size figure. Picked 1.4.
    - **New numbers, derived from the constant rather than hardcoded**: `player.gd`'s `DART_ROPE_LENGTH` and `rope_dart.gd`'s `ROPE_LENGTH` (mirrored by hand between the two scripts, same pre-existing convention as `HITBOX_DEBUG_RADIUS`/`DART_STATE_ANCHORED`) are now both `6.0 * (GameManager.PLAYER_HALF_HEIGHT * 2.0)` = **8.4** (up from 8.0 — confirmed this compiles fine as a `const` expression referencing an autoload's own `const`, not just assumed). `ROPE_PHYSICS_SEGMENTS` (`player.gd`) is `24` (down from 32); `ROPE_PHYSICS_SEGMENT_LENGTH` (already derived as `DART_ROPE_LENGTH / ROPE_PHYSICS_SEGMENTS`, confirmed the existing formula needed no change) is now **0.35** (up slightly from 0.25, since the length only shrank 5% while the segment count shrank 25%). `ROPE_SEGMENT_MASS` redistributed the same fixed 0.24 total chain mass (the original 8-segment chain's `0.03 * 8`) across the new count: `0.03 * 8.0 / 24.0` = **0.01** per segment (up from 0.0075 at 32 segments).
    - **Every other reference checked, per the task's own explicit ask, not assumed to be fine**: grepped the whole codebase for `DART_ROPE_LENGTH`/`ROPE_PHYSICS_SEGMENTS`/hardcoded `8.0`/`32`. `_clamp_to_rope_leash()`, `_update_physics_rope_anchors()`, `_rope_chain_rest_length_2d()`, etc. all already read `DART_ROPE_LENGTH`/`ROPE_PHYSICS_SEGMENTS` as symbols, not literals — no changes needed. `bot_controller.gd`'s `THROW_RANGE` (`[4.0, 5.5, 7.0]`) and its dodge-detection radius (a bare `8.0` in `_get_dodge_dir()`) were both checked and left alone: `THROW_RANGE`'s max (7.0) is still comfortably inside the new, slightly LONGER 8.4 capacity (the resize shortened the rope only relative to its old self, 8.0 -> 8.4 is actually longer in absolute terms since 6x1.4 > 4x~2.0's old derivation — see below); the dodge radius was traced via `git log -p` to predate `DART_ROPE_LENGTH`'s own introduction entirely and is an unrelated generic "close enough to bother dodging" heuristic, not a mirror of rope length — left untouched. Two test files had their own hand-mirrored `8.0` literals updated to the same `6.0 * (GameManager.PLAYER_HALF_HEIGHT * 2.0)` derivation: `tests/test_dart_void_throw.gd`'s `ROPE_LENGTH` and `tests/test_rope_leash_corner_wrap.gd`'s `DART_ROPE_LENGTH` (both explicitly comment "must match" the production constant). `tests/test_movement_direction_skew.gd` already reads `player.DART_ROPE_LENGTH` dynamically, no change needed. Two cosmetic print-label strings in `tests/test_rope_corner_tube_overshoot.gd` (non-load-bearing) were also updated for accuracy.
    - **Note on the old 8.0's own derivation, for anyone reconciling numbers**: the OLD `ROPE_LENGTH=8.0` was never actually "4x the 1.2/1.4 character height" — its own comment said "4x" of "a KayKit character reads at ~2.0 units tall on screen," a third, unrelated on-screen-visual-read heuristic. This round's "6x the real 1.4 height" spec supersedes that reasoning entirely, per the task's own explicit instruction to derive from the real height constant, not preserve the old visual heuristic.
    - **Verification protocol run in full, per the task's own checklist, using the same headless-Bash-invocation approach as ROUND 14** (this session's Godot MCP tools were unavailable — no `mcp__godot__*` tools were present in the tool list at all — so every run used `Godot --headless --path . res://tests/<scene>.tscn`, backgrounded, polled for each test's own `..._TEST_DONE` sentinel or a `RESULT`/process-exit fallback for the two tests with no sentinel, then killed by PID). Checked for stray/orphaned Godot processes first, per the task's own explicit ask: one process was found (`--path /Users/zhihao/personal_projects/ropedart-arena --editor`), but it's the user's own separate checkout at a different path from this worktree, in interactive `--editor` mode, not a stray headless test runner — left running untouched since it can't contend with this worktree's own headless invocations.
      - `tests/test_rope_physics_chain_settle.gd` (the primary suite, adapted automatically since it already reads `player.DART_ROPE_LENGTH`/`ROPE_PHYSICS_SEGMENT_HALF_LENGTH` as symbols, no test-file changes needed): idle collapse now settles to `max_dist_from_hand` ~1.11-1.21 (vs. the old 32-segment config's own documented ~1.35-1.38 — slightly TIGHTER at the new sizing, comfortably inside the unchanged `IDLE_COLLAPSE_RADIUS=1.6` tolerance, which needed no adjustment). All 8 settled-configuration-sweep obstacle-wrap tests passed at `0.00000` penetration on 2 of 2 full runs (one earlier partial run saw a single `random_0` config at `0.01093`, immediately reproduced as `0.00000` on re-run — the same run-to-run settled-config flakiness this file already documents for this exact test class, not a resize-caused regression). Post-retrieve collapse `1.02-1.37` (comfortably inside the unchanged `POST_RETRIEVE_COLLAPSE_RADIUS=4.5`).
      - **Rigidity re-check (secondary/informational, per the task's own framing) — re-measured at the new 24-segment/8.4 sizing, NOT assumed to inherit ROUND 13/14's conclusions**: `max_joint_gap` during a full-charge throw's unfold now reads **2.19-3.66** (2 runs) vs. ROUND 13/14's own documented 1.73-2.5 (later re-confirmed at exactly 1.8345) for the OLD 32-segment/8.0 config — i.e. the new numbers are NOT meaningfully better, if anything modestly worse (fewer, longer segments means each one carries a larger share of the initial "crack the whip" energy). `max_reach_unfold` came in at 10.0-10.25 against the new 8.4 capacity (a ~19-22% overshoot ratio, in the same ballpark as the old config's own relative overshoot). Per the task's own explicit instruction ("don't re-attempt the exact same two already-failed approaches by default... unless the new numbers suggest it's now easy"), since the new baseline is not meaningfully easier, **no new stiffness-tuning attempt was made this round** — ROUND 13/14's open finding (both moderate bias/damping tuning and `Generic6DOFJoint3D` conclusively fail; a from-scratch position correction breaks obstacle collision) stands unchanged, now confirmed to still apply at the new sizing rather than assumed to.
      - **`tests/test_rope_leash_corner_wrap.gd` and `tests/test_rope_recall_wrap_path.gd`**: both already-known-flaky tests (ROUND 9-12's own documented PASS/FAIL flipping on identical code) were run multiple times at the new sizing AND cross-checked against the old, un-resized code via `git stash`, per this project's own standing discipline, rather than either assumed to be fine or assumed to be broken from a single run.
        - `test_rope_leash_corner_wrap.gd`: new sizing across 3 runs — `max_pillar_penetration` 0.0602/0.0000/0.0000, `fold_events` 2/0/1. Old sizing across 2 runs (via `git stash`) — `max_pillar_penetration` 0.0000/0.0089, `fold_events` 0/0. The new sizing's worst sample is somewhat worse than the old sizing's worst sample here, but both configs show wide run-to-run spread with overlapping ranges (new sizing's own best run, 0.0000 penetration/0 folds, matches the old sizing's own best run) — consistent with this test's already-documented chronic flakiness, not conclusively a new regression, though the sample size (2-3 runs each) is too small to fully rule one out either.
        - `test_rope_recall_wrap_path.gd` Scenario A: new sizing across 4 runs — `max_pillar_pen` 0.0745(FAIL)/0.4256(FAIL)/0.0000(PASS)/0.1193(FAIL), i.e. 3/4 FAIL. Old sizing across 3 runs (via `git stash`) — `max_pillar_pen` 0.0000/0.0000/0.0000, 0/3 FAIL, notably consistent (`max_path_dev` within 0.060-0.086 across all three). **This is a more suggestive, though still not fully conclusive, signal than the leash-corner test above** — the old config's own 3-for-3 clean result is tighter than its usual documented flakiness elsewhere, while the new config failed 3 of 4 times, with a plausible mechanism: fewer, larger segments track a tightly-wrapped corner with coarser resolution during `RECALLING`'s fast tip retraction specifically (the scenario this test targets), and a larger segment cutting the corner more will read as a bigger single-sample penetration than several smaller ones would. Flagged here as an honest, disclosed, NOT-yet-conclusively-proven regression risk from the segment-count reduction specifically in the recall/wrap-retrace path — worth a dedicated follow-up with more samples if the user reports the rope visibly cutting through a pillar specifically while being reeled back in around a corner (as opposed to while anchored/idle, which the settled-configuration sweep above shows is unaffected).
        - `tests/test_rope_obstacle_clip.gd` (no `_TEST_DONE` sentinel; polled for its own `RESULT` line instead): new sizing `max_pillar_penetration_over_240ticks` = 0.4968 (one run); old sizing (`git stash`) = 0.0000 and 0.6462 across 2 runs — the new sizing's single sample sits comfortably inside the old sizing's own observed spread, consistent with this test's own already-documented pre-existing flakiness (ROUND 9/11/12), not a new regression.
      - `tests/test_movement_direction_skew.gd`, `tests/test_dart_void_throw.gd`, `tests/test_player_map_boundary.gd`: all PASS cleanly, one run each (none of these three are rope-physics-chain-penetration-sensitive in the way the four tests above are, and none are documented as flaky in this file) — `test_dart_void_throw.gd`'s own `ROPE_LENGTH` mirror was updated to 8.4 first (see above) so its exact-distance assertion (`dist_from_origin == ROPE_LENGTH`) stayed meaningful.
      - **A ~65-second, 4-hard-bot headless soak of the real `scenes/main.tscn` game loop** (`game_manager.gd`'s `lobby_mode`/`human_count`/`bot_difficulty` toggled at runtime only, reverted after — zero net diff confirmed via `git diff` before finishing) produced **zero output lines beyond the engine's own startup banner** — no errors, no warnings, across sustained real hard-bot throw/anchor/wrap/recall cycling at the new sizing.
    - **Honest confidence caveat**: the resize itself (new `DART_ROPE_LENGTH`/`ROPE_LENGTH`=8.4, `ROPE_PHYSICS_SEGMENTS`=24, mass redistribution) is verified numerically to preserve every existing hard guarantee this codebase has already established — idle collapse, the 8-configuration settled-wrap sweep, and the soak run all confirm zero penetration / zero errors at the new sizing, matching or exceeding the old sizing's own results. The one genuinely new, honestly-disclosed finding this round is the `test_rope_recall_wrap_path.gd` Scenario A signal above (3/4 new-sizing fails vs. 0/3 old-sizing fails) — reported as a real possibility, not swept into "known flakiness," but also not proven with enough samples to call it a confirmed regression; the already-open ROUND 13/14 fixed-segment-length issue is unchanged in kind and re-confirmed (not assumed) to persist at the new numbers, at roughly the same or slightly worse severity. Not verified visually — no screenshot/video access this session, so whether 8.4-unit/24-segment rope actually *reads* as meaningfully shorter on an actual screen (the user's own literal complaint, "the rope is currently too long") is unconfirmed; the numeric resize is correct per the user's own explicit formula, but this is a case where the spec was given as an exact multiplier rather than derived from visual feedback, so there's nothing further to numerically verify on that front.
  - **ROUND 16 (2026-07-25) — CORRECTION of ROUND 15's own height-basis pick, per direct user instruction, plus a first real geometric investigation of the idle "collapsed at the hand" shape.**
    - **Height basis switched 1.4 -> 1.2, i.e. `PLAYER_HALF_HEIGHT*2.0` -> the raw `scenes/player.tscn` `CapsuleShape3D.height`.** The coordinator pointed out a real contradiction in ROUND 15's own output: that round's explicit goal was "the rope is currently too long," but its own chosen basis (1.4) made the rope LONGER (6x1.4=8.4) than its pre-ROUND-15 value (8.0) — the opposite of the stated goal. Asked whether the raw physics capsule height (1.2, giving 6x1.2=7.2, genuinely shorter) was intended instead. User's reply, verbatim: **"Change to 1.2."** New shared constant `GameManager.PLAYER_CAPSULE_HEIGHT := 1.2` was added (mirroring `scenes/player.tscn`'s `CapsuleShape3D.height` by hand, same "no way to read a .tscn sub-resource from a const expression, so hand-sync it" convention this file already uses for e.g. `HITBOX_DEBUG_RADIUS`) so `player.gd`'s `DART_ROPE_LENGTH` and `rope_dart.gd`'s `ROPE_LENGTH` both reference `6.0 * GameManager.PLAYER_CAPSULE_HEIGHT` instead of either a bare `1.2` literal or the old `PLAYER_HALF_HEIGHT*2.0` expression. **New length: 7.2** (down from 8.4, and down from the original pre-ROUND-15 8.0 — this is the first sizing round that actually satisfies "the rope is too long, make it shorter"). `ROPE_PHYSICS_SEGMENTS` stays `24` (unchanged); `ROPE_PHYSICS_SEGMENT_LENGTH` (already derived as `DART_ROPE_LENGTH / ROPE_PHYSICS_SEGMENTS`, no formula change needed) is now **0.3 exactly** (down from 0.35).
    - **`ROPE_SEGMENT_MASS` re-derivation changed its own underlying principle, per the task's explicit instruction to scale mass with the new total length, not just hold it fixed.** Re-examined ROUND 12->15's own history first: the ratio "mass per unit of segment length" (`0.03` at the original 8-segment/1.0-length-per-segment baseline) was actually what stayed constant across every segment-COUNT change (8->32->24), not literally "total chain mass" — ROUND 15's own "keep the whole chain's total mass at 0.24" phrasing was equivalent to this density only because that round's own total length (8.4) never changed. Since this round changes the LENGTH too (8.4 -> 7.2), the constant is now expressed directly as `ROPE_SEGMENT_LINEAR_MASS_DENSITY (0.03) * ROPE_PHYSICS_SEGMENT_LENGTH`, so total chain mass automatically scales down with the shorter rope: `0.01 -> 0.009` per segment, `0.24 -> 0.216` total. This is a genuine behavior difference from "just recompute the same 0.24/24," but a physically better-justified one (same real-world principle as a shorter length of the same rope material weighing less) and was explicitly requested by name ("re-derive mass-per-segment proportionally for the new total length").
    - **Every other reference re-checked, not assumed fine**: grepped for `8.4`/`DART_ROPE_LENGTH`/`ROPE_LENGTH` across `scripts/` and `tests/`. `tests/test_dart_void_throw.gd`'s `ROPE_LENGTH` mirror and `tests/test_rope_leash_corner_wrap.gd`'s `DART_ROPE_LENGTH` mirror (both explicitly hand-synced to the production constant, same convention ROUND 15 already used) were updated to the same `6.0 * GameManager.PLAYER_CAPSULE_HEIGHT` expression. `tests/test_movement_direction_skew.gd` already read `player.DART_ROPE_LENGTH` dynamically; only a stale, non-load-bearing comment (`ROPE_LENGTH=8`, itself already wrong/stale before this round since it never got bumped to 8.4 in ROUND 15) was corrected to stop citing an exact number that keeps drifting. `bot_controller.gd`'s `THROW_RANGE` (max 7.0) and its unrelated dodge-detection `8.0` heuristic were re-checked against the new, smaller 7.2 capacity: `THROW_RANGE`'s max (7.0) is now much closer to the rope's own new capacity (7.2) than it was to the old one (8.4) — still comfortably inside it, not touched, but flagged here as having much less headroom than before if `THROW_RANGE` is ever tuned upward again.
    - **GEOMETRIC INVESTIGATION — "Is the rope folded when in hand?" answered from real data, not a guess.** Added a temporary diagnostic block to `tests/test_rope_physics_chain_settle.gd`'s `_test_idle_collapse()` (logged every settled segment's position AND orientation relative to the hand, plus a circular-variance summary of orientation and a min/avg/max radius summary; reverted before commit, zero net diff on that file, same TEMP-TESTING convention as `game_manager.gd`'s `lobby_mode` toggle) and ran it at the new 7.2/24/0.3 sizing.
      - **Position data (from a representative run, 24 segments, hand at the origin)**: segment 0 and segment 23 (the two ends, both pinned to the hand while `dart == null`) sit essentially at r=0.001 from the hand, as expected. Walking through the middle segments in index order, the radius from the hand rises smoothly (no jumps — every consecutive pair of segment centers is 0.13-0.30 units apart, matching each segment's own ~0.3-unit physical length almost exactly, confirming the joints are genuinely satisfied, not just approximately) out to ~0.7 by segment 8, dips back down to ~0.13 near segment 12 (close to the hand again), then rises a second time out to a peak of ~0.9 around segments 16-18, before returning to ~0 at segment 23. **This traces a real, physically continuous path that loops out from the hand and back TWICE** (roughly a doubled-over squiggle/loose figure-eight-ish shape near the hand), not a single clean loop and not a tight spiral coil with evenly-stacked rings.
      - **Orientation data**: circular variance of each segment's own long-axis orientation came out at **0.9464 and 0.9650 across two separate runs** (0 would mean every segment points the same direction, reading as a neat stacked/folded bundle; 1 would mean orientations are uniformly random) — i.e. very close to the "scattered" end of the scale, NOT "all roughly parallel." Individual segment yaws jump around by 50-150+ degrees between adjacent segments with no smooth rotational trend.
      - **Honest characterization, in plain words**: the rope's idle rest shape is **not** a neat, deliberately-organized coil or accordion-fold (which would show low orientation variance and/or evenly-spaced concentric rings) — but it is also **not** a broken, chaotic tangled ball (positions are smooth and continuous step-to-step, radius is bounded to well under 1 unit, and the overall two-lobe loop shape reproduces across runs, even though the exact fold path differs run to run — consistent with ROUND 12's own documented finding that "any folded shape that satisfies every joint is an equally valid equilibrium" for a slack, zero-tension, zero-gravity, non-self-colliding chain with both ends pinned to the same point). The most accurate plain-language description: **a loose, doubled-over squiggle or scribble of rope resting near the hand** — physically plausible for a slack rope with no self-collision keeping it from crossing itself, but visually it will read as a small messy loop-de-loop rather than a crisp intentional coil.
      - **Decision: no fix shipped this round.** Per the task's own explicit criterion ("only if the diagnostic data actually shows a real problem worth fixing"), this data shows a real, stable, converged, non-diverging equilibrium that traces a continuous (not jittery/discontinuous) shape within a small, bounded radius — a legitimate "collapsed near the hand" result, just not an aesthetically deliberate-looking coil. Since the render is an uncorrected Catmull-Rom curve through these exact positions (per the ROUND 12 architecture reset's own non-negotiable "no synthetic force it there" rule), the only physics-compatible lever available if the user wants a tidier look later is biasing the INITIAL spawn/rest layout or joint rest angles to naturally settle into a more coil-like shape (e.g. a helical rather than straight-line initial bunch — ROUND 12 already tried and rejected a naive spiral spawn layout for a different reason, see its own doc comment, so any future attempt should account for that prior finding) — not attempted this round since the data didn't show a clear enough problem to justify it, and the task explicitly asked not to over-engineer without one.
    - **Verification protocol, using the same headless-Bash-invocation approach as ROUNDS 14/15** (no `mcp__godot__*` tools were available this session either). Checked for stray processes first: only the user's own separate `--editor` checkout at a different path was running, left untouched.
      - `tests/test_rope_physics_chain_settle.gd`: idle collapse `max_dist_from_hand` ranged **0.899-1.406** across 4 runs at the new 7.2/24/0.3 sizing (comfortably inside the unchanged `IDLE_COLLAPSE_RADIUS=1.6`, same order of magnitude as ROUND 15's own 1.11-1.21 at 8.4/24). All 8 settled-configuration-sweep obstacle-wrap tests passed at `0.00000` penetration on the final clean run; one earlier run saw `open_air_far` (a deliberately adversarial config whose straight hand-to-tip line runs directly through `PillarA`'s own corner) fail at 0.092-0.148 — **cross-checked against the OLD 8.4/24 sizing via `git stash`, which failed the SAME config even worse (0.448)** — confirming this is the same pre-existing, already-more-severe-at-the-old-size behavior, not a new regression from the length reduction; if anything the shorter rope fares slightly better here. Post-retrieve collapse stayed well inside `POST_RETRIEVE_COLLAPSE_RADIUS=4.5`.
      - **ROUND 13/14's still-open "fixed segment length" issue, re-measured at 7.2/24/0.3, not assumed unchanged**: `max_joint_gap` during a full-charge throw's unfold read 2.70-4.01 across 3 runs (vs. the OLD 8.4/24 sizing's own 3.53 on a direct `git stash` re-run this round) — same order of magnitude, no clear direction of change; `max_reach_unfold` came in at 9.7-10.3 against the new, smaller 7.2 capacity, a ~35-43% overshoot ratio — a LARGER relative overshoot than ROUND 15's own 19-22% at 8.4, since the same absolute joint-gap slop is now a bigger fraction of a shorter total capacity. This is a genuine, disclosed, quantitative side effect of shrinking the rope while the underlying ROUND 13/14 rigidity issue remains unfixed — not a new issue in kind (the fixed-length problem itself is unchanged), but the shorter rope now "shows" that problem proportionally more. Still not attempted (out of scope per the task's own explicit "don't re-attempt already-failed approaches without a new reason" instruction, and no new lever was suggested this round either).
      - **`tests/test_rope_leash_corner_wrap.gd`**: 3 runs at new sizing — `max_pillar_penetration` 0.0000/0.0000/0.0000, `max_chain_overshoot` 2.233-2.820, `fold_events` 0/0/2. 3 runs at old 8.4 sizing (via `git stash`) — `max_pillar_penetration` 0.0000/0.0128/0.0295, `max_chain_overshoot` 1.167-2.671, `fold_events` 0/1/2. Overlapping ranges on every metric; if anything pillar penetration is cleaner at the new, shorter sizing. `max_chain_overshoot` fails this test's own `MAX_ACCEPTABLE_OVERSHOOT=0.75` threshold at BOTH sizings on every single run — this is the same ROUND 13/14 rigidity issue surfacing here too, pre-existing and unrelated to this round's resize.
      - **`tests/test_rope_recall_wrap_path.gd` Scenario A — the ROUND 15-flagged regression-risk signal, re-checked specifically as the task requested**: 3 runs at new 7.2 sizing — `max_pillar_pen` 0.2940(FAIL)/0.0000(PASS)/0.1094(FAIL), i.e. 2/3 FAIL. 3 runs at old 8.4 sizing (via `git stash`, same session) — `max_pillar_pen` 0.0000/0.0000/0.0000, 0/3 FAIL, matching ROUND 15's own report of a consistently-clean old-sizing baseline. **The signal is still present and, on this round's samples, not improved** — the shorter/still-24-segment rope continues to show real (if run-to-run variable) penetration specifically in this recall-around-a-corner scenario where the old, longer rope did not, consistent with ROUND 15's own hypothesized mechanism (fewer, now-even-larger-relative-to-total-length segments track a tight wrap with coarser resolution during fast `RECALLING` retraction). Still not proven as a hard regression with high confidence (small sample sizes throughout this test's history, and it already has documented flakiness), but two consecutive rounds now show the same direction of effect at progressively shorter rope lengths — this is the strongest each-round-more-consistent signal this specific test has produced yet, and is worth prioritizing in a future round if the user reports the rope visibly cutting through a pillar while being reeled in around a corner specifically.
      - `tests/test_rope_obstacle_clip.gd`: 2 runs at new sizing (0.2249, 0.2497) plus one earlier run (0.5216); 2 runs at old sizing via `git stash` (0.2193, 0.2193). All four new-sizing samples and both old-sizing samples fall within the same broad, already-documented flaky range for this test (ROUND 9/11/12/15) — no clear directional change.
      - `tests/test_movement_direction_skew.gd`, `tests/test_dart_void_throw.gd`, `tests/test_player_map_boundary.gd`: all PASS cleanly, one run each.
      - **A ~90-second, 4-hard-bot headless soak of the real `scenes/main.tscn` game loop** (`game_manager.gd`'s `lobby_mode`/`human_count`/`bot_difficulty` toggled at runtime only, reverted after — zero net diff confirmed via `git diff`) produced only the engine's own startup banner plus the already-known, already-reproduced-every-round `material_get_instance_shader_parameters` headless-dummy-renderer artifact — no new errors, no crashes, across sustained real hard-bot throw/anchor/wrap/recall cycling at the new sizing.
    - **Honest confidence caveat**: the height-basis correction and its downstream numbers (7.2 length, 0.3 segment length, 0.009 segment mass) are simple, verified arithmetic directly following the user's own explicit instruction — nothing left ambiguous there. The idle-shape geometric characterization is a genuine first-time answer to a previously-unaddressed question, backed by real per-segment position/orientation data (not a guess), but is inherently a qualitative judgment call ("loose doubled-over squiggle" vs. some other description a person might choose looking at the same numbers) rather than a pass/fail measurement — and, as with every prior round on this system, it has NOT been visually confirmed on an actual rendered screen (no screenshot/video access this session), so whether this numerically-characterized shape actually reads as acceptable or as a visible problem to an actual player remains open. The `test_rope_recall_wrap_path.gd` Scenario A signal is real and reproducible in direction across two rounds now, but still not large-sample-confirmed. The ROUND 13/14 fixed-segment-length issue remains open and unimplemented, now confirmed (not assumed) to be proportionally more visible at the shorter 7.2 length specifically in the unfold-overshoot-ratio sense, though not in absolute joint-gap magnitude.
  - **A 2026-07-25 EXPERIMENT that dropped the rope's Y-plane lock / `gravity_scale=0` / one-directional collision layer in favor of real gravity + Godot's default collision engine was tried, measured WORSE across every metric that changed (idle collapse, one obstacle-wrap topology, and the already-open joint-rigidity investigation), PLUS a new deterministically-reproduced cross-player bug (any dart's flight raycast could spuriously anchor on any rope segment once segments left the always-undetectable layer 0) — REVERTED WHOLESALE at the start of ROUND 17 (2026-07-26, commit reverting `1f37c47`) before any new work began, per this round's own explicit instruction not to build on top of a result already measured worse. The revert is bit-for-bit identical to this file's own pre-experiment state (confirmed via `git diff` against the commit immediately prior to the experiment) — nothing about the Y-plane lock / `gravity_scale=0` / `ROPE_OBSTACLE_LAYER_BIT` scheme described throughout this section changed as a result of that experiment ever landing on `main`; it only ever touched `feature/melee-slash-v2`, and even there it's now undone.**
  - **ROUND 17 (2026-07-26) — "Why is the rope shaking without the character moving?" investigated as a genuinely NEW configuration (dart ANCHORED and stationary for several seconds, no throw/retrieve in progress) — direct measurement found the real physics chain already near rest, RigidBody3D body-level `linear_damp`/`angular_damp` raised 1.6/2.2 → 3.0/4.0 as a real, disclosed, modest improvement, and a separate, comparably-sized, NOT-yet-addressed root cause identified in `arena_camera.gd`.**
    - **New test**: `tests/test_rope_physics_chain_settle.gd` section "5. ANCHORED STEADY-STATE JITTER" (`_test_anchored_steady_state_jitter()`/`_measure_steady_state_jitter()`) — anchors a dart via 4 different paths (`open_air_taut`: max-range, ~zero slack, the common real "missed, anchored in open air" case; `open_air_slack`: a shorter real anchor with real slack; `corner_wrap_anchor`: real obstacle contact; `real_throw_at_pillar`: a REAL `_throw()` left to fly and anchor naturally via `rope_dart.gd`'s own raycast, not a shortcut, to rule out the forced-anchor shortcut itself being unrepresentative), waits `STEADY_PRE_SETTLE_TICKS` (5s) past the anchor transition, then samples `STEADY_SAMPLE_TICKS` (6s / 360 ticks) of TICK-TO-TICK (not vs. a fixed reference) position deltas for every dynamic segment, the driven hand-anchor boundary (X/Z tracks the real, `Idle_A`-animated hand bone — see `get_hand_world_position()`'s own doc comment), and every per-joint gap (`_joint_gaps()`, already existing).
    - **First test-authoring mistake caught and fixed before trusting any number, per this project's own standing "verify, don't assume" discipline**: an early version of the `open_air` config copied the settled-configuration-sweep's own `open_air_far` hand/tip pair verbatim (~22.6 units apart) — physically impossible for a chain whose total capacity is `DART_ROPE_LENGTH`=7.2, since a kinematic tip anchor frozen at an unreachable distance forces the solver to fight an unsolvable constraint every tick forever. Measured symptom before catching this: `amplification(seg/hand)=2031x`, sustained (not decaying) `joint_gap_mean=0.78`. Re-run with PHYSICALLY ACHIEVABLE hand/tip distances (`open_air_taut` at exactly `DART_ROPE_LENGTH`, `open_air_slack` well under it) immediately dropped this to normal-range numbers — a real artifact of an invalid test config, not a reproduction of the reported bug, caught and fixed rather than reported as a finding.
    - **Real baseline numbers (OLD 1.6/2.2 damping, 3 repeated runs each for variance, all several seconds after a real settle)**: `open_air_slack`, `corner_wrap_anchor`, and `real_throw_at_pillar` all measured tiny and consistent — `seg_max_step` 0.0007–0.006 units/tick, `joint_gap_max` 0.000–0.008. `open_air_taut` (zero-slack, max-range) alone showed real, bimodal, run-to-run variance — `seg_max_step` ranging 0.0019–0.0335, `joint_gap_max` 0.004–0.041 — i.e. even the WORST of these four real configurations, at the OLD damping, is already an order of magnitude smaller than what the user's own video description ("visibly, measurably changes shape... from frame to frame") would suggest for a rope that's 7.2 units long.
    - **Damping raised to 3.0/4.0, same test re-run 3x for variance plus the full existing regression suite once**: `open_air_slack`, `corner_wrap_anchor`, `real_throw_at_pillar` all dropped further — `seg_max_step` now 0.0002–0.002, `amplification(seg/hand)` frequently below 1.0 (the chain now damps out the driving hand-bone's own tiny idle-animation motion faster than it introduces it, vs. 2-13x amplification at the old damping) — a real, direct, measured improvement, not assumed. `open_air_taut` was UNCHANGED in kind by this — still the same ~0.0002–0.03 bimodal range at the new damping too. Reasoned root cause: a chain sitting at EXACTLY its own total physical capacity (zero slack) has no spare length to silently absorb a tiny per-joint numerical residual the way every other, slacker configuration does — this reads as a solver degenerate-case sensitivity tied to zero slack specifically, a geometric-limit problem, not a "too much kinetic energy" problem body damping (which drains existing motion) is positioned to fix — left open and disclosed, not attempted further this round (matches ROUND 13/14's own separate finding that JOINT-level, as opposed to body-level, stiffness tuning is the dangerous lever in this family; body-level damping was never tried in any round before this one, unlike joint bias/damping, which was tried and rejected multiple times).
    - **Regression check, git-stash A/B same as every prior round**: full suite (idle collapse, 8-config settled-obstacle sweep, throw unfold/retrieve fold) re-run at both old and new damping. Both show the SAME already-long-documented flakiness class (ROUND 9/11/12/15/16) on the settled-configuration sweep — at old damping, `open_air_far` (the intentionally-overstretched penetration-only config, unrelated to jitter) flaked FAIL twice of 3 runs (0.077, 0.557 penetration); at new damping, a different config (`random_3`) flaked FAIL once of 1 run (0.1998) — same pre-existing noisy-test class, which SPECIFIC config flakes shifts run to run (already an established property of this test), not a new or increased failure rate from the damping change. The already-open ROUND 13/14 fixed-segment-length `unfold` FAIL (`max_joint_gap` tolerance 0.15, never once passed in this file's history) is unchanged in kind. A 50-second, 4-hard-bot headless soak (`GameManager.lobby_mode`/`total_players`/`human_count`/`bot_difficulty` set at runtime on a scratch scene, same as prior rounds' own soak convention, no `game_manager.gd`/`project.godot` edits made or needed) completed all ticks with zero errors, zero NaN, sustained `PLAYING` state throughout.
    - **A SEPARATE, DIRECTLY-MEASURED, comparably-sized finding this round, NOT addressed by the damping change and flagged for the user's own judgment**: `arena_camera.gd`'s `_process()` recomputes camera position AND orthographic `size` every single frame from the AABB of ALL alive players plus ALL active darts, unconditionally — not just whichever player/dart happens to be the visual subject of a given clip. A scratch probe (a real 2-player match: one player force-anchored and never touched again after settling, one hard bot left to roam freely) measured the camera's own per-frame position/size step at ~0.019 / ~0.025 units/frame — driven ENTIRELY by the other bot moving elsewhere, with the "stationary" player's own rope chain independently confirmed still near-rest (`chain_max_step`=0.017, consistent with the numbers above). This is COMPARABLE TO OR LARGER than this round's own worst-case rope-chain jitter numbers, and is a completely independent mechanism (continuous camera re-centering/re-zooming driven by ANY other active player/dart in the match) that could easily read as "things on screen visibly reshape even though nothing near them moved" to someone watching a recording and only checking whether the ONE player/rope they're focused on is moving. Not fixed this round (out of the explicit "try rope segment damping" scope) — if the user reports this again, first confirm whether other players/bots were active/moving anywhere in the same clip before assuming it's the rope chain again.
    - **Honest confidence caveat**: every number above is a direct measurement (tick-to-tick deltas, not vs. a fixed reference; real A/B via git-stash at identical code otherwise; a real 50s multi-bot soak), not an assumption — same discipline as every prior round. What remains UNCONFIRMED, same limitation as every round on this system: no screenshot/video access this session, so whether the OLD 1.6/2.2 damping's already-tiny numbers, or the NEW 3.0/4.0's smaller-still numbers, actually correspond to what the user saw in their own recording is not something this session could directly verify — the honest, disclosed possibility raised above is that the user's own video may have been showing the camera-motion effect (a totally different, unaddressed mechanism) rather than the rope chain's own physics, given how much smaller the chain's own measured motion is than the reported severity. If the user's next report is still "the rope shakes" with confirmation that they were alone/no other players were active in that specific clip, the `open_air_taut` zero-slack degenerate case (unfixed by this round) is the next physics-side lever to investigate; if other players/bots WERE active, the camera is the more likely culprit and hasn't been touched at all.
  - **ROUND 18 (2026-07-28) — "the rope jitter happens when close to the pillar" (a genuine sustained-contact configuration, camera explicitly ruled out by the coordinator's own frame-by-frame review this round, unlike ROUND 17's own unaddressed camera hypothesis) — FOUR separate hypotheses tested by direct measurement, ALL FOUR RULED OUT; NO gameplay code changed; root cause NOT found this round.** Every mechanism actually measured stayed in the ~0.0002–0.002 units/tick range (at 60 ticks/s) — one to two orders of magnitude below anything that could plausibly read as "visibly, measurably reshapes" on an 8fps recording. This is a genuine negative result, reported honestly rather than forced into a fix.
    - **HYPOTHESIS 1 (the task's own lead): contact-state flicker (`rope_segment_body.gd`'s `_debug_last_has_contact` toggling tick to tick at a genuine obstacle contact) — TESTED, REAL BUT TWO ORDERS OF MAGNITUDE TOO SMALL.** New instrumentation added to `tests/test_rope_physics_chain_settle.gd`'s existing `_measure_steady_state_jitter()` (permanent, kept): per-tick contact-state read for every segment, flicker-event counting (contact↔no-contact transitions), and a contact-vs-non-contact step-size split. On `corner_wrap_anchor` (the existing config with genuine, sustained, diagonal-opposite-corner real obstacle contact — confirmed via direct measurement to hold `contact_ticks=360/360` = 100% of the whole 6s sampling window in every run, not just "near" the pillar), flicker events varied run to run (0, 24, 28, 30 across four separate runs — a real, disclosed run-to-run non-determinism, same class already documented elsewhere in this file) but `contact_step_max` never exceeded **0.00200 units** in any run, and in three of four runs the contact-adjacent segments were the MOST stable part of the chain (`contact_step_max` smaller than `noncontact_step_max`), not the least. Contact-state flicker is real and reproducible, but its magnitude is nowhere near large enough to explain a visibly reshaping loop.
    - **HYPOTHESIS 2 (a new config, "large local excess of slack folded up right at one real corner contact" — deliberately different geometry from `corner_wrap_anchor`'s own near-taut diagonal stretch, matching the user's own "small hook/loop... at the pillar's base" description more closely): `near_corner_slack_bunch`, added as a new permanent config in the same test.** Puts both hand and tip close (0.3 margin) to the SAME single pillar corner on its two adjacent edges (real span ~1.8, wrap path ~2.1, against the same `DART_ROPE_LENGTH`=7.2 capacity — ~5 units of unavoidable slack). **A real, serious-looking finding surfaced here first (hand-anchor tick-to-tick steps up to 1.3 units, recurring with a clean, exactly-reproducible ~160-tick/2.667s period) — investigated to its actual root cause below (HYPOTHESIS 3) rather than reported at face value, and found to be a SELF-INFLICTED TEST-HARNESS BUG, not a real rope/physics/animation phenomenon.** Once fixed (see below), this config measures identically tiny to every other config (`seg_max_step=0.00021-0.00024`, `contact_ticks=0/360` — its own short real span + large slack lets the chain drift away from the corner entirely rather than staying pressed against it, so it does not actually reproduce sustained contact; kept in the suite anyway as a materially different, valid slack/geometry configuration, clean and non-contaminated after the fix below).
    - **HYPOTHESIS 3 (root-causing HYPOTHESIS 2's own initially-alarming numbers) — a genuine, real, but ENTIRELY TEST-HARNESS-SPECIFIC bug, found and fixed; NOT a general animation-clip or physics artifact.** Root-caused via direct measurement, not assumed: (a) a clean control probe — fresh spawn, `Idle_A`, no throw, no rope, logged every tick for 500 ticks (~8.3s, roughly 3 full periods of the suspect ~160-tick interval) — found NOTHING beyond the expected one-time T-pose→`Idle_A` blend-in at spawn, ruling out a general `Idle_A` loop-seam artifact outright. (b) A dedicated isolated repro (same position/throw as `near_corner_slack_bunch`, full per-tick tracing) showed `player.global_position`, `player.velocity`, and `player_mesh.quaternion` all bit-identical across every single logged tick including the "jump" ticks — ruling out CharacterBody3D-vs-pillar collision/depenetration and facing-rotation as causes. (c) The actual mechanism, found by tracing `AnimationPlayer.current_animation`: the character was stuck displaying **"Push"** (the RECALL sequence's own HOLD-phase clip, `anim_len=2.6667`s = exactly the suspect ~160-tick period) — even though this test never calls `recall()`. Root cause: for a SHORT real hand-to-tip span (this config's own ~1.8 units), the REAL, un-overridden throw fired by the test's own force-anchor shortcut can travel far enough within its own brief settle window to land inside `rope_dart.gd`'s real `pickup_radius` (0.9) of the still-standing player — a genuine, natural `ANCHORED → (pickup check) → recall()` transition (`rope_dart.gd`'s own `State.ANCHORED` branch) that sets `player.gd`'s `_is_recalling = true` via its existing dart-state sync in `_physics_process()` — **before** the shortcut's own subsequent `dart.state = 1` override lands. That override stomps `dart.state` back to `ANCHORED`, but nothing in the test shortcut was clearing `_is_recalling` (normally only ever cleared by `_on_dart_returned()`/`kill()`/`reset_for_round()`, none of which a raw test-side override calls) — leaving `_advance_recall_anim()`'s HOLD phase permanently unable to exit (it only exits once `_is_recalling` goes false), so the character was stuck looping "Push" forever, and what looked like a "periodic hand-position jump" was simply that clip's own loop point (real, but only ever visible in this stuck-forever state — a normal in-game recall completes in well under 0.3s, far short of "Push"'s own 2.667s loop length, so this could never surface in ordinary play even if "Push"'s own loop seam has some real discontinuity, which was not separately verified since it's moot). **Fix, in `tests/test_rope_physics_chain_settle.gd` only**: the force-anchor shortcut (shared by all four `jitter_configs`) now also explicitly resets `_is_recalling = false` and both the throw/recall `ThrowAnimPhase` + timer pairs immediately after the `dart.state`/`head_2d` override, matching `kill()`'s own established reset pattern — makes the shortcut produce a genuinely clean forced-`ANCHORED` state regardless of how far the real pre-override throw happened to travel. Re-verified via the same probe: `near_corner_slack_bunch` now measures identically tiny to every other config (see HYPOTHESIS 2 above).
    - **HYPOTHESIS 4 (a mechanism never directly measured before — every prior round's render-side check, ROUND 8/9/10, was about STATIC penetration/overshoot, not DYNAMIC frame-to-frame motion): does the Catmull-Rom tube-mesh curve AMPLIFY the tiny raw-segment jitter into something visually bigger, especially near a sharp corner?** New permanent instrumentation in the same test: `_compute_rope_tube_curve_points()` (the real, shipped render function, called on the real per-tick control points, not a synthetic proxy) is sampled every tick alongside the raw segments, and `curve_amplification = curve_max_step / seg_max_step` is computed and printed for every config. Result: `curve_amplification` stayed in the **0.99×–1.91× range across every config**, and on `corner_wrap_anchor` specifically (the real sustained-contact case) it measured **1.007×** — i.e. essentially no amplification at all; the rendered spline faithfully tracks the already-tiny physics motion with no meaningful spline-induced exaggeration. Rules this out too.
    - **What shipped this round**: `tests/test_rope_physics_chain_settle.gd` only — the four new/extended measurements above (contact-state flicker, the `near_corner_slack_bunch` config, the self-pickup contamination fix, and render-curve amplification tracking) are all kept as permanent additions to `_measure_steady_state_jitter()`'s own diagnostic output, since they're genuinely reusable investigative capability for any future jitter report, and the self-pickup fix is a real bug fix independent of this round's specific investigation (any future config with a short real hand-to-tip span would have hit the same contamination). `STEADY_PRE_SETTLE_TICKS` raised 300→600 (10s, still within the task's own requested 5–10s range) after `near_corner_slack_bunch`'s own (pre-fix, since-explained) slow-looking settle prompted a closer look — kept at the larger value since it's strictly more conservative and costs only a few extra seconds of test runtime. **Zero gameplay code touched this round** (`scripts/player.gd`, `scripts/rope_segment_body.gd`, `scripts/rope_dart.gd`, `scripts/game_manager.gd` all show zero diff, confirmed via `git diff --stat`) — this round is diagnostic-infrastructure-only, per its own honest negative result.
    - **Verification protocol followed in full**: full `tests/test_rope_physics_chain_settle.gd` suite re-run (idle collapse: PASS; all 8 settled-configuration-sweep obstacle wraps: PASS, `0.00000` penetration on every one; throw-unfold: FAIL, but confirmed via direct `git stash` A/B on the same commit to be the exact already-documented, already-open ROUND 13/14 fixed-segment-length issue — baseline measured `max_joint_gap=3.2555` vs. this round's `3.2585`, within normal run-to-run noise, not a new or worse failure; retrieve-fold: unaffected). A 40-second, 4-hard-bot headless soak of the real `scenes/main.tscn` game loop (`GameManager.lobby_mode`/`human_count`/`bot_difficulty` toggled at runtime only, reverted after, zero net diff confirmed via `git diff --stat` before finishing) produced only the engine's own startup banner, zero errors.
    - **Honest confidence caveat, stated plainly rather than glossed over**: this round is a genuine, thorough, multi-hypothesis NEGATIVE result — four independent, directly-measured mechanisms (contact flicker, a large-slack near-corner bunch configuration, a general animation-clip artifact, and render-curve amplification) were tested and every one stayed 1-2 orders of magnitude below any plausible visible threshold. The coordinator's own video review (pixel-identical character/camera/pillar, only the rope's own rendered curve near the contact point visibly reshaping) remains unexplained by anything this round could measure. Two honest possibilities for a follow-up round, neither yet tested: (a) the effect is real but requires a LONGER observation window or a specific, not-yet-reproduced geometry/timing this round's four configs didn't happen to hit (worth trying: an even tighter/more taut real corner-wrap than `corner_wrap_anchor`'s own 1.2-margin diagonal, or sampling well past 6s in case of a very slow, small-amplitude drift this round's window was too short to catch); (b) per this project's own established practice, the single highest-value next step is the same one ROUND 17 already flagged and this round couldn't act on — a real screenshot or screen recording placed directly in the repo (as the coordinator apparently had available this round via direct review, but this session did not) for pixel-level comparison against these now-fairly-exhaustive numeric probes, since blind numeric measurement across four independent hypotheses has now been tried without finding a matching-magnitude mechanism.
  - **ROUND 19 (2026-07-28) — TELEPORT-FREE LEASH REDESIGN, per direct, explicit user requirement (confirmed via a clarifying question): "The max length of the rope shouldn't be computed between the dart and the character."** Confirmed to mean `_clamp_to_rope_leash()` specifically: every round since ROUND 6 had it compute a pivot+radius bound, then SNAP `global_position` directly onto that boundary the instant the player was found outside it — a discontinuous, once-per-violating-tick teleport, exactly the "compute a formula, then force reality to match it" pattern this whole rope saga has repeatedly moved away from everywhere else (the ROUND 12 architecture reset already did this for the rope's own rendering/shape; the player's own canonical position was the one remaining place still doing it).
    - **HYPOTHESIS TESTED FIRST, DIRECTLY, PER THE TASK'S OWN PROTOCOL: "does the old clamp's own repeated firing explain the still-unsolved ROUND 17/18 anchored steady-state jitter?" — REFUTED for the exact bug-report scenario, with a genuine methodology gap found along the way.** `tests/test_rope_physics_chain_settle.gd` NEVER set `GameManager.current_state = PLAYING` anywhere in its history — every spawned test player's real `_physics_process()` early-returns at its `current_state != PLAYING` gate BEFORE ever reaching the leash clamp, meaning every ROUND 17/18 steady-state jitter number was measured with the real leash clamp completely inert the whole time, an unnoticed gap in both of those rounds' own otherwise-thorough measurement. Fixed permanently (this line now sits near the top of `_run()`, applies to the whole file). New permanent instrumentation added to `_measure_steady_state_jitter()` (`player_pos_max_step`/`player_pos_fire_events`, in its own "LEASH-CLAMP-FIRING SUMMARY" print) tracks `player.get_pos_2d()` tick-to-tick during the stationary sampling window — with zero real keyboard input the whole run, ANY motion there is attributable to the leash mechanism itself, nothing else. Result, re-run 3 times across all 5 real configs including `open_air_taut` (zero slack, right at the boundary) and `corner_wrap_anchor` (100% real obstacle contact the whole 6s window, confirmed via the existing contact-state instrumentation): **`player_pos_fire_events = 0/360` in every single config, every run** — the old clamp never fired even once in this exact "player stationary, dart anchored near a pillar" reproduction, while those same two configs' own segment jitter stayed at their already-known-and-still-unexplained ~0.05–0.07 units/tick regardless. A stationary player who starts within their own tether radius simply has nothing for a position-snap to correct. **The still-open ROUND 17/18 jitter has some other, unidentified cause — this round's fix is not that fix, and is not claimed to be.** (A genuinely different scenario where the old clamp WAS proven to fire repeatedly, by this project's own prior evidence: `tests/test_rope_leash_corner_wrap.gd`'s adversarial sweep, where a player actively pushes outward against the tether every tick — see below.)
    - **The redesign — velocity projection before `move_and_slide()`, never a position write after it.** New `player.gd` function `_rope_leash_pivot_and_radius() -> Array` factors out the EXACT SAME bound computation the old function used (wrap-aware pivot-on-first-segment via `rope_segment_body.gd`'s `_debug_last_has_contact` + `_rope_chain_rest_length_2d()` when genuine obstacle contact exists, else a plain fallback circle of radius `DART_ROPE_LENGTH` around the anchor) — a pure read, no side effects, so all of ROUND 6/7's own already-verified wrap-aware/transient-snap reasoning carries over unchanged, only what happens with the result is different. New `_apply_rope_leash_velocity_clamp(delta)` decomposes the player's already-computed `velocity` into radial (toward/away from the pivot, from the player's CURRENT real position) and tangential components; only ever reduces the OUTWARD radial component, down to exactly this tick's remaining budget (`(radius - cur_dist) / delta`, floored at 0), and only ever when moving outward (`radial_component <= 0.0` — i.e. moving inward or purely tangentially — is untouched). Called from `_physics_process()` right before `move_and_slide()` (was: `_clamp_to_rope_leash()` called right AFTER it, directly mutating `global_position`). A player pushing straight outward against a taut tether now decelerates smoothly to a dead stop exactly at the boundary — never overshoots, because it's never allowed to move past the boundary to begin with, not corrected after the fact — while pushing at an angle keeps sliding freely along the boundary, preserving the old function's own stated "slide along the tether's edge" feel through a structurally different mechanism.
    - **Test-harness call sites updated to match the new before-`move_and_slide()` order**: `tests/test_rope_leash_corner_wrap.gd` and `tests/test_rope_corner_tube_overshoot.gd` both manually drive a player through `velocity = ...; _update_physics_rope_anchors(); move_and_slide(); [old] _clamp_to_rope_leash()` each tick (their own established pattern, mirroring `_physics_process()`'s real order) — all 4 call sites (2 per file) reordered to `velocity = ...; _update_physics_rope_anchors(); _apply_rope_leash_velocity_clamp(delta); move_and_slide()`, with no other logic changes.
    - **A real, disclosed test-harness dependency found and fixed while A/B-verifying against a clean baseline, per this project's own standing discipline (`git stash` itself was blocked by this session's own permission classifier; used a non-destructive `git show HEAD:<path> > tmp` + file-swap instead, restored after)**: `tests/test_rope_physics_chain_settle.gd`'s `_test_settled_configurations()` (the 8-config obstacle-penetration sweep, unrelated to jitter) turned out to have been SILENTLY relying on the OLD clamp's own teleport side effect the whole time. Its `open_air_far` config spawns a player at `hand2` then force-anchors the dart's tip ~22.6 units away (deliberately unreachable, to stress-test whether the CHAIN tunnels through a pillar when asked to bridge an impossible span) — with the old clamp, the very first tick would snap the player from `hand2` to within `DART_ROPE_LENGTH` (7.2) of the far anchor, silently shrinking the real span the chain ever had to bridge to something achievable; with the new teleport-free design, a stationary player with zero input genuinely never moves (exactly the intended fix), so the chain is now asked to sustain the full, genuinely unreachable 22.6-unit span for the whole settle window — and can, in that harsher condition, measurably penetrate the pillar between hand and tip (`max_pen` 0.22–0.79 across repeated runs, vs. 0.0 both with the old clamp AND with the new code when `GameManager.current_state` is left at its old default `LOBBY`, which made the whole leash-clamp code path unreachable either way). This was never something `open_air_far` intended to test (its own header comment: "does the chain avoid tunneling through a pillar between hand and tip," independent of player-position mechanics) — fixed by explicitly pinning `player.global_position` to `hand2` every tick of that test's own settle loop, decoupling the measurement from whichever leash mechanism happens to be in effect. Re-verified clean (`0.00000` on all 8 configs) after the fix, matching true-baseline behavior exactly.
    - **Full regression suite, re-run after both fixes**: `tests/test_rope_physics_chain_settle.gd` — idle collapse PASS, all 8 settled-configuration-sweep obstacle wraps PASS at `0.00000`, throw-unfold FAIL (confirmed via the same file-swap A/B against true unmodified baseline+PLAYING-enabled to be the exact pre-existing, already-open ROUND 13/14 fixed-segment-length issue, `max_joint_gap` 3.5–4.4 in every run tried, old or new code — unrelated to this round, not touched), retrieve-fold PASS. `tests/test_rope_leash_corner_wrap.gd` (the adversarial "push outward every tick while wrapped around a corner" stress test — the scenario that WOULD have exercised the old clamp's repeated firing) — re-run 2x on the new code (0.651 PASS, 1.288 FAIL) and 1x on true unmodified baseline (0.972 FAIL), all three runs showing `max_pillar_penetration=0.0000` and `fold_events=0` (the two metrics that actually gate real geometry/visual-fold correctness) — consistent with this exact test's own long-documented (ROUND 7/8/9/10/11/12) chronic PASS/FAIL flip on identical code from real physics/floating-point non-determinism across separate process launches, not a new regression; `MAX_ACCEPTABLE_OVERSHOOT` (0.75) is itself a stale threshold calibrated for machinery deleted at the ROUND 12 reset and never recalibrated since (already flagged as such in ROUND 12's own writeup). `tests/test_rope_corner_tube_overshoot.gd` PASS (`max_pen_raw=0.0000`). `tests/test_rope_obstacle_clip.gd` — small FAIL (`0.0084` penetration), but this test/its underlying code (`rope_segment_body.gd`) is untouched by this round's diff (structurally impossible for this round's change to affect it) and matches the same test's own long-documented pre-existing flakiness (ROUND 9/11: `0.086`–`0.246` on unmodified code). `tests/test_rope_recall_wrap_path.gd` (both scenarios), `tests/test_movement_direction_skew.gd`, `tests/test_dart_void_throw.gd`, `tests/test_player_map_boundary.gd` all PASS cleanly, unaffected.
    - **New permanent regression test for the core tether-limit gameplay guarantee**: `tests/test_rope_leash_velocity_clamp.gd`/`.tscn` — isolates the SIMPLEST case (plain open-air anchor, no obstacle/corner-wrap at all, chosen far enough from both `PillarA`/`PillarB` that a full-radius sweep around it can't accidentally engage the wrap-aware pivot branch, which `test_rope_leash_corner_wrap.gd` already tests deliberately). Scenario A (sustained straight-outward push, 5s): `max_dist_from_anchor` converges to and holds EXACTLY `7.2000` (= `DART_ROPE_LENGTH`, zero measurable overshoot), `max_single_tick_step=0.10000` (ordinary one-tick movement, matching `MOVE_SPEED * delta` exactly), `teleport_ticks=0` across the entire run — direct, positive confirmation both that (a) the tether genuinely still stops outward movement (the actual gameplay mechanic) and (b) it does so with zero discontinuous position jumps (the actual redesign requirement). Scenario B (sustained 45°-angled push, 10s, tangential + outward): `tangential_travel=13.27` (genuinely slides along the boundary, not frozen solid) while `max_dist_from_anchor` settles into a small, stable, self-limiting `~7.13–7.34` band (never runs away) — this residual ~0.06–0.14 band above the 7.2 cap is a real, geometrically-inherent property of ANY discrete-time circular constraint enforced via a per-tick velocity projection (a straight-line tangential step from a point on a circle of radius r always lands very slightly outside it — basic secant-vs-tangent geometry, `~r + step^2/(2r)` — not fixable without curving the per-tick path itself, which would reintroduce exactly the "compute the player's own trajectory" shaping this whole redesign removes), confirmed via a 600-tick/10s sustained run to plateau rather than grow without bound.
    - **Soak test**: a temporary (deleted before commit, per this project's own `lobby_mode` toggle-and-revert convention) scratch scene drove a real 4-hard-bot `scenes/main.tscn` match for 40s/2400 ticks via the actual `GameManager._init_game_local()` path (not a shortcut) — zero NaN player positions on every tick, sustained `PLAYING` state throughout, zero script/physics errors beyond the known benign headless-dummy-renderer `material_get_instance_shader_parameters` line already documented in prior rounds. `git status`/`git diff --stat` confirmed zero net trace of the scratch files after cleanup.
    - **Environment note**: found and cleared roughly a dozen orphaned headless `Godot --headless` processes accumulated from this round's own earlier test invocations (never killed after each background run) before the soak test — they were silently contending for CPU and made later runs (including one `_run()` that appeared to hang past a 2-minute timeout) far slower than a clean run; `pkill -f "Godot.*--headless"` between batches is now the established pattern for this kind of iterative headless test session, not just a one-off cleanup step. The Godot MCP server tools were not available in this session (not present in the tool list) — used the established headless-Bash fallback (`/Applications/Godot_mono.app/Contents/MacOS/Godot --headless --path . res://tests/<scene>.tscn`, backgrounded with a sentinel-polling loop) throughout, per this project's own memory of the same fallback from ROUND 14.
    - **Honest confidence caveat**: every number above is a direct measurement (tick-to-tick player-position deltas via new permanent instrumentation, a dedicated new regression test for the core tether mechanic, full-suite re-runs cross-checked against a true unmodified baseline via a non-destructive file-swap A/B, and a real 40s multi-bot soak) — not inferred from "no errors," consistent with this file's own established discipline. What's genuinely new and load-bearing this round: the hypothesis that motivated the whole task (leash-clamp firing causes the still-open steady-state jitter) is DIRECTLY REFUTED for the specific bug-report scenario, not merely "not investigated" — the redesign itself is justified purely by the user's own explicit architectural requirement, not by claiming it fixes ROUND 17/18's jitter (it doesn't, and isn't expected to). Not visually verified (no screenshot/video access this session) — same standing limitation as every round in this file; what's confirmed is that the player's own canonical position now provably never jumps discontinuously (`teleport_ticks=0`, `player_pos_fire_events=0/360`) while the hard tether-limit gameplay guarantee remains intact and directly measured (`max_dist_from_anchor` converges to exactly the rope's own capacity and stays there).
  - **ROUND 20 (2026-07-29) — FLAT LEASH RADIUS, per direct, explicit user requirement: "The max rope length should be total rope length instead of the distance between dart and character."** Confirmed to mean `_rope_leash_pivot_and_radius()`'s own WRAP-AWARE branch (untouched by ROUND 19, which only changed HOW the bound it returns gets enforced, not what the bound IS): while at least one segment rested against real obstacle contact, that branch pivoted on the chain's first dynamic segment with `radius = DART_ROPE_LENGTH - rest_len`, where `rest_len` was `_rope_chain_rest_length_2d()` — a value LIVE-COMPUTED every tick from the chain/dart's current real positions. This is exactly the "computed from the distance between the dart and the character" pattern the user is now rejecting, the SAME standing preference as ROUND 19's own request one round earlier (that round already removed the position-teleport half of this system; this round removes the underlying radius FORMULA itself, not just how it was applied). **This is a deliberate simplification per repeated, direct, two-consecutive-round user preference — not a bug fix, and not something to silently walk back even though it trades away some geometric correctness (see tradeoff below).**
    - **The change**: `_rope_leash_pivot_and_radius()` now unconditionally returns `[anchor, DART_ROPE_LENGTH]` — the wrap-aware branch (pivot-on-first-segment, live `rest_len` subtraction) was deleted, not merely bypassed. `_rope_chain_rest_length_2d()`, whose sole caller was that deleted branch, was removed as dead code (only remaining references are historical doc-comment prose in `tests/test_rope_corner_tube_overshoot.gd`, describing an already-superseded mechanism, left as-is). `tests/test_movement_direction_skew.gd`'s own diagnostic-only debug-print block, which called the now-deleted helper purely for logging, was updated to report the real (flat) bound directly instead — no behavior/assertion change, only what the diag string prints.
    - **KNOWN, DISCLOSED TRADEOFF (measured honestly, not papered over)**: this was expected, per the task's own framing, to reintroduce some of the ROUND 6 (`dd8c600`) corner-wrap-at-max-leash fold failure mode — a player wrapped around a corner can now walk further outward than the real, already-partly-spent-on-the-wrap chain can physically reach, since the flat constant no longer accounts for length used up going around the corner. Re-ran `tests/test_rope_leash_corner_wrap.gd` 3x on the new flat-radius code: `max_chain_overshoot` = 0.923, 0.914, 0.836 (all > the test's own stale `MAX_ACCEPTABLE_OVERSHOOT = 0.75` threshold, so all 3 runs report `FAIL` on that metric) — but **`max_pillar_penetration = 0.0000` and `fold_events = 0` in all 3 runs**, the two metrics the test's own header comment calls out as gating "real geometry/visual-fold correctness" (as opposed to `max_chain_overshoot`, a softer "how far past its own fixed length is the chain's reported path being asked to stretch" measure). Compared directly against ROUND 19's own last-recorded numbers for this exact test (`0.651` PASS, `1.288` FAIL on new-ROUND-19 code; `0.972` FAIL on a true pre-ROUND-19 baseline — also always `max_pillar_penetration=0.0000`/`fold_events=0` in every one of those runs too): ROUND 20's `0.836–0.923` band sits comfortably WITHIN the noise range this exact test has always shown across separate process launches (already documented since ROUND 7/8/9/10/11/12 as chronic real physics/floating-point non-determinism, not code-driven) — and is actually a *tighter* band (spread 0.087) than ROUND 19's own 3-run spread (0.637). **Net honest read: `max_chain_overshoot` did not measurably worsen beyond this test's own pre-existing run-to-run noise, and the two metrics that most directly correspond to visible geometry/fold correctness (penetration, fold events) show zero change at all** — the independent geometric clamps inside `rope_segment_body.gd` (Y-plane lock, `MAX_SEGMENT_SPEED`, `ROPE_TAUT_PERP_RADIUS` distance-from-line tension clamp) that keep the chain's own shape coherent are completely unaffected by this change (they operate on the chain's real simulated positions, not on where the leash allows the player to stand) and appear to be doing enough of the real work of preventing visible penetration/folding on their own that the leash radius formula's own contribution to that specific failure mode is smaller than the ROUND 6 writeup's original framing assumed. This is a genuine, disclosed, not-catastrophic tradeoff, not a clean bill of health — a player CAN now stand further from a wrapped corner than the chain's true wrapped length allows (the literal, intended effect of the user's own request), it just doesn't reproduce as pillar penetration or a visible zigzag fold in this test's own adversarial sweep.
    - **A/B'd against a clean pre-ROUND-20 baseline** (non-destructive `git show HEAD:scripts/player.gd` + file-swap, this project's own established convention — `git stash` remains blocked by this session's own Bash permission classifier) for the unrelated full regression suite: `tests/test_rope_physics_chain_settle.gd`'s idle-collapse and 7-of-8 settled-configuration-sweep obstacle wraps PASS identically on both baseline and ROUND 20 code; `random_3` config penetration is nonzero on BOTH (`0.06359` baseline, `0.00873` new code) — same pre-existing flaky config, not a new regression, in fact numerically better on the new run (attributable to the same run-to-run physics noise, not a real improvement); throw-unfold `max_joint_gap` 3.3379 (baseline) vs. 3.4018 (new code), both within the already-documented pre-existing ROUND 13/14 `3.5–4.4` band (this specific run landed just under it on both sides, same bug, not a regression); `LEASH-CLAMP-FIRING SUMMARY: player_pos_fire_events=0/360` identically in every one of the 5 steady-state configs on BOTH versions — this test's own configs never push the player outward, so the leash radius formula change (which only affects the boundary a pushing player can be stopped at) produces literally zero observable difference here, confirming the flat-radius change's blast radius is exactly as narrow as intended.
    - **`tests/test_rope_leash_velocity_clamp.gd` (ROUND 19's own new test, isolating the plain open-air/no-wrap fallback circle case) re-run on the ROUND 20 code**: Scenario A (straight-outward push) converges to and holds exactly `max_dist_from_anchor=7.2000` (`DART_ROPE_LENGTH`), `max_single_tick_step=0.10000`, `teleport_ticks=0` — bit-for-bit identical to ROUND 19's own recorded numbers, as expected, since this test was already isolated to the open-air fallback circle (which was already `[anchor, DART_ROPE_LENGTH]` even before this round — ROUND 20 only removed the OTHER, wrap-aware branch this test deliberately avoids exercising). Scenario B (45°-angled push) converges to the same small, self-limiting `~7.34` secant-creep band. **Confirms the velocity-clamp mechanism itself (ROUND 19) is functionally unchanged and unaffected by ROUND 20's radius-formula simplification** — in every configuration now (not just the previously-already-flat open-air case), a straight-outward push converges to exactly `DART_ROPE_LENGTH`, since that IS the bound in every configuration as of this round, not just the fallback one.
    - **Multi-bot soak**: 4 hard bots, `GameManager.lobby_mode`/`human_count`/`bot_difficulty` toggled at runtime only (never the file's own committed default, reverted immediately after — this project's own established convention), real `scenes/main.tscn` via headless Godot for a sustained 60s — zero script errors, zero `SCRIPT ERROR`/crash lines in the captured log (only the already-documented benign headless-dummy-renderer `material_get_instance_shader_parameters` noise seen in some but not all runs, pre-existing and unrelated).
    - **Honest confidence caveat**: numerically verified via direct re-runs of all four requested test files (`test_rope_leash_corner_wrap.gd` x3, `test_rope_physics_chain_settle.gd` x2 A/B, `test_rope_leash_velocity_clamp.gd`, plus `test_movement_direction_skew.gd` re-run clean after its own diagnostic-only edit) and a real multi-bot soak — not inferred from "no errors" alone. What's genuinely disclosed rather than hidden: this change trades away some of ROUND 6's own corner-wrap correctness margin by design, per the user's own explicit, repeated (two-round) direction; the measured impact on the two metrics most tied to visible correctness (pillar penetration, fold events) was zero across every sample taken, but this is a narrower, softer-adversarial regression test than a full range of real player corner-hugging behavior, and `MAX_ACCEPTABLE_OVERSHOOT`'s own staleness (flagged since ROUND 12, still not recalibrated) means the test's own PASS/FAIL verdict on `max_chain_overshoot` should be read with that caveat, not taken as a strict gate. Not visually verified (no screenshot/video access this session) — same standing limitation as every round in this file.
  - **ROUND 21 (2026-07-29) — WRAP-AWARE LEASH RESTORED, per direct user report on ROUND 20's own disclosed tradeoff manifesting for real: "The rope is too long when it's wrapped around the map object. Even when the rope is not straight the max length shouldn't change. It should always be fixed."**
    - **Why this is NOT a reversal of ROUND 19/20's own standing preference, despite superficially undoing ROUND 20's diff** — this was read carefully before touching any code, per the task's own explicit framing, rather than assumed to mean "put the old formula back because the user changed their mind again": ROUND 19/20 established two SEPARATE, independent requirements that this round is careful to keep apart:
      1. **ROUND 19's requirement — HOW the boundary is enforced must never teleport the player's canonical position.** Still in full effect, completely untouched by this round: `_apply_rope_leash_velocity_clamp()` still only ever projects velocity before `move_and_slide()`, never snaps `global_position` after it. Verified unchanged both by inspection (this round's diff touches zero lines of that function's own logic, only adds a doc-comment note) and by re-running `tests/test_rope_leash_velocity_clamp.gd`, whose numbers came back bit-for-bit identical to ROUND 19/20's own recorded values (see below).
      2. **ROUND 20's requirement — WHAT the boundary is must be "total rope length," a value the user considers fixed.** ROUND 20 read this as "always exactly `DART_ROPE_LENGTH`, flat, no matter the geometry" — and shipped that, with its own writeup explicitly disclosing the tradeoff as a real, known risk ("a player CAN now stand further from a wrapped corner than the chain's true wrapped length allows"). The new report is that disclosed risk actually manifesting: a player CAN reach a position where the straight-line distance to the anchor reads as exactly `DART_ROPE_LENGTH` (satisfying the flat check) while the REAL wrapped path (hand → around the corner → dart) is longer than `DART_ROPE_LENGTH` — i.e. the rope's fixed physical budget being asked to stretch past its own capacity. That is precisely what "the rope is too long when it's wrapped" describes, and precisely what "the max length shouldn't change, it should always be fixed" is asking to be respected: the TOTAL REAL ROPE BUDGET (`DART_ROPE_LENGTH`) must never be exceeded, wrapped or not. A flat circle around the anchor that ignores wrap geometry entirely does not actually keep the max length fixed once a wrap is involved — it lets the real total path run past `DART_ROPE_LENGTH` while still reporting the boundary as satisfied. Restoring wrap-awareness is what makes "max length is always fixed" literally true again, not a rejection of it.
    - **The fix**: `_rope_leash_pivot_and_radius()` and `_rope_chain_rest_length_2d()` restored to their exact ROUND 6/7 form (see `55348f7`'s own reverse-diff, applied verbatim) — while at least one physics-chain segment genuinely rests against real obstacle contact (`rope_segment_body.gd`'s own `_debug_last_has_contact`, an unambiguous "the solver actually put this segment here" signal), the bound pivots on the chain's own first dynamic segment with the radius shrunk by `_rope_chain_rest_length_2d()` — the REAL, already-simulated remaining chain length from that segment to the tip, wrap included, read from live physics state, not an invented/synthetic route. Otherwise (no real contact — the common open-air case) it falls back to the plain `[anchor, DART_ROPE_LENGTH]` circle, unchanged from ROUND 20. This is deliberately real-physics-state, not a hand-computed corner/geometry formula — the same distinction the user's own ROUND 19/20 objection was actually drawing (rejecting synthetic "compute a route" logic and position teleports, not rejecting reading the chain's own real simulated state).
    - **Verification, protocol-driven per this round's own explicit instructions**:
      - `tests/test_rope_leash_corner_wrap.gd`, re-run 3x: `max_chain_overshoot` = 0.734 (PASS against the test's own stale `MAX_ACCEPTABLE_OVERSHOOT=0.75`), 1.177, 1.513 (both FAIL on that same stale threshold) — but **`max_pillar_penetration=0.0000` and `fold_events=0` in all 3 runs**, the two metrics the test's own header comment calls out as gating real geometry/visual-fold correctness. Directly compared against ROUND 6's OWN original numbers for this exact mechanism (recorded in this file's own ROUND 6 entry above): overshoot 0.68–1.02, penetration 0.000–0.128, fold jump 1.18–1.67 across 1–3 events. This round's restored code matches or beats ROUND 6 on both gating metrics (0.0000 penetration vs. 0.000–0.128, 0 fold events vs. 1–3) — a genuine improvement, not merely parity — while `max_chain_overshoot`'s own noisy band (0.734–1.513) sits in the same general noise class this exact test has shown on IDENTICAL wrap-aware code across every round that's used it (ROUND 6: 0.68–1.02; ROUND 7/8/9/10/11/12: same mechanism, no new formula; ROUND 19/20's own flat-radius numbers were 0.651–1.288 and 0.836–0.923 respectively, for comparison, on a DIFFERENT, simpler formula) — i.e. this is the same pre-existing chronic run-to-run physics/floating-point non-determinism already documented since ROUND 7, not a regression introduced by restoring the formula. Inspecting where the peak overshoot occurred (all 3 runs): a brief transient in the first ~15 ticks of the adversarial sweep phase (right as sustained outward+tangential push begins after a stationary 90-tick settle), decaying to at-or-under capacity for the remaining ~280 ticks — the same bounded-transient-then-decay pattern this codebase has always accepted elsewhere (e.g. ROUND 2's own disclosed unspool jump), not a sustained/growing violation.
      - `tests/test_rope_leash_velocity_clamp.gd` (open-air/no-wrap fallback case, deliberately isolated from any obstacle): Scenario A converges to and holds exactly `max_dist_from_anchor=7.2000`, `max_single_tick_step=0.10000`, `teleport_ticks=0`; Scenario B (45°-angled push) settles at the same small self-limiting `~7.34` secant-creep band — bit-for-bit identical to ROUND 19/20's own recorded numbers, confirming the wrap-aware restoration has zero effect on the unwrapped case (exactly as expected — bound 1 of `_rope_leash_pivot_and_radius()` only ever engages on real obstacle contact) and that the velocity-clamp mechanism itself is untouched.
      - `tests/test_rope_physics_chain_settle.gd`, full suite: idle collapse PASS; all 8 settled-configuration obstacle-wrap sweeps PASS at `0.00000`; the 5-config steady-state jitter probe's own `LEASH-CLAMP-FIRING SUMMARY` shows `player_pos_fire_events=0/360` in EVERY config including `corner_wrap_anchor` (real obstacle contact the whole window, now able to engage the restored wrap-aware branch) — matching ROUND 19/20's own recorded 0/360 exactly, confirming a stationary player still never gets their position corrected even with the wrap-aware branch live again (nothing to correct when the player never moves); throw-unfold still shows the same pre-existing, already-disclosed ROUND 13/14 `max_joint_gap`/`max_reach_unfold` FAIL (this run: 5.71 / 10.35, within the same already-flaky family as the documented 3.5–4.4 band) — structurally impossible for this round's change to have caused, since `_rope_leash_pivot_and_radius()` unconditionally returns `[]` (a no-op) whenever `dart.state != ANCHORED`, and the entire unfold measurement window is during `FLYING`; retrieve-fold PASS (`dart_returned=true`).
      - `tests/test_rope_recall_wrap_path.gd` (both scenarios), `tests/test_movement_direction_skew.gd`, `tests/test_rope_obstacle_clip.gd`, `tests/test_rope_corner_tube_overshoot.gd` all re-run and PASS cleanly, unaffected.
      - **`tests/test_movement_direction_skew.gd`'s own diagnostic-only print block updated**: it had been hardcoded (by ROUND 20) to compute the flat-circle formula directly for its logging string; now calls `player._rope_leash_pivot_and_radius()` directly so the diag always reflects whatever the real bound is, regardless of which branch is live — no assertion/behavior change, matches ROUND 20's own precedent of keeping this print in sync with the real function.
      - **Multi-bot soak**: 4 hard bots, `GameManager.lobby_mode`/`human_count`/`total_players`/`bot_difficulty` toggled at runtime only via a temporary scratch scene/script (`tests/_tmp_soak_leash_wrap.gd`/`.tscn`, deleted before commit — never the files' own committed defaults, matching this project's established convention), real `scenes/main.tscn` via headless Godot for a sustained 60s/3600 ticks — zero NaN player positions on every 5s sample, zero `SCRIPT ERROR`/crash lines (only 2 instances of the already-documented benign headless-dummy-renderer `Parameter "material" is null` noise). `git status`/`git diff --stat` confirmed zero net trace of the scratch files after cleanup.
    - **Honest confidence caveat**: every number above is a direct measurement (re-running all four test files named in this round's own verification protocol, cross-referenced explicitly against ROUND 6's own original numbers rather than only ROUND 20's, plus a real 60s multi-bot soak) — not inferred from "no errors." Genuinely new and load-bearing this round: confirmed the restored wrap-aware bound beats ROUND 6's own original gating-metric numbers (penetration, fold events), not merely "no worse than ROUND 20." The `max_chain_overshoot` metric itself remains noisy across every round that has ever used this exact mechanism (ROUND 6 included) — this is disclosed as pre-existing, not claimed to be newly fixed. Not visually verified (no screenshot/video access this session) — same standing limitation as every round in this file; what's confirmed numerically is that the real, already-simulated physical chain length (hand → real segment path → dart) is once again the quantity the leash boundary is derived from, so a player can no longer satisfy the boundary check via a straight-line reading that ignores real wrap-consumed rope length.

### Rope dart move design reference

Three named moves, from design notes — current code implements Throw (Linear Strike) and Retrieval (Reel In) as described; the Wrap (Grapple/Bind) is not yet implemented (no bind/wrap mechanic exists today — a body hit currently applies `trip()`'s stagger, not a spiral bind).

- **The Throw (Linear Strike)**: The character delivers a directional thrust. The metal dart fires straight from the hand. The rope uncoils rapidly into a taut line. The asset maintains absolute linear rigidity at peak extension. Upon impact, the dart freezes briefly in place. The rope sags slightly to indicate lost momentum.
- **The Wrap (Grapple/Bind)**: The character initiates a wide, circular pivot spin. The dart follows a curved, high-velocity arc. The weapon makes contact with the target geometry. The dart rotates tightly around the impact point. The rope wraps around the object in spiral layers. The line snaps completely taut to lock the bind.
- **The Retrieval (Reel In)**: The character snaps their anchor hand backward. The dart dislodges instantly from the target. The weapon flies backward along the original trajectory line. The rope retracts dynamically toward the character's hand. The loose slack forms smooth, looping folds during recovery. The dart snaps back into the idle hand position.

**`bot_controller.gd`** — `Node`, attached as child `"BotController"` under bot players by GameManager
- Sets `parent.bot_controller = self` in `_ready()`.
- State machine: `CHASE → AIM → RETREAT`. Medium/Hard bots also dodge incoming darts via `_get_dodge_dir()`.
- `get_desired_throw()` consumes a one-shot `_throw_pending` flag; the player's `_prev_throw` tracking handles just-pressed detection correctly.

**`hud.gd`** — `CanvasLayer`, instanced from `scenes/hud.tscn` in main scene
- All UI nodes are created in code (no scene editor layout). `_build_skeleton()` creates the panel structure; `_setup_player_panels()` (deferred one frame) populates them after GameManager has spawned players.
- Connects to `GameManager.state_changed`, `round_ended`, `match_ended`, and each player's `player_killed` / `player_eliminated`.

**`arena_camera.gd`** — script on `Camera3D` in main scene
- Orthographic projection. Computes AABB of all alive players (by `lives > 0`) each frame and lerps `size` and position to keep them in frame.

**`arena_obstacle.gd`** — `StaticBody3D` script on pillar nodes
- Adds node to `"obstacles"` group. Exposes `get_rect_2d() -> Rect2` for the rope dart's swept-rect stop test — a plain 2D check, no physics-engine query needed.
- Also tags itself with an additional collision layer bit, `layer 2` (named `"rope_obstacles"` in `project.godot`'s `[layer_names]`), on top of whatever layer it already had (normally the default layer 1, left untouched) — this is the dedicated bit `player.gd`'s physics rope chain uses as its `collision_mask`, so the chain reacts to real obstacle geometry without ever being able to detect/collide with players, the ground, or the dart head (none of which carry this bit). One-directional by design: the chain's own `collision_layer` is left at `0`, so nothing else's mask can ever detect the chain back — this is what keeps a literally-simulated rope from leaking into gameplay (pushing a player, blocking movement, or interfering with the dart's own flight raycast).

## Node groups

| Group | Members | Used by |
|---|---|---|
| `"players"` | All player CharacterBody3D nodes | `rope_dart.gd`, `bot_controller.gd`, `arena_camera.gd`, `hud.gd` |
| `"darts"` | All active thrown-rope-dart Node3D instances (flying, anchored, or recalling, until picked up) | `bot_controller.gd` (dodge detection), `arena_camera.gd` (keep in frame) |
| `"obstacles"` | Pillar StaticBody3D nodes | `rope_dart.gd` (swept-rect stop check) |
| `"spawn_points"` | Node3D markers in main.tscn | `game_manager.gd` (spawn positions) |

## Duck typing convention

Player script properties (`player_index`, `is_dead`, `lives`, `get_pos_2d()`, `kill()`, etc.) are accessed from other scripts via duck typing on untyped variables. Use `for p in array:` (not `for p: Node in array:`) when iterating players, and `Variant` for signal callback parameters that receive player nodes, to avoid GDScript strict-mode type errors.
